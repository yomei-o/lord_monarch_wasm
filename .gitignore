# The Ghidra project is machine-local; decomp/ is committed so the analysis can
# be continued without Ghidra installed.  Rebuild either with the command in
# RESUME.md.
/ghidra_proj/

# Scratch: PNG dumps, the flat memory image, disassembly probes.
/tmp/

# Build output.
/tests/*.exe
/monarch.exe
*.o

# The PC-98 font ROM.  It is NEC's, not ours: the port can use one if you drop
# it here, but it is never committed or shipped.
FONT.ROM*
font.rom
*.ROM
