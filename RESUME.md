# RESUME — ロードモナーク (PC-98, 1991) を C に戻して native -> WASM

先行の [windepth_wasm](https://github.com/yomei-o/windepth_wasm) と
[super_depth_wasm](https://github.com/yomei-o/super_depth_wasm) と同じ手順。

**対象は PC-98 のオリジナル版**。1997 年の Windows 版
（[loadmonarch_wasm](https://github.com/yomei-o/loadmonarch_wasm)）とは
キャラクターも中身も別物で、流用するのは**展開コーデックだけ**。

## 対象

日本ファルコム「ロードモナーク」体験版フロッピー（1991）。
配布アーカイブは `orig/` にそのまま、展開した中身は `disk/`。
実機のスクリーンショット `ss0.jpg`（タイトル）と `ss3.jpg`（ゲーム画面）が
リポジトリ直下にある。**`ss3.jpg` は製品版の画面**で、枠 (`WAKU`) の絵は
体験版と同じだがマップとキャラクターは別物。だから色の答え合わせに使えるのは
タイトルと枠までで、地形タイルの色には使えない。

## 抽出は検証済み

* LZH は格納 CRC-16 と一致（`python tools/lzh.py orig/*.LZH` が `ok` を出す）。
  **イメージはバイト単位で正しい**
* `[FIM]` は **256 バイトヘッダ + 生セクタ**。ヘッダは
  `uint32 1 / uint32 0x2000（= 1 トラック 8192 バイト）/ uint32 0x9a（= 154 面）`。
  256 + 154 × 8192 = 1,261,824 = ファイルサイズちょうど
* FAT12: 1024 バイト/セクタ、1232 セクタ、FAT 2 枚 × 2 セクタ（論理 1..4）、
  ルート 6 セクタ（論理 5..10、192 エントリ）、データ開始 11
* サブディレクトリ無し・同名ファイル無し（**上書き事故の心配なし**）。
  `PROG.DAT` = クラスタ 2 から連続 8 個、`PROG.BIN` = クラスタ 10 から連続 42 個
* **イメージ形式の判定は拡張子ではなくヘッダ長の総当たり**（raw=0 / FIM=256 /
  FDI・HDM=4096）＋ BPB の妥当性。倉庫番の `[FDI]` も同じツールで読めるはず

180 ファイル / 1,147,014 バイト。

## 起動とメモリ配置 — 解読済み

`IO.SYS` と `MSDOS.SYS` は **0 バイトのダミー**で DOS は入っていない。
ブートセクタ（イメージ 0x100 から 1024 バイト、`tmp/boot.bin`）は
`DS = ES = CS` で動き、

```
mov ah,0x0d / int 18h            CRT 初期化
mov ah,0x42 / mov ch,0xc0 / int 18h   640x400 グラフィック
（FAT を CS:0x400 に、ルートディレクトリを CS:0xc00 と CS:0x1800 に INT 1Bh で読む）
mov word [0x3fd],0x376 / [0x3ff]=0x1000 / [0x401]=0      -> PROG.DAT
mov word [0x3fd],0x381 / [0x3ff]=0      / [0x401]=0x1000 -> PROG.BIN
ljmp 0x1000:0
```

ローダ（CS:0xf5）はルート 192 エントリを `repe cmpsb` で 11 バイト比較し、
クラスタを 3000:0000 に読んでから `les di,[0x3ff]` の先へ `rep movsw` で移す。
つまり **`[0x3ff]` がオフセット、`[0x401]` がセグメント**:

| | |
|---|---|
| `PROG.DAT` | **0000:1000**（linear 0x1000〜0x2f53） |
| `PROG.BIN` | **1000:0000**、エントリはオフセット 0 |

セクタ読みは `CH = 3`（= 128 << 3 = 1024 バイト/セクタ）、`CL` シリンダ、
`DH` ヘッド、`DL` セクタ（1 起算）、`ES:BP` バッファ。

### PROG.BIN と PROG.DAT は圧縮されている — 解読済み

**これが長いあいだ全部を狂わせていた。** ブートローダのファイル読み込みには

```
016e  mov ax,[di+0x16]    ディレクトリエントリの時刻フィールド
0171  and ax,0x1f
0174  cmp ax,0x1f
0177  je  -> 0x1bb        一致したら別経路
0179  ...                 素の rep movsw（こちらは死んでいる）
```

という分岐があり、**180 エントリ全部が `time & 0x1f == 0x1f`** なので
常に 0x1bb 側を通る。0x1bb は LZSS 展開器（`tools/lmz.py` に写した）:

```
uint16  展開後サイズ
uint16  最初のフラグ語、以降必要に応じて
...     ブロック

フラグ語は MSB から 16 ビット使う。
  0  リテラルブロック。カウントバイト n が続き
       n & 1 == 0 -> そのまま n>>1 バイトコピー
       n & 1 == 1 -> 1 バイト続き、それを n>>1 回書く
  1  マッチ。uint16 w が続き
       長さ = (w >> 12) + 3        (3..18)
       距離 = w & 0x0fff           出力へのバックリファレンス
```

**嵌りどころ: フラグ語の再読み込みは 16 回目のシフトの時点で、
その項目のオペランドを読む前に起きる。** ブートセクタでは
`shl bx,1` と `jb` の間に `dec dl / jne / lodsw` が挟まっている。
オペランドの後で再読み込みすると **15 項目まで正しく展開してから
ずれる**という、たいへん説得力のある間違い方をする。

| | 圧縮 | 展開後 | 置かれる範囲 |
|---|---|---|---|
| `PROG.BIN` | 42,040 | **58,268** | 1000:0000..1000:E39B |
| `PROG.DAT` | 8,020 | **11,290** | DS:1000..DS:3C19 |

**確認は二重に取れている**: `PROG.BIN` の先頭 2 バイトは `9c e3` = 0xE39C で、
コード中に `call 0xE36C` という確定位置の呼び出しがある（展開前の
42040 = 0xA438 では範囲外で説明がつかなかった）。`PROG.DAT` は
`1a 2c` = 0x2C1A、載る先が 0x1000 なので終端は 0x3C1A で、
startup が `mov [0x3c1a],bx` と書いている番地そのもの。
さらに両ファイルとも**ソースを 1 バイト残らず消費**して展開が終わる。

展開すると逆アセンブルは 89.9% が命令として通り、デコード不能は 6 箇所だけ
（展開前は 80.6% / 195 箇所で、しかも境界がずれていた）。

### PROG.BIN は CS = 0x1000 / DS = ES = SS = 0 で走る

先頭が `xor sp,sp / mov ds,sp / mov es,sp / mov ss,sp / mov sp,0x1000`。
**DS = 0 の裏付け**は 3 か所の PC-98 BIOS ワーク読み出し —
`[0x564]`、`[0x56c]`、`[0x584]`。最後のはブートセクタが
`xor ax,ax / mov es,ax / mov al,es:[0x584]` と明示的に ES=0 で読んでいる
ディスク装備バイトそのもの。だから PROG.BIN 中の `[0x1234]` は**全部 linear
低位メモリ**で、このファイルのオフセットではない。

低位メモリの地図:

| | |
|---|---|
| 0x0000-0x03ff | 割り込みベクタ |
| 〜0x1000 | スタック（SP=0x1000 から下に伸びる） |
| 0x1000-0x2f53 | PROG.DAT。**末尾は作業領域として使い回される**（0x1428, 0x2470 に書き込みがある） |
| 0x3200 〜 0x5exx | ワーク |
| 0xc400 〜 0xf3xx | ワーク |

startup が張るベクタ: INT 00h → `cs:0x73c`、INT 09h → `cs:0x7e0`、
INT 0Ah → `cs:0x755`、INT 13h → `cs:0x873`、INT 15h → `cs:0x8b6`。
元の INT 1Bh は `[0x3e7c]` に保存され、ファイル読みは 0x5706 の
`lcall [0x3e7c]` 経由。

## パレット — 解読済み

**ハードウェア側**（PROG.BIN 0x5db7）:

```
5db7  mov si, 0x3e20          ; DS:0x3e20 = 生きているパレット表（48 バイト）
5dba  mov bh, [0x34d6]        ; フェード値
5dbe  mov bl, 0               ; パレット番号
5dc0  mov al, bl / out 0xa8, al
5dc4  mov dx, 0xae            ; 0xae=BLUE -> 0xac=RED -> 0xaa=GREEN
5dc7  mov cx, 3
5dca  lodsb / and al,0x0f / inc al / mul bh / mov al,ah / out dx,al
5dd4  sub dx, 2
5dd7  loop 5dca
5dd9  inc bl / cmp bl,0x10 / jb 5dc0
```

* **1 色 3 バイト、順序は B, R, G**
* 値は `((byte & 0x0f) + 1) * fade >> 8`。`fade = 0xff` なら実質そのまま
* フェードは `[0x34d6]` 現在値 / `[0x34d7]` ステップ / `[0x34d8]` カウンタ /
  `[0x34da]` 目標表のアドレス。0x8e0c のクロスフェードが
  `[0x3eba+i] = [0x3e20+i] - [si+i]`（i=0..0x2f）で差分表を作り、
  最後に `mov si,[0x34da] / mov di,0x3e20 / mov cx,0x18 / rep movsw` で確定
* RAM 上の表の配列は `DS:0x249b`、0x30 刻み。startup 0x008b が
  `mov si,0x249b / mov di,0x3e20 / mov cx,0x18 / rep movsw` で初期化し、
  フェードは 0x24cb（0x912a）、0x24fb（0x914a）、0x252b（0x9294）を使う

**表の中身はディスクの、32×32 地形バンクの末尾にある。**
`B_0n0L.CH4` は展開すると 32816 バイト = 64 × (32×32×4/8 = 512) + **48**。
その 48 バイトが**素の 0〜15 の値**で並んだパレット。

```
0..9  共通    000 00A D00 AEE 0D3 0BF FD0 FFF E80 08C
10..15 set10  840 888 084 0AA BDB CCC     草原
       set20  840 AA7 DDA 774 552 BBB     電車の国（線路）
       set30  840 0B7 064 089 B60 999
       set40  800 446 007 88B BBE 888
       set50  B73 840 095 06A EA6 999     鉛筆王国
```

**なぜ長く見つからなかったか**（同じ穴を掘らないための記録）:

* `PROG.DAT` / `PROG.BIN` にはパレットは無い。ゲームがタイルセットと一緒に
  ディスクから読んで RAM に写している。両ファイルを 48 バイト窓で
  総当たりし、フェード値 150〜255 も、ニブル詰め 24 バイト形式も試して
  すべて空振りだったのは当然だった
* **`.CH4` の「余り」を見落としていた。** S と M は割り切れるが
  **L だけ 48 バイト余る**。展開後サイズを 512 で割った余りを見れば一発だった。
  プレーン画像（32000）と S/M バンクがきっちり割り切れたので
  「余りは無い」と早合点した
* index 0..9 が 5 セット共通で 10..15 だけ違う、という構造も見落としの原因。
  製品版のスクリーンショット (`ss3.jpg`) の枠から測れたのは 0..9 だけで、
  それは全セット共通の部分だった

**照合は 3 通り独立に一致した**:

| | |
|---|---|
| index 8 = `E80` | 製品版の枠から実測した (13,7,0) |
| index 7 = `FFF` | `org45.gif` の `#efefef` |
| index 1/2/3/5/6/9 | 上記どちらとも一致 |
| set50 の 10/11/13 | `org45.gif` のタイル形状照合の投票（`B_050S.CH4` が 51/145 一致）と一致 |

### タイトル画面のパレット — 解読済み（`DS:24fb`）

地形バンクが無いので `.CH4` 末尾の手は効かないが、**展開後の `PROG.DAT` の中**に
あった。オープニングの流れ（`sub_c946` → `0xc9b2`）:

```
（乱数で星の表を埋める。0x50 / 0xa0 / 0xf0 の 3 段階）
c9b2  call sub_5dfd        DS7TTL を 3 プレーン読む
c9b5  sub_7c1c 全画面矩形
c9fb  si=0x24cb を [0x3e20] に直接コピー
ca0f  sub_c6ad / sub_c612  フェードイン
ca15  [0x32d3]=0x1e でキー待ち
ca28  si=0x24fb / call sub_c630   ここへクロスフェード
```

* **`DS:24fb` がタイトルのパレット。** `ss0.jpg` から実測した値と
  index 2 = `4FA`、index 3 = `088`、index 4 = `477` が**厳密に一致**し、
  5 と 6 も細線の JPEG にじみの範囲内
* **index 0 は画面に出ない。** 表の index 0 は `06A`（青）だが、
  画面の背景は黒。ゲームは先に画面を消してから DS7TTL を
  **index 0 を透過として**重ねているので、ロゴの背後に見えているのは
  index 1 = `000`。この解釈で描くと index 1（背景 134,136 px）と
  index 3（ロゴ本体 15,888 px）が `ss0.jpg` と**厳密に一致**する
* `DS:24cb` はその前段の 2 色だけの表（8 個の `06A` と 8 個の `DDF`）
* 表の配列は `DS:249b` から `DS:267b` まで 0x30 刻みで、
  使うのはタイトルとエンディング。**ゲーム中のパレットは `DS:3eea`**
  （`B_0n0L.CH4` 末尾から写されたもの）

### フェードの中身

| | |
|---|---|
| `[0x34d6]` | 明度 0..0xff |
| `[0x34d7]` | 1 ステップの増減 |
| `[0x34d8]` | クロスフェードの残り |
| `[0x34da]` | 目標表のアドレス |
| `[0x3eba]` | 差分表 48 バイト |

* `sub_c612` — フェードイン。`[0x34d6] += [0x34d7]` を桁溢れするまで、
  毎回 `sub_9aa6`（パレット適用 + VSYNC 待ち）
* `sub_c630` — SI へクロスフェード。まず
  `[0x3eba+i] = [0x3e20+i] - [si+i]` を 48 バイトぶん作り、
  `[0x34d8]` を減らしながら `sub_c673` で補間して適用、
  最後に `rep movsw` で live = 目標

## 圧縮 — 解読済み（借り物）

ディスク上のデータは全部ファルコム自前の LZ（`.BZ` コーデック）。
Windows 版のために書いた `bz.c` がそのまま通る。**実測**:

| ファイル | 圧縮 | 展開 |
|---|---|---|
| `B_000.MAP` | 1175 | **2306** |
| `B_010S.CH4` | 2721 | 4640（145 タイル × 8×8 × 4 プレーン） |
| `B_010M.CH4` | 7622 | 16384（128 タイル × 16×16 × 4） |
| `C_010M.CH4` | 16761 | 32768（256 タイル × 16×16 × 4） |
| `DS7TTL.B1` | 7990 | **32000** = 640×400/8 ちょうど |
| `C_MOJI.DAT` | 1396 | 4096（128 文字 × 8×8 × 4） |
| `C_ICON.DAT` | 3956 | 8704（68 個 × 16×16 × 4） |
| `PROG.BIN` / `PROG.DAT` | — | **BZ ではない。生**（`bzcat` が 0 バイトを返す） |

## グラフィック — 形式は判明

* `.CH4` = 4 プレーンのタイルバンク。1 タイルにつきプレーン 0,1,2,3 の順に、
  各プレーンは行優先・MSB が左
* `.B1/.R1/.G1/.E1` = 640×400 画面のビットプレーン 4 枚を別ファイルに。
  パレット番号は `(E<<3)|(G<<2)|(R<<1)|B` で、**拡張子の順がそのままビット順**
  （`ss0.jpg` / `ss3.jpg` との突き合わせで確認済み）
* `B_nnnS/M/L` は同じ地形セットの 8×8 / 16×16 / 32×32 の 3 段。
  `C_nnnM` `C_nnnL1..L4` がキャラクター
* タイトルの星空は `DS7TTL` に入っていない。**コードが描いている**

## `.MAP` — 解読済み

展開後 2306 バイト = **48 × 48 セル（1 バイト/セル）+ `uint16`**。

* 行幅 48 は、候補幅ごとに「あるバイトと 1 幅後のバイトの平均絶対差」を測って
  最小のものを取って決めた。48 が 12.6、49 が 17.5、47 が 17.8、96 が
  第 2 高調波として出る（本物の画像の挙動で、偶然ではこうならない）
* 末尾の `uint16` は**地形セット番号**。52 本の内訳は
  10 が 12 本、20/30/40/50 が各 10 本で、ディスク上の 5 組
  `B_010* B_020* B_030* B_040* B_050*` にちょうど対応する。
  **マップは自分のタイルセットを自分で持っている**
* セル値 = バンク内のタイル番号。`B_nnnS/M/L` = 同じ地形セットの
  8×8 / 16×16 / 32×32 なので、同じマップが 3 段のズームで描ける
* 地形セットは 5 組でそれぞれ世界のテーマが違う —
  10 = 草原・森・山・海、**20 = 電車の国**（線路が道）、30 = 森と煉瓦の村、
  40 = 氷と青いパイプ・結晶、**50 = 鉛筆王国**（「HB」と書かれた鉛筆、
  六角形の鉛筆の束、青い罫線ノート、消しゴム、鉛筆削り）。
  体験版のテーマは鉛筆王国で、セット 50 を使うマップは
  B_014/015/016/029/030/031/044/045/046/051 の 10 本
* セル値の統計（全 52 本）: `0x00` = 草地 33%、`0x01` 11.8%、`0x60` 4.0%、
  `0x30` 3.7%。`0x30` 台が水面（タイルが index 1 の青と index 5 で構成される）、
  `0x00` は index 4 / 12 / 13 / 14 で構成される草地

`tools/map.py` がこれを絵にする。ss3.jpg の地図と照合しようとしたが、
**ss3 は製品版の画面でマップもキャラクターも体験版とは別物**なので
（一致率 78% 止まり）、パレット 10〜14 の実測には使えなかった。

## コードの地図（展開後アドレス、1000:xxxx）

`tools/lmdis.py` と `decomp/`（`sh tools/ghidra.sh` で再生成）から。

### 起動の流れ

```
0000  cli / xor sp,sp / DS=ES=SS=0 / SP=0x1000
000d  INT 13h の元ベクタを [0x3c1a] に退避
001a  ベクタを張る: INT 00h -> cs:073c, INT 09h -> cs:07e0,
      INT 0Ah -> cs:0755, INT 13h -> cs:0873, INT 15h -> cs:08b6
004d  INT 1Bh の元ベクタを [0x3e7c] に退避（ファイル読みはここ経由）
0059  sub_0187 / sub_01b9 / sub_024b  — 8255・PIC・PIT・FM の初期化
0104  sub_5f7e     BGM ロード（[0x3bc6] = 曲番号）
0107  sub_0ceb
010a  sub_e365
010d  sub_5ca2     C_MOJI.DAT（フォント）
0115  int 1ch AH=0, BX=0x3e70   カレンダ BIOS で日時を読み
011d  [0x3e75] 回 sub_9a36 を呼んで乱数を撹拌
0146  sub_74c6
0149  [0x249b] の 48 バイトを [0x3e20] へ（初期パレット）
0154  sub_5cef     C_ICON.DAT（アイコン）
0157  sub_5d0e     WAKU.*（枠 4 プレーン）
0184  jmp 0x191c   ゲーム本体へ
```

### タイトル画面（0x191c）

`[0x32d4] = 0x1e` を置いてキー待ちループ。`sub_c8d8` がキーを返し、
`and al,0x6f` で判定。`[0x32d4]` が割り込みで減って 0 になると
0x1a34（デモ）へ落ちる。

### ファイル名表と読み込み

**`DS:3302` から 12 バイト刻みで 30 エントリ**、FAT の 11 バイト形式。
番号やサイズ文字はその場で書き換える（`sub_c492` が数値を数字に、
`sub_c449` が `cx` 桁を `di` に書く）。

| エントリ | 読む場所 |
|---|---|
| `DS:3302 PROG    BIN` | `e36f` |
| `DS:330e C_000M  CH4` | `61c1` 以降（M と L1..L4 の 5 か所） |
| `DS:331a B_000M  CH4` | `610e`（S） `613c`（M） `615f`（L）|
| `DS:3326 B_000   MAP` | `6059` `62bc` |
| `DS:3332 FM000   DAT` | `5fac`（BGM ローダ `sub_5f7e`） `6002` |
| `DS:333e SAVE00S DAT` | `6372` 以降 |
| `DS:334a SCORE   DAT` | `6342` `637e` `6488` |
| `DS:3356 C_MOJI  DAT` | `5ca9` |
| `DS:3362 C_ICON  DAT` | `5cf6` |
| `DS:336e WAKU    B1 ` | `5d1a` `5d35` `5d50` `5d6b`（4 プレーン） |
| `DS:337a WAKU2   B1 ` | `5d94` 以降（4 プレーン） |
| `DS:339e DS7TTL  G1 ` | `5e09` `5e24` `5e3f`（**3 プレーンだけ**） |
| `DS:33aa GAKU    G1 ` | `5e63` 以降（4 プレーン） |
| `DS:33b6..345e END*`  | `cb19`〜`e1c0` |

読み込みの下請け: `sub_687e` が名前で探す、`sub_6f4b` が転送先を
`ax:di` で設定、`sub_68a0` が読む、`sub_6cb5` がエラー時の再試行。

### 地形バンクの置き場所と、パレットの行き先

`0x6105` の関数が `[0x3bc4]`（地形セット番号）で 3 本読む:

| | |
|---|---|
| `B_0n0S.CH4` | **7000:0000** |
| `B_0n0M.CH4` | **3000:0000** |
| `B_0n0L.CH4` | **4000:0000** |

L を読んだあと `4000:7ca0` から低位メモリへ 3 ブロック写す:

```
4000:7ca0 + 512 バイト -> DS:3c20
4000:7ea0 + 352 バイト -> DS:c632     16 バイト × 22 レコード
4000:8000 +  45 バイト -> DS:3eea     ← パレット 15 色ぶん (0..14)
```

`4000:8000` はちょうど 64 タイル × 512 の直後、つまり**末尾 48 バイトの
パレットの先頭**。index 15 だけ写していないのが面白いところ。
続いて `0000:c642` から 16 バイト刻みで 5 レコード、各先頭バイトから
0x10 を引いて `DS:3485` の 9 バイト刻みに書く。

### パレットの適用（0x7374 と 0x739f）

`[0x3372]` の判定で 2 系統に分かれる。

* `0x7374` — アナログ 16 色。`DS:3e20` の 48 バイトを 1 色 3 バイト
  B,R,G の順に 0xae/0xac/0xaa へ。`((byte & 0x0f) + 1) * [0x34d6] >> 8`
* `0x739f` — **８階調表示液晶**向け。各チャンネルを 1 ビットに落として
  出す（`shr ah,1 / sbb al,al / and al,0x0f`）。文字列にある
  「８階調表示液晶ディスプレイ」がこれ

## 描画のしくみ — 解読済み

### VRAM とタイル描画（`sub_8789`）

16×16 タイルの描画本体。38 か所から呼ばれる。

```
8789  ax = タイル番号
878c  ax *= 8 / ax += 0x3000 / ds = ax     ; タイルは (0x3000 + n*8):0000
8799  es = 0xa800                          ; VRAM
879e  al = 0xc0 / out 0x7c                 ; GRCG を RMW モードで有効化
87a2  dx = [si+0x60]   プレーン 3 (E)
87a5  cx = [si+0x40]   プレーン 2 (G)
87a8  bx = [si+0x20]   プレーン 1 (R)
87ab  ax = [si]        プレーン 0 (B)
      bp = ~(ax & bx & cx & dx)            ; マスク
87b6  out 0x7e に B,R,G,E の各バイト       ; GRCG のタイルレジスタ
87c5  stosb  マスクバイト                  ; 1 バイト書くと 4 プレーンに展開される
87dc  di += 0x4e                           ; = 80 - 2、次の行
```

これで確定すること:

* **VRAM は 80 バイト/行**（`di += 0x4e` と 2 バイト書き込み 2 回で 0x50）
* **タイルは 128 バイト**（4 プレーン × 32 バイト）で、`B_0n0M.CH4` を
  3000:0000 に読んだときタイル n は `(0x3000 + n*8):0000`。
  つまり `.CH4` はプレーン 0,1,2,3 の順という読みが正しい
* **index 15 が透過色。** `~(B & R & G & E)` をマスクにするので、
  4 プレーンすべて立っている画素は書き込まれない。
  裏付け: 16×16 の地形バンクは index 15 が**ちょうど 0.0%**、
  一方キャラクターバンクは 18〜27%（スプライトの背景）

### 画面の中の座標

`sub_54e2` は EDIT モードのタイル一覧を描く。`di = 0x1e3e` から
列送り 2 バイト × 8、行送り `0x4f0`。0x1e3e / 80 = 96 余り 62 なので
**(x=496, y=96) から 8 × 16 個の 16×16 タイル** — 右パネルの中。

## ゲームの中核データ — 解読済み

### ユニット配列 `DS:C800`

更新ループは `sub_32cc`（`0x1a50` から 1 か所だけ呼ばれる）:

```
32cc  [0xc52c] = 0
32d2  si = 0x40 / cl = [0x3c02] / si >>= cl / si--   ; 1 回で処理する体数
32de  si = [0x3bec] + 0xc800                          ; 巡回カーソル
32e6  al = [si]; ビット 7 が立っていれば飛ばす
32ed  al &= 0xfe                                      ; 「今回処理した」印を落とす
32f1  push 0x3318                                     ; ハンドラは ret でここへ戻る
32f4  [si+0]&2      -> 0x493a
32f8  [si+0x0c]==4  -> 0x3b86
32fe  [si+0x0a]&0x20-> 0x3a67
3305  [si+0x0a]&0x10-> 0x3608
3309  それ以外      -> 0x383e
3318  si += 0x1f0; 0xcc00 以上なら -0x400                ; 次のスロット
```

* **1 レコード 16 バイト、64 体**（窓が `0xc800..0xcbff` の 1024 バイト、
  刻み `0x1f0` = 31 レコードで 64 と互いに素なので全部を巡る）
* **`[0x3c02]` はゲーム速度**。1 回に `0x40 >> speed` 体を処理する。
  文字列にある「速い / 普通 / 遅い」がこれ
* 巡回カーソルは `[0x3bec]`

レコードの中身（`[si+N]` のアクセスを全部集計して割り出した）:

| | |
|---|---|
| +0x00 | フラグ。bit7 = 未使用スロット、bit1 = 別ディスパッチ、bit0 = 処理済み印 |
| +0x01 | タイマー（`6` を入れる箇所が多い） |
| +0x02 | word |
| +0x04 | **いる場所**。セル配列への*バイト*オフセット（= セル番号 × 2） |
| +0x06 | **運んでいる量**。セルから `xchg` で受け取って貯める |
| +0x08 | word |
| +0x0a | 状態。**下位ニブルが命令**で `cs:[bx+0x3a47]` の 16 本へ分岐、bit 0x10/0x20 がモード |
| +0x0b | 目標 / `0xff` が「無し」 |
| +0x0c | **所属勢力 0..3** |
| +0x0e | フラグ |
| +0x0f | カウンタ |

### セル配列 `DS:D27E` — 1 セル 2 バイト

`.MAP` は 1 セル 1 バイトだが、読み込んだあと **後ろから in-place で 2 バイトに
展開**する（`0x60c3`）:

```
si = 0xdb7d / di = 0xe47c / cx = 0x900 / ah = 0x64
cli / std / lodsb / stosw / loop / cld / sti
```

つまり各セルが `(タイル, 100)` になる。**上位バイトはそのマスが持っている量**で

* 1 目盛りごとに `add al,0x0a`（+10）で増える
* `add al,ah / jae / mov al,0xff` で 255 に飽和する
* `cmp byte [di+0xd27f], 0xc8`（200）というしきい値がある
* ユニットがそのマスに乗ると `xchg byte [di+0xd27f], al`（al=1）で
  **持って行き**、`[si+6]` に貯める

これがロードモナークの経済そのもの。

アクセスの変位が `±1, ±2, ±94, ±96, ±98` に固まっていることから
**行送り 96 バイト（= 48 セル × 2）**も裏付けられる。
48 × 48 × 2 = 4608 で、次の配列がちょうど `map + 4608`。

### 占有配列 `DS:E47E`

セルごとに 1 word で、そのマスにいるユニットのレコードアドレス（0 なら空）。
`mov word ptr [di + 0xe47e], si` と書かれる。こちらも 4608 バイト。

### 4 勢力の城はマップの中にある

ユニットのハンドラが `cell - 0x14 < 4` を `[si+0x0c]`（所属）と比べている。
**セル値 `0x14`〜`0x17` が 4 勢力の城**で、`.MAP` 52 本すべてに
**ちょうど 1 個ずつ**入っている（例: `B_000.MAP` は
(6,8) (30,11) (41,34) (5,39)）。

`src/game.c` にこの初期状態の組み立てを入れた。ビューアで `C` を押すと
城に印が出る（**原作にはそんな表示は無い**。解析用の目印）。

### 移動 — 解読済み

`0x3793` からの並びが 1 歩ぶん:

```
3793  di = [si+4]
3796  call sub_c291        次に向きたい方向 al を決める（AI / 経路）
3799  cmp [si+1], al
379c  jne -> [si+0x0e] = al / [si+1] = al / ret     向きが違えばこの回は向き変えだけ
37a5  al = [si+1] / call sub_9ad3                   方向 -> 差分 cx
37ab  dx = [si+2] / call sub_ad80                   dx に差分を足す。範囲外なら CF
37b3  call sub_9b34                                 dx -> bx = セルのバイトオフセット
37b6  cmp byte [bx+0xd27e], 0x30 / jae 塞がり        0x30 以上は通れない
37c9  cmp word [bx+0xe47e], 0    / jne 塞がり        既に誰かいる
37d6  [bx+0xe47e] = si / xchg [si+4],bx / [bx+0xe47e] = 0 / [si+2] = dx
37ec  塞がり: dec [si+0x0f]、0 になったら sub_adbe（別のユニットを確保）
```

* **`[si+1]` はタイマーではなく向き。** 初期値 6 = 右で、
  実際 2 体目は城の右隣に置かれる
* 方向は `DS:2827` の 8 エントリ表（`(dy<<8)|dx`）。
  0=上 で反時計回り: 上, 左上, 左, 左下, 下, 右下, 右, 右上
* `sub_ad80` が x も y も **1..0x2e** に収まることを要求する。
  つまり**外周 1 マスは立入禁止**（文字列の「外周部当り」がこれ）
* `sub_9b34` は `(y<<8)|x` を `(y*48+x)*2` に変換するだけ
* **1 マスに 1 体**。占有配列 `DS:E47E` がそれを保証している
* **城の 3×3 タイル `0x70..0x79` は `0x30` 以上なので通れない。**
  中央のマスに出入りできるのは右隣の `0x1d` だけで、
  開始時そこには 2 体目がいるので君主は箱の中にいる

### 経路探索 — 解読済み

セグメント **0x2000** が経路専用:

| | |
|---|---|
| `2000:0000..11ff` | 48×48 の word グリッド。flood fill が距離を書く |
| `2000:1200..23ff` | 2 面目 |
| `2000:2400..43ff` | 64 本の経路バッファ、1 本 128 バイト |
| `2000:4400..` | fill 自身のスタック（`[0x4400]` `[0x4402]`） |

* 経路バッファ = **124 バイトの 2 ビットステップ（496 個）** +
  word の長さ (+0x7c) + word の現在位置 (+0x7e)。
  496 は builder が `cmp ax,0x1f0 / jae` で拒む上限そのもの
* `[si+0x0b]` がバッファの番号。`0xff` は「経路なし」
* `sub_c050` の flood fill は走査線塗りつぶし。コストが一様なので BFS と
  同じ距離になる。**4 近傍のみ**（`±2` と `±0x60` しか出てこない）
* `sub_c0bd` が目標から距離場を最急降下しながら 2 ビットを詰める。
  コードは「どの隣が小さかったか」で **0=下 1=右 2=上 3=左**。
  逆向きに記録されるので、前から読んで `sub_c291` が 2 倍すると
  8 方向表の **上/左/下/右** になり辻褄が合う
* 歩き終わると `0xc2c0` が現在位置を進め、長さに達したら `[si+0x0b]=0xff`

### 世界の tick（`sub_3332`、`0x1a4d` から）

**勢力ごと**（4 つ、`0xc792` 刻み 0x16）:

```
[si+0]&8 なら飛ばす                    ; 脱落
[0x3c00] == その勢力なら飛ばす          ; 人間側は AI 調整しない
[si+0x10] == 0 なら bl = 18 - (資金>>8)、下限 1
[si+0x12]（レート、初期 10）を bl に 1 ずつ寄せる
```

**セルごと**: `(144 >> speed) - 1` 個。カーソル `[0x3bea]` は
**46 バイト = 23 セル刻み**で 2304 と互いに素なので全部を巡る。

| タイル | 処理 |
|---|---|
| `5` | 量が +10。255 を超えるとき空きマスなら**中立ユニットが湧いて量が 0 に戻る**（`0x3533`） |
| `0x08 + side` | 自領。下記 |
| `0x14 + side` | 城。下記 |

`sub_adeb` が 8 近傍を数えて「同じタイルの数 / 空き(タイル 0)の数 /
最後の空きの位置」を返す。

#### 自領のマス（`0x33d1`）

1. 量が「同じ自領の隣接数 + 1」だけ増える。**AI 側は `[0x347e]` が
   立っていると倍**。255 で飽和
2. 量が `(隣接数 + 1) × 16` 以下なら、ここで終わり
3. 隣接数 + 1 が **9 以上（八方すべて自領）ならユニットを産む**
4. そうでなければ、空き隣接があればそこを **`0x0c + side` の量 100** にして
   領土を広げる
5. さらに量が **200 以上ならユニットを産む**

ユニットを産む（`0x346a`）とき: そのマスが空いていれば、
**所持 = 量 - 1** のユニット（状態 1、その勢力）を置き、**マスの量は 1 になる**。

`sub_b7cf` は空き隣接が無いときに参照される分岐で、そこだけ未解明。

#### 城のマス（`0x3581`）— 徴税

```
si = そのマスにいるユニット。いなければ終わり
そのユニットがその勢力のもので、[si+0x0a] & 0x20 でなければ終わり
城の 2 マス右が他勢力の土地（0x08..0x0f のうち別の side）なら終わり
距離場を城から張って、
全 2304 セルのうち タイルが 0x08+side かつ 距離 < 0x4000 のものについて
    取り分 = (レート × 量) >> 8
    資金 += 取り分（[bx+0x0e] と桁上がりの [bx+0x10]）
    マスの量 -= 取り分
```

これで経済が閉じる — 土地が育ち、城が繋がっている土地から取り立て、
育ちきった土地がユニットになる。

**ただし世界の tick が触るのは `0x08 + side` だけ**で、広がった先にできる
`0x0c + side` は何もしない。開始時の `0x08` は 1 マップに 10 個ほどしかない
（52 マップで 561+504+643+539）ので、放っておいてもほとんど育たない。
`0x0c` を `0x08` に変えるのはユニットの仕事のはずで、そこがまだ未着手。
30000 tick 回すと資金だけが 22433 / 8874 / 8874 / 6937 と貯まる。

### ユニットの命令 — 一部解読

ディスパッチャ (`0x32cc`) から:

| 条件 | 行き先 |
|---|---|
| `[si+0]&2` | `0x493a` |
| `[si+0x0c]==4`（中立） | `0x3b86` |
| `[si+0x0a]&0x20`（君主） | `0x3a67` |
| `[si+0x0a]&0x10` | `0x3608` |
| それ以外 | `0x383e` → 下位ニブルで `cs:[bx+0x3a47]` の 16 本 |

16 本の中身は薄いラッパで、本体は `sub_3ec7` / `sub_3f2a` / `sub_3f62` /
`sub_4040` / `sub_4163` / `sub_41dc` / `sub_4247` など。

#### 状態 1 と 3 — 働き手（`0x3998`）

```
sub_3ec7 で今いるマスを開発。成功したら終わり
失敗したら sub_adbe（行動予算 [0xc4f2]）を消費して sub_4881 へ
予算が無ければ sub_af42 で自領の方向へ 1 歩
```

`sub_4881`（の `0x458e`）は:

```
自領の上にいるなら 状態 2、目標 = 現在地
そうでなければ sub_4814 で距離場を張り、sub_bc56 で最も近い自領を探す
持ち物が距離の 2 倍以上でなければ諦める
sub_c0bd で経路を作り、[si+0x0b] に経路番号、状態 2、[si+0x0f] = 4
```

`sub_bc56` は距離場を走査して **最も近い `0x08+side` のマス**とその距離を返す
（496 以上なら失敗）。`sub_af42` は上→下→左→右の順で最初に一致する隣の方向。

#### 開発（`sub_3ec7`）

```
今いるマスが 平地(0) か 0x0c..0x0f でなければ失敗
8 近傍に 0x08+自分 か 0x08+同盟 が無ければ失敗（sub_ae9e、土地は繋がっていること）
資金から 100 を引く（sub_abc7。資金は +0x0e/+0x10 の 32 ビット）
使う量 = min(持ち物, 200)
マス = (0x08+side, 使う量/2 + 1)
持ち物 -= 使う量。0 になったらそのユニットは終わり（sub_a9ca）
```

#### 拾い上げ（`0x34bb`）

自分の `0x08+side` のマスに乗っていると `xchg` でマスの量を持って行き、
マスには 1 が残る。持ち物は 0xffff で飽和。

#### 君主の死（`sub_a9ca`）

`[si+0x0a]&0x20` なら君主。`[si+0x0f]` が倒した側で、それを
勢力レコードの `+0x15` に書く。`+0x15` は世界の tick（`0x34ce`）が
**滅んだ勢力の領地を引き継がせる**先として読む。辻褄が合う。

#### 君主（`0x3a67` → `sub_3c7f`）

勢力レコードの `+0x08/+0x0a` は **その勢力の土地の合計量**（32 ビット）。
`sub_a6a5` が 2304 セルを走って `0x08+side` の量を足し上げる
（タイル 5 の巣は中立勢力に足す）。startup の `0x0140` から呼ばれる。

自分の城の上にいるとき:

```
所持 / 2 > 土地の合計量  ->  門（城の右隣）が空いていれば、
                             所持の 1/4 を持たせた働き手（状態 1）を置き、
                             君主はそのぶん失う
そうでなければ            ->  上限 = 土地の合計量（0xffff で丸め）として
                             所持 += (上限 - 所持)/4 + 1。届いていたら何もしない
```

つまり**君主の手持ちは土地が抱えている量を追いかけ、土地が枯れると
働き手が出る**。生産が自動的に足りるところで止まる仕掛け。

#### 未解読

* 目標選択の `sub_4814` / `sub_bd3b` / `sub_bd84`（AI と人間で分岐する）。
  移植では**そこだけ自前**にしてある（近くて自領に接する開発可能マスを選ぶ）。
  その先の処理は全部原作どおり
* 君主の `0x3a67`、中立の `0x3b86`、`0x10` モードの `0x3608`
* 資金の他の使い道: `imul ax,ax,0x1e`（×30、`0x40b5`）、
  `ax = dx>>2`（`0x3fbe` と `0x4007`）、`ax = dx*2`（`0x4106`）

### 地形コードの意味

| | |
|---|---|
| `0x00`〜`0x07` | 平地（`0x06` だけ特別で中立ユニットになる） |
| `0x08 + side` | その勢力の**生産する土地**。世界の tick が処理するのはこれだけで、育って広がってユニットを産み、城の徴税もここから取る |
| `0x0c + side` | その勢力の**ただ占めている土地**。tick では何も起きない。`sub_41b5` が他勢力のものを踏むと (0,100) に戻す。`0x08` の土地が広がるときに作られるのもこれ |
| `0x14 + side` | 城の中央 |
| `0x1d` | 城の右隣。ここだけ通れる |
| `0x30` 以上 | 通れない（水など）。城の `0x70..0x79` もここに入る |

## PROG.DAT の中身の地図（分かった分）

| オフセット | 中身 |
|---|---|
| 0x0000 | 小さなテーブル群 |
| 0x002b | SJIS メッセージ（「表示装置を選択してください。」「アナログＢＧＢディスプレイ」…） |
| 0x1490 | 8 バイト単位、JIS 風コードと SJIS かなの対応表 |
| 0x1560-0x15a4 | かな一覧（SJIS 2 バイトの並び、0 終端） |
| 0x15a5 | **2 ビット → 8 ビット倍化表** 16 バイト `00 03 0c 0f 30 33 3c 3f c0 c3 cc cf f0 f3 fc ff` |
| 0x15b5 | 0x33e8, 0x37d0, 0x3bb8 … と低位メモリ番地を含むレコード列 |
| 0x2302 | **ファイル名表**（FAT 8.3 の 11 バイト形式、12 バイト刻みで 30 エントリ） |
| 0x149b | **パレット表の配列**（`DS:249b`、0x30 刻み。index 0..9 は地形バンク末尾と一致、`DS:24fb` はタイトル画面の色） |
| 0x1868 | 称号「９級」〜「６段」、続いて「　０」〜「２１」の数字列 |
| 0x2059 | エンディングの台詞（殿・重臣・曲者・家来・敵兵） |

`tools/lmdis.py strings` で全部読める（254 本）。書式は独自のテンプレートで
`@3b` `@.2t` `@5w` `@S` `@O` `@?` `@o` `@10l` などが埋め込まれている。

## これから

1. ~~フロッピーからの抽出~~ 済（検証済み）
2. ~~ブートの仕組みとロード番地・セグメント~~ 済
3. ~~グラフィックのフォーマット~~ 済
4. ~~パレット~~ 済（`B_0n0L.CH4` の末尾 48 バイト）。タイトルの表だけ未発見
5. ~~`.MAP` の構造を確定して地図を絵にする~~ 済
6. `PROG.BIN` のゲームロジック（**大半解読**。初期状態・移動・経路探索・
   土地の成長と拡張・開発・拾い上げ・城の徴税・君主の生産まで移植済み。
   残るのは AI の目標選択（`sub_4814` / `sub_bd3b` / `sub_bd84`）、
   戦闘、中立ユニットの行動、`0x10` モード）
7. ~~ネイティブの描画土台~~ 済（`src/`、`make`）。ゲームロジックはまだ
8. ~~WASM（ソフトウェア描画のみ、WebGL 不使用）~~ 済（`make wasm` → `docs/`）

## ネイティブ側の現状

`make` で `tmp/monarch.exe`（Win32・8bpp DIB・2 倍表示）と
`tmp/monarch_shot.exe`（ウィンドウを開かない PNG 出力）ができる。

* `monarch_shot` の出力は `tools/map.py` の出力と**バイト単位で一致**する。
  FAT12・BZ・プレーン組み立て・タイル・マップ・パレット・PNG の全経路を
  Python 実装と突き合わせて確認済み
* `make shots` で `tmp/shots/` に確認用の PNG が並ぶ
* 枠の中の地図ウィンドウは **320 × 384**（`WAKU` でほぼ全高が空いている列が
  x = 160..479、行が y = 8..391）。16×16 タイルなら 20 × 24 セル分なので、
  48 × 48 のマップに対してスクロールする窓。8×8 で全体を出すには 384 px
  必要で入らないから、全体図は別画面（左パネルの `MAP` ボタン）と思われる
* **`tmp/monarch.exe` は実行して確認していない。** ウィンドウが開くので、
  デスクトップを使っている最中は動かさない方針。共通部分は
  `monarch_shot` で検証済みなので、残る未検証は DIB の blit だけ
* ホスト非依存の部分は `src/app.c` に寄せた。Win32・WASM・
  `monarch_shot view` の 3 つが同じ経路を通る

## WASM 側の現状

`make wasm` で `docs/monarch.js` と `docs/monarch.wasm` ができ、
`docs/index.html` がそれを読む。GitHub Pages が `docs/` を配信する設定なら
そのまま動く。

* **WebGL 不使用。** C 側で RGBA に描いて `putImageData` するだけ
* イメージは `--embed-file` で焼き込み。ディスク読み出しはネイティブと同じ C
* `node tests/wasm_check.js` — WASM を node で動かして生フレームを吐く。
  `HEAPU8` は `EXPORTED_RUNTIME_METHODS` に足さないと `undefined` になる。
  また `Buffer.from(HEAPU8.buffer, p, n)` は**ビュー**なので、次の描画で
  中身が変わる。`subarray` で複製すること
* `node tests/page_check.js` — `index.html` のインラインスクリプトを
  DOM スタブ上で実行して、キーが届くか・`putImageData` が呼ばれるか・
  Shift 併用で地図が変わらないかを見る
* **WASM が吐いたフレームとネイティブの PNG はバイト単位で一致**
  （タイトル / 地形 10 / 地形 50 の 3 枚）

## 環境

* Python `C:\Python313\python.exe`、mingw `C:\prog\w64devkit`、
  emsdk `C:\prog\emsdk\emsdk`（**`emcc.bat` は無く `emcc.exe`**）
* Ghidra 12.1.3 + JDK 21 を `C:\prog\ghidra` に。`ghidra_proj/` は事前に mkdir が要る
* capstone は Python に入っている。**objdump も使える**
  （`C:\prog\w64devkit\bin\objdump.exe -D -b binary -m i8086`）— 逆アセンブルで
  迷ったときの第二意見に有用

## 罠

* **PROG.BIN には 8086 の未定義エンコードがある。** 0x0084 の `fe e6` は
  `FE /4` すなわち `inc dh`。capstone も objdump も**両方これを拒否**するので、
  線形逆アセンブルがここで同期を失い、その先が全部ゴミに見える。
  「1 バイト余計に挿入されている」ように見えるが**そうではない**
* **Ghidra は生バイナリにエントリが無いと 0 関数になる。**
  `SetEntry.java` を `-preScript` で挟む。`sh tools/ghidra.sh` が
  展開後イメージを作ってから流すので、**330 関数・失敗 0** で出る。
  展開前に流していたときは偽関数 802 個でほぼ使えなかった
* **Ghidra 同梱の JDK を analyzeHeadless.bat は自力で見つけられない。**
  `JAVA_HOME=C:/prog/ghidra/jdk-21.0.12.1+1` を渡す
* **ファイル末尾を越える `call` は「圧縮されている」の兆候だった。**
  展開前は 2132 件中 351 件が 0xa438 を越え、そのうち 0x00b7 の
  `call 0xc6c2` は前後が一意に確定する位置にあった。展開後は全部収まる
* **Bash ツールのヒアドキュメントはバックスラッシュを食う。**
  バックスラッシュを含む編集は Edit/Write ツールで
* **Windows は大文字小文字を区別しない。** `cp a.mid A.mid` は自分自身への
  コピーになり、続く `rm` が原本を消す
* **planar と packed はサイズが同じことがある。** 4 通り全部 PNG に出して見る
* **エントロピーだけで「圧縮されている」と判断してはいけない。**
  PROG.BIN は全域 7.0〜7.3 だが生のコード＋データ表である（この作業で 3 回
  同じ間違いをしかけた）
* **`subprocess` に相対パスを渡すと CreateProcess が見つけられない。** 絶対パスにする
* **GUI は出さない。** PNG に描いて確認する（user が同じデスクトップで作業中）
