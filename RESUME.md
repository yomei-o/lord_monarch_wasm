# 引継ぎ（2026-09-03 夜の時点）

家の Claude へ。この節を最初に読めば続きから入れる。以下は全部 push 済み。
https://github.com/yomei-o/lord_monarch_wasm — Pages で動く。
`index.html` はバージョンを付けられないので、ブラウザ確認は **Ctrl+Shift+R**。

## いまの状態

**起動からゲーム開始までは原作どおりに通り、速さも直った。**
user 確認済み: 表示装置選択画面 OK、オープニング OK、ゲームスタートまで
バグ無し、ゲームの速さ OK。

流れは（全部 binary から確定させたもの）

1. **表示装置選択** `sub_06e7`（起動 `0x00b5`、ディスクを読む前）
   「表示装置を選択してください。」＋「アナログＢＧＢディスプレイ／
   ８階調表示液晶ディスプレイ」。取消不可（`ah = 0x23`）。答えは `[0x3286]`
2. **オープニング** `sub_c946`（起動 `0x0143`）
   `DS:0x24cb` の二値パレットでロゴがシルエット → 星が降る → 30 tick 保持 →
   `sub_c630` が `DS:0x24fb` へフェード → キー待ち。最下段は色 6 の箱に色 1 の
   文字（＝白帯に黒文字）と隅への斜め線 2 本
3. **マップ無しのゲーム画面** パネルだけが生きている
4. **GO** `sub_1afa` の `0x1b21` が `[0xce70]` の面を読み、`add sp,2` で
   パネルループを脱出

## ESC と Enter の効果音が鳴っていなかった（2026-09-03、user 指摘）

**原因は音の出し先ではなく、効果音ボイスの音色バイトの初期値。**

`sub_0e3f` は効果音ボイスを 30 バイト分 0 で埋めたあと、4 つだけ手で書く。
その 3 つめが

```
0e5f  mov byte ptr [di + 0x1f], 0x0f
0e63  mov byte ptr [di + 4], 0x80     <- [si+4] = トーンのみ・ノイズなし
0e67  or  byte ptr [di - 0x21], 0x40  <- 2 つ前の構造体（曲の SSG ch B）を借用
```

`[si+4]` は `sub_10ee` がレジスタ 7 の自分の 2 ビットに直すバイトで、
**bit7 でノイズを止め、bit6 でトーンを止める。両方 0 なら両方鳴る。**
19 個の効果音のうち `f4` コマンドで自分でこのバイトを立てるのは 6 個だけ
（0/1/2/5/11/17）。**4（拒否）・6（決定）・7（できない）は立てない**ので、
0x0e63 の初期値がそのまま音色になる。

移植は `start_at` の `memset` のまま `algo = 0` で走っていた。すると
mixer がノイズ源も開け（周期 0）、チップがトーンとノイズを AND するので
**音程が半分の振幅＋ヒス**になり、曲の下に完全に潜る。数字は
`tmp/fxcmp.c`（FM016 を 2 回描いて片方だけ 1 秒目に効果音を出し、
250ms ごとに 220/392/494/587Hz を測る）で:

| 494Hz 成分 | 直す前 | 直した後 | 効果音単体 |
|---|---|---|---|
| 曲の上で鳴らした効果音 6 | 285 | **553** | 550 |

単体再生（`snd_render_effect`）はレジスタ 7 に `0x3e` を決め打ちしていたので
**曲が鳴っていない画面だけ正しく鳴っていた**。だから最初の表示装置選択画面の
Enter は鳴り、ゲーム中の ESC / Enter は鳴らない、という出方になる。
その決め打ちも直した（効果音 2 は中身が `f4 40 f4 40` ＝ノイズのみなのに、
単体では音程で鳴っていた）。両方の player が同じ `algo_write`（= `sub_10ee`）
を通るようにした。

* `src/sound.c` `start_at` に `v->algo = 0x80`（`snd_start` / `snd_start_beep`
  はここを通る。`snd_start_bytes` は前から立てていた）
* `snd_render_effect` の `ssg_write(7, 0x3e)` → `algo_write`
* `sound_check` に 2 つ足した。**効果音 19 個すべてが `[si+4] = 0x80` で
  始まること**（これが直接の検査。逆に戻すと 19 行 FAIL する）と、
  494Hz の絶対値（正 225 / 壊れ 102）
* `index.html`: 曲が無いときの効果音ノードのゲインを 0.5 → 0.85。曲の上で
  鳴らすときと同じチップ・同じコードなのに、押した音の大きさが
  BGM の有無で変わっていた

**残っている懸念**: 効果音は SSG の 1 ch なので、曲が鳴っていると
ピークで曲の半分ほど（0.14 対 0.33）。原作もそうなので faithful だが、
user がまだ「小さい」と言うなら次に見るのは SSG と FM の音量比
（`ssg_render` の `v * 3`）で、あれは耳で決めた数字。

## 直近で直したこと（速さ）

**ゲームが 8.5 倍速かった。世界を毎フレーム進めていた。**
原作の主ループは

```
1a34  cmp byte ptr [0x32d1], 0
      je 0x1a3e                  0 なら世界を 1 歩進める
      jmp 0x1998                 でなければ何もせず回るだけ
1a3e  mov byte ptr [0x32d1], 8   進めたら 8 に戻す
      inc byte ptr [0x3be8]      ターン計数
      ... 掃引・日・終局判定 ...
```

で、`0x9a9e` に `cmp byte ptr [0x32d1],0 / jne 0x9a9e / ret` という空回り待ちが
ある。**このバイトを減らすのはループではなく割り込み** — ゲームが INT 0Ah に
入れる垂直同期で、PC-98 の 640x400 なら 56.42Hz。その **8 回に 1 歩**なので、
世界は毎秒約 7 歩しか動かない。`app.c` の `STEP_FRAMES` と `stepWait` がそれ。

`[0x3c02]`（ゲーム内 SPEED）は前から効いていた。掃引の数 `0x40>>速さ` と
`0x90>>速さ`、それに `game_day` のマスク `0xfe<<速さ`。**あれはこの待ちの上に
乗るもう一段の割り算で、代わりにはならない。**
1 日 = 8 フレーム × 4 ステップ = **32 フレーム**で、`app_check` がそれを
実測して確かめる（`a day took 32 frames`）。

あわせて検査側の古い前提を 3 つ直した（前から落ちていたもの）:

* `tests/page_check.js` に `putsBefore` の二重宣言があり構文エラーで起動しなかった
* `wasm_check.js` が `lm_init` 直後の 1 枚を「タイトル」として 4 色以上を
  期待していた。あれは `sub_06e7` の表示装置選択画面で 3 色が正しい
* `page_check.js` がタイトルを 4 フレーム目（まだ二値シルエット）で見ていた。
  フェードは 30 tick の保持の後なので 150 フレーム進めてから見る（7 色）

## 次にやること（優先順）

1. **ゲーム開始後の挙動**。user がまだ通し切っていない。特に命令
   2 拠点防衛 / 4 村を壊す / 5 村を作る が原作の関数
   （`sub_386c` / `sub_b037` / `sub_af6f`）と別物のまま — 詳細は下の節にある
2. **音**。YM2608 のリズム音色データは**この作品では使わない**
   （レジスタ 0x10〜0x1d を一度も書かない）ことは確認済み。詰めるなら FM の
   エンベロープ形と効果音の優先度あたり
3. EDIT (`sub_2368`) と FORM (`0x2081`) の 2 アイコン。窓の一覧は下にある
4. エンディング `sub_b661`（51 面クリアで到達）
5. 税率バーの水色マーカーの値。原作の式は `0x14 - (財布>>8)` だが初期状態では
   1 になり、税率 10 と一致しない。user は「水色＝現在の税率」と言っており、
   まだ食い違っている（下の節）

## 作業のしかた（守ること）

* **GUI を開かない。** PNG に描いて `Read` で見る。`tmp/monarch.exe` は
  一度も実行していない
* `tools/monarch_shot.exe <fim> view <map> <tile> out.png --fontrom
  font/shinonome.fnt --press 1,5,1 --frames 80` で任意の画面を撮れる。
  `--press` は APP_KEY_* の番号、`--frames` は演出を進めるため
* **WebGL 禁止**（対象機に無い）。ソフトで描く
* Bash のヒアドキュメントは**バックスラッシュを食う**。`\n` を含む C や JS を
  流し込むときは Write/Edit を使うか、**python のパッチを scratchpad の
  ファイルに書いてから走らせる**（ヒアドキュメント経由の `s.replace` が
  黙って空振りする事故を何度もやった）
* Windows のファイル名は大文字小文字を区別しない（`cp a.mid A.mid` は自分自身へのコピー）
* `s.index()` で範囲を切るときは**前方宣言に当たっていないか確かめる**。
  一度これで `app.c` を 2320 行消した（`git checkout` で復旧）
* **推測で埋めない。** この日は「両方のロゴを同時に出す」「BGM 4 がプレイ中の曲」
  のように、根拠の無い読みを user に二度指摘された。分からないところは
  binary を読み直すか、分からないと書く

## 検査（全部通ることを確認してからコミット）

```
bash tools/build.sh                 # native と wasm、index.html に md5 を刻む
timeout 900 ./tmp/app_check.exe     # 面 0 を勝ちまで打つ。60 項目ほど
./tmp/game_check.exe                # 52 面のルール、経路の 3 段、カレンダー
./tmp/sound_check.exe               # 音列と DFT
node tests/wasm_check.js            # wasm を node で。命令・スロット・音番号
node tests/page_check.js            # index.html を stub DOM で
```

node は PATH に無いので
`PATH="/c/prog/emsdk/emsdk/node/22.16.0_64bit/bin:$PATH"` を前に付ける。

## もう一つの移植（同じ日に始めた）

**倉庫番 Select 30** が `C:\prog\claude2\soko_ban_wasm` にあり、
https://github.com/yomei-o/soko_ban_wasm / Pages で動く。30 面・絵・音まで
通っている。あちらの `docs/format.md` と `docs/sound.md` に形式を全部書いた。
MMD2 の MML と YM2203 の使い方はあちらのほうが詳しいので、こちらの音を
詰めるときに読む価値がある。

## この日わかったことの要点（詳細は下の各節）

* **世界は毎フレーム進まない。** `[0x32d1]` に 8 を入れて垂直同期で減らす
  （上の節）
* `sub_724a` は **`al` = プレーンマスク、`ah` = 色**。これを逆に読んで
  選択画面を真っ黄色にした。`sub_7518` は色 0 ＝画面消去
* 選択行の帯は `sub_9239` が GRCG で「色 1 を読んで色 15 で書き戻す」＝
  **窓の地の塗り替え**。そして `0x07b6` が **entry 15 の赤**を三角波で振るので
  **選択行の背景だけが玉虫色に変わる**
* 文字色は `[0x32a9]` ではない。あのアドレスは**書かれるだけで誰も読まない**。
  色は直前の `sub_724a` が残した GRCG の状態
* パネルのカーソルは **C_ICON の piece 0〜3 の指差し手**。位置表 `DS:0x2055` は
  アイコンではなく**手の位置**（x−8, y+16）で、以前の謎が解けた
* パネルの点灯は `sub_4cdd` の 4 マスクで、**ガード表と一対一**
* `[0x3bee]`（パネルの選択）は初期化されず、**手は前回の位置に戻る**
* マップの端からパネルに入る分岐は**原作に無い**
* ページが `dirty || running` でしか描いていなかったので、
  **タイトルの演出も窓の帯も止まっていた**。原作は全部割り込みなので常時描画に
* 命令の「実行後」の 2 ビットは `0x3807` が読む。
  bit7 で継続、bit6 で待機（`[si+0xa] = 0x10`）、無しでオート
* 待機（state 0）は `sub_3867` =「右を向いて何もしない」。移植は
  `default:` で `decide()` していたので**待機していなかった**

## パネルの点灯マスクとカーソルの端（2026-09-03）

### `sub_4cdd` は GO ではなく「8 アイコンの点灯マスク」

```
4cde  dl = 0x76     ; 面が未読込（[0x3bc2] == -1）
4ce7  dl = 0x00     ; 面あり、[0x3bd4] 未セット
4cf0  dl = 0x03     ; 進行中
4cf9  dl = 0xf6     ; さらに [0x3bd6] セット（終了）
4d10  si = 0x22e5   ; VRAM 位置 8 個
4d16  bx = 0x840    ; piece の基準
4d19  al = 変化したビット → bit7 から左シフトで 8 回
      立っているビットは piece +0x20（暗い絵）、0 なら +0（点灯）
```

`DS:0x22e5` の 8 個は (16,24) (48,24) (16,56) (48,56) (16,120) (48,120)
(16,184) (48,184) = 移植の先頭 8 アイコン（GO VIEW TAX INFO SPEED ZOOM ALLY
EDIT）と完全一致。**bit7 が icon 0**。

マスクを開くとガード表そのもの:

| 状態 | 暗い | 点灯 |
|---|---|---|
| 面が未読込 `0x76` | VIEW TAX INFO ZOOM ALLY（全部 `sub_b509` 要求） | GO SPEED EDIT |
| 面あり `0x00` | — | 全部 |
| 進行中 `0x03` | ALLY EDIT（`sub_b52e`） | 他 |
| 終了 `0xf6` | SPEED と EDIT 以外（`sub_b509` の2条件目、GO は `0x1b10`） | SPEED EDIT |

移植は「実装済みかどうか」の固定配列だったので、どの局面でも合っていなかった。

### カーソルは端で止まるだけ

```
19cc  sub_9ab9       ; 方向を取る。無ければ 0x1a34
19d1  dx = [0x3be4]
19d5  sub_ad80       ; 動かす
19d8  jb 0x1a34      ; ★拒否されたら何もしない道へ
19da  [0x3be4] = dx
```

**マップループには端からパネルに入る分岐が存在しない。** 入口は取消ボタン
（`0x19be` → `0x1a9b`）だけ。「左端でさらに左を押すとパネルに入る」は移植の発明で、
しかもユニットを持っている最中に発動するので邪魔だった。削除。

## ページが常時描画していなかった（2026-09-03）

`index.html` の `frame()` は `dirty || lm_running()` のときだけ描いていた。
タイトルは `running` が 0 なので**最初の 1 フレームで止まり**、
星もフェードも進まず、3行の文字が出るところまで到達しない。窓が開いている間も
世界が止まるので選択行の脈動が止まる。

原作の演出は**すべて割り込み**（星が `0x0949`、パレット脈動が `0x07b6`）で、
他が何をしていても回る。だからページは**毎フレーム描く**ようにした。

移植側の脈動も `ticks`（`app_tick` の中でしか進まない）で駆動していたので、
専用の `pulseTick` に分けた。窓が開くと世界が止まるのに、
帯が動くのは窓が開いているときなので、これが致命的だった。

## パネルの手の初期位置

`[0x3bee]`（パネルの選択）は**どこでも初期化されない**。書き込みは
`0x4e26`（矢印）だけで、`0x1f5b`/`0x1f62` は LOAD ハンドラの前後で
push/pop して保存している。つまり**手は前回置いた場所に戻る**。
移植は ESC ごとに GO へ戻していた。

初期値は原作では未初期化なので、移植は 0（GO）にしている。

## 税率バーの水色マーカーの値

半分の色は画素値で確定した（左 = 2/8/6 の金、右 = 9/5/3 の水色）。
`sub_8b7a`/`sub_8e2b` はどちらも `lodsb` + `inc si` で**行送り 2 バイト**、
`stosb` で**1 バイト = 8 ドット**書き、`di += 0x4f` で次の行。だから
si=0 が左半分、si=1 が右半分で間違いない。

* 金 = `sub_4e9f`（`sub_8b7a`）が `0xc90 + [bx+0x12]` に描き、**動く**
* 水色 = `sub_4e49`（`sub_8e2b`）が `0xc90 + max(0, 0x14 - (財布>>8))` に
  **一度だけ**描く

初期化（`0x0355`〜`0x0362`）は `[bx+0x0e] = 0x1388`（5000）、
`[bx+0x12] = 0x0a`（10）なので、面の開始時は
**水色が 1、金が 10** の位置になる。これは移植の計算違いではなく原作の式そのもの。
徴収は `0x35ce` が `dl = [bx+0x12]` を使い `(税率 × マスの量) >> 8` を
財布に足してマスから引くので、税率フィールドは `[bx+0x12]` の一つだけ。

つまり `0x14 - (財布>>8)` は「いま実際にかかっている率」を財布から導いた値で、
`[bx+0x12]` はそこへ向けて設定する目標値、という読みになる。
実機で水色が金と同じ位置に見えるなら、どちらかの読みがまだ違う。

## 税率バーのマーカーは piece の左半分と右半分（2026-09-03）

`sub_8b7a` と `sub_8e2b` は `sub_8789` と同じ形のブロッタ
（`ds = 0x3000 + piece * 8`）だが、**1 行 1 プレーンあたり 1 バイトしか読まない**:

* `sub_8b7a` は `si = 0` から → piece の**左 8 ドット**
* `sub_8e2b` は `si = 1` から → **右 8 ドット**

そして piece `0x8a7` は**左半分が金・右半分が水色**の 2 つのマーカーを並べて
持っている。だから

| マーカー | 描く場所 | 意味 |
|---|---|---|
| **金**（`sub_8b7a`、`0x4eca`） | `0xc90 + [bx+0x12]` | **設定したい税率** |
| **水色**（`sub_8e2b`、`0x4e94`） | `0xc90 + max(0, 0x14 - (財布>>8))` | **現在の税率** |

`0x14 - (財布>>8)` が現在の税率という読みは user の実機観察から確定した。
つまり**実際にかかる税率は財布から導かれ**（金が増えるほど下がる）、
`[bx+0x12]` は目標値。財布の上位ワードが 0 でなければ（65536 以上）0。

piece 全体を両方の位置に描いていたので、両色が両方に出ていた。
`blit_half()` で半分ずつ描くようにした。

## `sub_724a` を読み違えていた（2026-09-03、user 指摘の訂正）

```
724a  and al, 0xf / xor al, 0xf / or al, 0xc0 / out 0x7c, al   ; プレーンマスク
7254  cx = 4
7257  shr ah, 1 / sbb al, al / out 0x7e, al / loop             ; ah のビット→色
```

**`al` = プレーンマスク、`ah` = 色**。だから

* `sub_7518` の `ax = 0x0f` は **ah = 0 → 画面を色 0 で消去**。
  「index 15 で塗る」と読んで選択画面を真っ黄色にしてしまった
* オープニングの `sub_7c1c(ax = 0x607)` は **色 6 の箱**、
  `sub_7ad3(ax = 0x107)` は **色 1 の線**

### 選択行の帯は「塗り替え」だった — `sub_9239`

```
9265  bx = 0x10                 ; 16 行
9268  ax = 0x10f; sub_7262      ; GRCG を色 1 の読み出しに
9270  push es:[di] ... loop     ; 行のワードを読む
9278  ax = 0xf0f; sub_724a      ; GRCG を色 15 の書き込みに
9283  pop es:[di] ... loop      ; 書き戻す
9288  di += 0x50; dec bx        ; 次の行
```

GRCG 越しに「色 1 で読んで色 15 で書く」＝**その行の色 1 の画素を色 15 に塗り替える**。
色 1 は `sub_91ea`（`ax = 0x10f`）が窓の地を塗った色。つまり
**帯は窓の地の塗り替え**で、色は 15。移植は色 2 の矩形を描いていた。

そして割り込みの `0x07b6` が動かすのは **entry 15 の赤**。
だから**選択行の背景だけが、時間をかけて色を変える**（玉虫色）。
許可フラグ `[0x32e0]` はオープニング中だけ 0（`0xc9fb`）で、
`0xca40` 以降は 1 なので、ゲーム中も動いている。

### 文字色は `[0x32a9]` ではない

`sub_759b` の制御バイト `0x10`〜`0x1f` は `0x7641` で `[0x32a9]` に書くが、
**バイナリ全体で `0x32a9` への参照はこの書き込み 1 箇所だけで、誰も読まない**。
文字色は**直前の `sub_724a` が残した GRCG の状態**。

だからオープニングの3行は、直前の `sub_7ad3(ax = 0x107)` が残した **色 1**。
`DS:0x24fb` では entry 6 が白・entry 1 が黒なので **白い帯に黒文字**、
`DS:0x24cb` ではどちらも同じ青なので**シルエット中は帯ごと見えない**。
最初に「行 326〜399 を色 0 で消して色 7 で書く」としたのは逆だった。

### 表示装置選択の座標

メッセージは `0x070e` が VRAM `0x231b` に描く → **行 112、バイト 27 = (216, 112)**。
メニューはその下。移植は (32, 192) に置いていた。

## パネルの音（`sub_1aa6` / `sub_4db2`）

| 操作 | 原作 |
|---|---|
| マップで取消（ESC）→ パネルに入る | `0x1a9b` が **0x602** を鳴らして `0x193f`（パネルループ）へ |
| 手を動かす | `0x4dfc`〜`0x4e29` は `[0x3bee]` を変えて `0x4dd8` の再描画へ直行。**無音** |
| 決定 | `0x4e2b` が **0x601**。そのあとアイコンのハンドラが自分の 0x602 を鳴らす |
| パネルから戻る | `0x4e36` は `stc` だけ。**無音** |

移植はパネルに入るときと決定のときが無音だった。

## user の指摘3件（2026-09-03）

### 1. ESC のカーソルは**手の形**

`sub_1aa6` が呼ぶ `sub_4d4a` がパネルのカーソルを描いている:

```
4d4c  al = [0x328a]         ; いまカーソルが載っているアイコン
4d53  cmp al, [0x3bee]      ; 移動先
4d59  sub_4d86              ; 古いのを消す
4d6a  di = [idx*4 + 0x2055] ; 位置（4バイト×14、先頭ワードが VRAM オフセット）
4d6e  ax = 0x898; sub_97e3  ; 下地を退避
4d74  ax = 0x8a0; sub_8738  ; ★C_ICON の piece 0〜3 を 32×32 として描く
```

`piece 0x8a0` は C_ICON.DAT の先頭なので **piece 0〜3**。実際に描いてみると
**握った手から人差し指を立てた形**（赤）。移植は矩形の枠を描いていた。

`DS:0x2055` の位置は x が 8 か 40、y が 40/72/136/200/264/296/328。
アイコン本体（x 16/48、y 24/56/120/184/248/280/312）に対して
**x−8, y+16**。以前「相対配置は合うのに原点が (−8,+16) ずれる」と書いた謎は
これ — **表は手の位置**で、手はアイコンの左下にはみ出して載る。

取消キーは移植では BACK。ページは Backspace と **Escape** の両方を割り当てていた
（実機の取消が ESC なので合っている）。

### 2. 表示装置選択画面は一色地で、その色が巡回する

`sub_06e7` の前処理:

```
06e8  [0x3e20] から 21 バイト 0    ; パレット entry 0〜6 を黒
06f2  di += 3                      ; entry 7 は残す
06f5  さらに 21 バイト 0           ; entry 8〜14 を黒
06fa  sub_7518                     ; ★VRAM 全体を 0xffff で塗る（GRCG mode 15）
```

`sub_7518` は `ax=0x0f` で `sub_724a` を呼び、`0xa800` に `0x3e80` ワード分
`0xffff` を書く。つまり**画面全体が index 15**。`DS:0x249b` では entry 7 が
白、entry 15 が `fa0`（橙）で、それ以外は黒。だから**橙一色の地に暗い文字**。

そして割り込みの `0x07b6` が `[0x3e4e]` を三角波で動かす:

```
07a7  cmp [0x32e0], 0    ; 許可フラグ
07ae  test [0x32da], 1   ; 1 tick おき
07b6  al = [0x3e4e]      ; = パレット 46 バイト目 = entry 15 の R
      15→0→(0xf0)→0xff→15 と往復
07ce  sub_736c           ; 毎回アップロード
```

`[0x3e4e]` は entry 15 の**赤**。画面全体が index 15 なので**地の色そのものが
巡回する**。許可フラグ `[0x32e0]` は演出の中（`0xc9fb`/`0xca40`）でしか書かれず、
この時点では未初期化 — 実機では動いていたという報告なので、移植でも動かす。

### 3. タイトルの文字が重なる

`sub_c946` は文字を置く前に

```
c9b5  sub_7c1c(0x607, (0,326)-(639,399))   ; ★最下段を塗りつぶす箱
c9c7  sub_7ad3(0x107, (0x49,326)-(0,399))  ; 左隅への斜め線
c9d9  sub_7ad3(0x107, (0x236,326)-(639,399)) ; 右隅への斜め線
c9eb  DS:0x3b4f の3行
```

を走らせている。`sub_7ad3` は `sub_724a` で色を決めて `0xa800` を
Bresenham 的に走る**線**、`sub_7c1c` が**箱**。DS7TTL は最下段にも絵を持って
いるので、**塗りを飛ばして文字を重ねると字が絡む**。行 326〜399 を index 0 で
消してから描くようにした。

## オープニングの正体と、命令の「実行後」（2026-09-03）

### `sub_c946`（起動 `0x0143`）は2段の演出

1. `DS:0x24cb` をパレットに置く。**0〜7 が全部同じ青、8〜15 が全部同じ白**なので
   DS7TTL のロゴは**二値のシルエット**になり、星（E プレーン）だけが動く
2. `DS:0x3b4f` の3行（CPU Power Level / FM sound Driver / Disk Caching）を描く
3. `sub_c6ad(0x949)` は**星の割り込み**を差し込むだけ（フェードではない）。
   `0x949` は再入防止のあと `0x0755` に連鎖し、`[0x3bb2]`/`[0x3bb4]` の
   星テーブルを VRAM セグメント 0xe000 に and/or する
4. `0xca15` で `[0x32d3] = 0x1e`、**30 tick 保持**。`and al, 0x60` でボタンを
   押すと `0xca35` へ飛んで演出ごと飛ばす
5. `0xca28` で `si = 0x24fb` を `sub_c630` に渡す。これがフェード:
   * `[0x3eba+b] = [0x3e20+b] - [si+b]` で48バイトの差分を作る
   * `[0x34d8]` を `0xff` から `[0x34d7]` 刻みで減らし、1 tick ごとに
     `sub_c673` が `target + ((|差分|+1) * blend >> 8)`（符号付き）を書く
   * 最後に目標パレットをそのままコピー
6. `0xca2e` でボタン待ち

`[0x34d7]`（刻み幅）は逆アセンブルの届く範囲に書き込みが無い。移植は 8 にした
（32 tick ＝ 直前の保持と同じ約0.5秒）。移植は最初から完成パレットだったので
演出が無かった。

index 0 は演出中も黒（ss0.jpg の実測。表の `06a` は使われない）。

### 命令の「実行後」は `0x3807` が読んでいる

第2メニューの答えは `ror ah, 2` で state の上位2ビットに入るが、**それを読む
場所がある**:

```
3807  al = [si+0xa]
380a  test al, 0x80
380e  ret                  ; bit7 → state をそのまま = 同じ命令を繰り返す（継続）
380f  add sp, 2            ; 戻り番地を捨てる
3812  test al, 0x40
3816  jmp 0x378e           ; bit6 なし → オートの道
3819  [si+0xa] = 0x10      ; bit6 → 待機
```

`ror ah,2` は 0/1/2 を 0x00/0x40/0x80 に写すので、メニューの
「オートにする／待機にする／〈命令〉を継続」とビットが一対一で対応する。
移植は作業が終わると必ず `decide()` していたので**第2メニューが無意味だった**。

### `CS:0x3a47` の16本と移植の対応

| state | 原作 | 移植 |
|---|---|---|
| 0 待機 | `sub_3867` = `[si+1]=6; ret`（何もしない） | **`default:` に落ちて `decide()` していた＝待機しない。修正** |
| 1 オート | `sub_3998` | `state_develop` |
| 2 拠点防衛 | `sub_386c`（城の隣かどうかで `sub_adbe`+`sub_4881` か `0x47e9`） | `game_develop`（その場を開発）— 近いが別物 |
| 3 援軍 | `sub_3998`（**1 と同じ**） | 1 と同じ ✓ |
| 4 村を壊す | `sub_38eb` → `sub_b037` | `state_outward` |
| 5 村を作る | `sub_38c1` → `sub_af6f` | `state_develop` |
| 6 柵を作る | `sub_3914` → `sub_3f62` | `game_job` ✓ |
| 7 橋を作る | `sub_3941` | `game_job` ✓ |
| 8 開墾 | `sub_3959` | `game_clear` ✓ |
| 9 柵を壊す | `sub_3934` | `game_job` ✓ |
| 10 橋を壊す | `sub_394d` | `game_job` ✓ |
| 11 洞窟封鎖 | `sub_3979` | `game_job` ✓ |
| 12 (相続) | `sub_3985` | ✓ |
| 13/14/15 | `sub_3867`（何もしない） | 13 と 15 は手前で処理済み |

`sub_af6f` は目的地の4近傍を見て、地形が `< 0x30` かつ所有バイトが 0 か
8〜15 のマスに `sub_ae9e` を呼ぶ（村の実体）。`sub_b037` がその逆。
2/4/5 を原作の関数どおりにするのは未着手 — `state_develop` が
`0x08+side`（生産地＝村）を作るので方向は合っている。

## 窓の全数点検（2026-09-03）

`sub_4a4d` / `sub_49bb` を呼ぶ全箇所と表アドレスを機械的に列挙して、
**ゲームが出しうる窓を一つ残らず**突き合わせた（`tmp/windows.txt` の作り方は
「`mov si, imm16` の 8 バイト以内に 0x4a4d か 0x49bb への call があるもの」）。
移植の抜けは 5 つだった。

### 抜け1 — 王の死の窓（`DS:0x11e3`、`0xaa50`）

`sub_a9ca` は王が死んだとき `[0xc546]` に国名を置き、
**「@Sの王が倒されました。」** を出して `0x302` を鳴らし `sub_c921` でキー待ち、
**そのあとで** `0xaa67` が `sub_b102` を呼ぶ。つまり

```
王が倒されました → (キー) → 滅亡しました → (キー) → 0x0f03
```

の3連鎖で、移植は2番目だけ出していた。

### 抜け2 — 同盟破棄の窓（`DS:0x11d5`、`0xb274`）

`sub_b102` の末尾。フラグ bit3 が立った国を数え、**ちょうど2国**になったとき
（`0xb230` の `cmp dx, 2`）最初の該当国の同盟を両側から外して
「@Sと@Sの／同盟は破れました。」を出す。4国＝2組なので、2国目が倒れた時点で
2対2が1対1になる。

### 抜け3 — `sub_b509` の2つめの条件

```
b50e  cmp [0x3bc2], -1   ; 面が無い
b515  cmp [0x3bd6], 0    ; ★面が終わっている
```

移植は前者だけ見ていた。`[0x3bd6]` は勝利（`0xb433`）と敗北（`0xb2d3`）で
立ち、`sub_6315`（進行）が消す。だから**終わった面はそのまま画面に残り、
どのコマンドも「マップをロードしてください。」で断られる**。

### 抜け4 — 得点なしの勝利窓（`DS:0x1119`、`0xb46b`）

勝利窓は2種類。`0xb38b` が「勝ったのが自分でない」、`0xb394` が
「面番号が 52 以上」を見て、どちらかなら `DS:0x1119`（6行、繰越日数の行が無い）
を出す。自分が 52 面未満で勝ったときだけ `DS:0x1105`（8行）。

### 抜け5 — 進行の条件がもう一段厳しい

```
b451  ax = [0x3bc2] + 1
b455  cmp ax, [0xce70]
b459  jne 0xb48a          ; ★一致しないなら sub_6315 を呼ばない
b45b  sub_6315
```

`[0xce70]` は `0xb3ec` で「クリアした面が到達面以上」のときだけ増える。
よって `[0x3bc2]+1 == [0xce70]` は「いま到達面が動いた」と同義で、
**すでに突破済みの面をもう一度クリアしても次の面は読まれない**。
`[0x3bd6]` が立ったままなのでパネルに戻り、マップを読み直すしかない。

### 文面の誤り2件

* `DS:0x10ff` は「@16t@s**実行後**」。移植は「を行う」と書いていた（書式エンジンが
  動く前の当て推量）。正しくは「〈命令〉実行後 → オートにする／待機にする／
  〈命令〉を継続」
* `DS:0x1129` は「@5w日 **繰越日数が増加**しました。」（表は使っていたので
  表示は正しく、コメントだけ誤り）

### 実装しないと決めたもの

* **国変更コマンド**（`0x1c6e` あたり、ガードは `sub_b509`＋`sub_b571`＋
  `sub_b553`）。`sub_b553` は `[0x3bc2] >= 0x34` でないと
  「このマップでは国変更はできません。」で断るので、**52 面以上専用**。
  体験版にその面は無く、パネルの14アイコン表（`DS:0x202d`）にも載っていない
* EDIT（`sub_2368`）と FORM（`0x2081`）の窓群 —
  `DS:0x1079`/`0x1081`/`0x10dd`/`0x11f5`/`0x11fd`/`0x1203`/`0x1209`/`0x120f`/
  `0x1215`/`0x1233`/`0x118f`〜`0x11c1`
* ディスク周りの窓 — `DS:0x10d3`（再試行/中止）、`0x116b`、`0x1171`、`0x1177`、
  `0x11ef`、`0x1223`、`0x12a5`、`0x11b1`

## オープニング `sub_c946`（起動 `0x0143`）

1. `[0x3bb4] = 0x80 >> (3 - [0x3c1e])` — 星の数を CPU 速度で決める
2. `0xce74` 以降に星の x と速度（`0x50`/`0xa0`/`0xf0` の3択）を乱数で敷く
3. `sub_5dfd` が **DS7TTL** の B1/R1/G1 を VRAM へ
4. `sub_7c1c` / `sub_7ad3` ×2 でロゴのスライドイン
5. `DS:0x3b4f` のリストを描く。**各レコードは「VRAM オフセット, 文字列, その
   引数」で 0xffff 終端**。`sub_759b` が `0x78b4` で `[bp+2] = bx` として
   **呼び出し元の si に終端の次を書き戻す**ので、ループは自力で進む

```
0x6919 (200,336)  "     CPU Power Level @1w"   [0x3c1e]
0x6f59 (200,356)  "     @? sound Driver"           [0x3b3a] → FM / BEEP
0x7599 (200,376)  "     Disk Caching @4w KB"   [0xc4e8]
```

6. `[0x32e0] = 0`、`DS:0x24cb` のパレット、`sub_c6ad(0x949)`、`sub_c612` でフェード
7. `[0x32d3] = 0x1e` の 30 tick 待ち。**`and al, 0x60` でどちらかのボタンで飛ばせる**

移植はこの3行を出していなかった。値は移植自身の答え（速度は最高の3、
`[0x3b3a]` は 1 なので FM、キャッシュは 1232KB = フロッピー1枚ぶん）。

## 起動時の画面遷移 — user の指摘どおりだった（2026-09-03）

user 曰く「オープニング画面→グラフィック選択→ゲーム画面マップ非表示→GOでマップ表示」。
binary を追うと**順序は「表示装置選択→タイトル→マップ無しのゲーム画面→GO」**で、
選択が先。

### 1. 表示装置の選択 `sub_06e7`（起動の `0x00b5`、ディスクを読む前）

```
0x0090  in al, 0x42 / test al, 0x10   ; 8255 ポートB のビット4 = 機種の答え
0x0096  [0x3286] = 1                  ; それを初期値にする
0x00b5  sub_06e7
  0x070e  DS:0x1026「表示装置を選択してください。」を sub_759b で直接描く
          （descriptor ではなく生文字列。VRAM 0x231b）
  0x0717  DS:0x101c を sub_49bb へ、答えは [0x3286]
          ヘッダは 6 バイト `0a 06 02 0e 02 00` = セル(10,6)、2行、全2行
          ポインタは DS:0x1022 →「アナログＢＧＢディスプレイ」
                                「８階調表示液晶ディスプレイ」
  0x0720  ah = 0x23 → 上下と決定だけ。**取消は無い**
```

`DS:0x1022` を文字列の先頭と読んで「C^表示装置を…」になったのが最初の躓き。
その2バイトは1本目のポインタ。

### 2. `[0x3286]` が変えるもの — `sub_736c` の2本の腕

* `0x7374` アナログRGB。`DS:0x3e20` の 4 ビット×3 をそのままポートへ
* `0x739f` 8階調。`sub_73d4` が **4×緑 + 2×赤 + 青**（緑が最重 = 輝度）を作り、
  `[0x34d6]`（明るさ）を掛けて 15 で割り、**`not`** して 0〜7 にする。
  `0x739f` はそのビット 0/1/2 を青/赤/緑に「全点灯か消灯」で書く。
  黒（合計0）は 7 = 全点灯

つまりゲームが出すのは色立方体の8頂点で、**それを 8 段の灰色に写すのは LCD
パネル側**。だから移植では 8 頂点を灰色ランプに落とす（`not` があるので
黒は黒・白は白に落ち着く）。色として出すとマゼンタと黄色の塗り分けになる。

### 3. マップは GO まで出ない — `sub_1afa`

```
[0x3bc2] == 0xffff        ; 面が未読込。起動時はこれ
  → [0x3bc2] = [0xce70]   ; 到達面
    sub_6033              ; 面を読み込む ← プログラム中ここだけ
    → 0x1b37
0x1b37: [0x3bd4] = 0xffff ; 進行中にする
        add sp, 2 / ret   ; 戻り番地を捨ててパネルループを脱出
```

`sub_1aa6` がパネルのループで、抜ける道が GO しかない。`sub_4cdd` は
`[0x3bc2]`/`[0x3bd4]`/`[0x3bd6]` から **GO アイコンの絵柄**を選んでいる
（未読込 0x76、進行中 0、敗北保留 3）。

移植は起動でいきなり 0 面を読んで世界を描いていた。いまは
`app_show_ready()` が枠とパネルだけ出し、GO が `reached` の面を読む。
枠内は黒で消す — WAKU が x 96/128 にアイコンの薄い複製を隠し持っており
（`snapshot_dim_icons` の取得元）、マップが無いと素材が見えてしまう。

### 4. 起動時に読むもの

* `sub_5d0e` = WAKU の各プレーンを VRAM へ（枠）。`sub_5dfd` が DS7TTL
  （タイトル絵）で、呼ぶのは `0xc9b2` だけ
* `sub_633b` = **SCORE.DAT** を `DS:0xcc00` に 628 バイト。これが campaign の
  進行表で、読めなければ 0 クリアして `sub_acbb` で累積を再計算。
  スロットとは**別ファイル**（スロットは `SAVEnnS.DAT`）
* `sub_054b` = 「プログラムディスクを取りだし／ユーザーディスクをセットして
  下さい。」（`DS:0x11b1`）。フロッピー固有なので移植では出さない
* `[0x3bc6]` は **BGM 番号**で `FM0nn.DAT` を読む（`sub_5f7e`）。
  起動 4、設定画面 5、面は `16 + 2*(terrain/10 - 1)` の偶数と、
  `or [0x3bc6],1` で奇数の対

## LOAD / SAVE 実装（2026-09-03）

窓は原作そのまま（`DS:0x10bd` の 1 本のテンプレートを行番号で引く、80 枠中 10 行）。
`DS:0x170c` の解読どおりに動くようにするため、書式エンジンに 3 つ足した:

* `@.Nt` — 行番号 `[0xc54c] - 1` × N + 引数。1 語消費（`0x77b4`）
* `@.o` — 引数**そのもの**をベースに（`0x7746`）。`@o` は「引数の指す語」を
  ベースにするので移植では相当物が無く `FMT_BASED` 扱い（`0x7735`）
* `@O` — 呼び出し元のベースを継承。**引数を1語も消費しない**（`0x7754` は
  `bx` を動かさない）。ここを 1 語消費にしていたため日付が
  `9/03/11 50` とずれ、`MAP-No.` が消えていた

一覧は `1 : MAP-No.001 beginning 26/09/03 11:51` と出る。
`beginning` は `[0x3bd4]`（面が進行中）が立っていないとき（`0x64e8`）。

スロット表は `DS:0xf67e` の 12 バイト × 80 をそのまま模す:
`+0` 使用中、`+2` 日数（0 なら beginning）、`+4` 面番号（0 なら
`Only cursor position`）、`+6..+10` が 年/月/日 時:分。

中身はフロッピー互換にできないので移植独自の 1 枚のバイト列
（`SaveBlob` = マジック + ディレクトリ項目 + `StageRecord[52]` + `reached`
+ `Game` = 19080 バイト）。ディレクトリ項目も同じ配列に入れてあるので、
ホストは中身を知らずにバイトを出し入れするだけでよい。

* C 側 — `app_slot_bytes` / `app_slot_put` / `app_slot_stamp`
* wasm — `lm_slot_buffer` / `lm_slot_capacity` / `lm_slot_read` /
  `lm_slot_write` / `lm_slot_used` / `lm_slot_stamp`
* index.html — `lm.slot.N` キーで localStorage に base64。`lm_slot_stamp` が
  変わったフレームだけ書き出す。読めない／古い形式の枠は消す

`0x1f39` の「空の枠は選べない（一覧に戻る）」も入れた。取消が窓を閉じる側で、
そのとき `0x602` が鳴る（`0x1f27`, `0x202c`）— ドライブ窓と同じ逆転。

### 直したついでのバグ

スクロール窓の選択行を `dlg.first + dlg.pick` で読んでいたので、
箱が先頭以外にあるとき別の項目を選んでいた（面一覧にも同じ問題があった）。
描画と同じ `first + (pick - top)` にした。

## アイコンシート `C_ICON.DAT` が読めた（2026-09-03）

* `sub_8789(ax = piece, di = VRAM)` は `ds = 0x3000 + ax*8` の 128 バイトを
  16×16 として描く。1 piece = 16行 × 2バイト × 4プレーン、プレーンは
  `+0`/`+0x20`/`+0x40`/`+0x60` の順（`0x87a2`〜 の読み順そのまま）。
  **移植の `gfx_load_bank(..., 16)` がそのまま使える形式**
* `0x5cef` が `C_ICON.DAT` を **セグメント 0x7000、オフセット 0x5000** に読む
  （`sub_6f4b(ax = segment, di = offset)`）。よって
  `0x30000 + p*128 == 0x70000 + 0x5000` から **piece `0x8a0` が先頭**
* BZ で 3956 → 8704 バイト = **68 piece**。中身は
  0〜3 が小物、**4〜7 がスライダのレールとつまみ**、
  以降がパネルアイコン（32×32 = 2×2 のまとまり）、
  カーソルキー／BS／DEL／INS／END のキーキャップ、漢字4枚
* `C_MOJI.DAT` は 1396 → 4096 = 32 piece（`0x2000:5000` へ）

### 税率窓を原作の絵にした

```
0x4b61  枠。bx=0x0101 cx=0x1302 → セル(1,1)＝(112,24)、19セル × 2行
0x4e53  レールを VRAM 0xc8f＝(120,40) に:  0x8a4、0x8a5 ×15、0x8a6
0x4e94  つまみ 0x8a7 を 0xc90 + (0x14 - (財布>>8)) に（税率ではなく財布）
0x4eca  税率のつまみ、同じ piece を 0xc90 + [bx+0x12] に
0x4ed3  DS:0x1ae4 = "@2t@o@2b%" を 0xcb1＝(392,40) に
```

`0xc90` は行40のバイト16なので税率1段＝8ピクセル。0〜30 が 128〜368 に載り、
レールは 120〜392。

## LOAD / SAVE（解析完了、実装は次の単位）

### 窓

* 題名 — LOAD `DS:0x1183`「ゲ ー ム ・ ロ ー ド」、SAVE `DS:0x1189`「ゲ ー ム セ ー ブ」
* 一覧 — `DS:0x10bd`。ヘッダは `04 01 8a 15 50 00` なので
  **x=4 y=1、見える行 10、bit7 立ち（テンプレート1本を行番号で引く）、
  高さ 21、全体 80 行**。選択は `[0x3bf6]`
* テンプレート `DS:0x170c` = `@2b : @.12t@.o@?`、引数
  `c54c f67e 0000 0000 174d 1729`

  1. `@2b` ← `[0xc54c]` = **今の行番号**（`sub_759b` が持つ）
  2. `@.12t` ← `0xf67e + (行 - 1) * 12` を次の枠に書く
  3. `@.o` ← その値を**ベース**に（`0x7746`: `ax = [bx]`）
  4. `@?` ← ベース+0 が 0 なら `DS:0x1729`（`-----`）、非0なら `DS:0x174d`

* 使用中の行 `DS:0x174d` = `@O@? @2b/@.2b/@.2b @2b:@.2b`
  引数 `0004 1779 1792 0006 0007 0008 0009 000a`
  → ベース+4 が非0なら `DS:0x1779`、0なら `DS:0x1792`、そのあと
  **+6/+7/+8 が 年/月/日、+9/+10 が 時:分**
* `DS:0x1779` = `@OMAP-No.@.3w @?` 引数 `0004 0002 17a9 17b8`
  → ベース+4 が面番号（3桁）、ベース+2 が 0 なら `DS:0x17b8`「beginning」、
  非0なら `DS:0x17a9` = `@O@5w DAYS`
* `DS:0x1792` = `@OOnly cursor position`

**スロット表は `DS:0xf67e`、12 バイト × 80。** `0x1f39` の
`cmp word ptr [si - 0x982], 0` が「+0 が 0 なら空」。空を選ぶと一覧に戻る。
取消は効果音 `0x602` を鳴らして戻る（`0x1f27`, `0x202c`）。

### 中身（`sub_6473`）

1. 進行表 — `0x6495`: `DS:0xcc00` から **0x274 = 628 バイト**。
   52 面 × 12 バイト + 到達面 `[0xce70]`
2. 変数群 — `0x64d8`: `DS:0x3bc2` から **0x57 = 87 バイト**。
   面番号・経過日数・残り日数・カーソル・速度・拡大・人間側など
3. `[0x3e94]` がスロット表の該当エントリを指し、`[di+0x0e]` に日数
   （面が進行中で敗北保留でないときのみ、それ以外は 0）、`[di+0x10]` に面番号
4. `0x650d` で面が進行中なら**さらに盤面**を書く（未追跡）

ファイル名は `DS:0x333e` / `DS:0x334a` に組み、`[0x3344] = 'S'` で2本目を区別。

### 移植方針

フロッピーが無いのでバイト互換は不可能。同じ**事実**が残ればよい:
面ごとの成績表（`StageRecord`、実装済み）、到達面、そして進行中なら
`Game` 構造体そのもの。ブラウザ側は localStorage、native はファイル。
窓は原作の一覧をそのまま出せる（`@.Nt` の行番号形式と `@.o` / `@O` を
`fmt_render` に足す必要がある。いまは `@.Nt` を読み飛ばしている）。

## 「つまみ」の窓は3つ、規約はメニューと別（2026-09-03）

税率 `sub_4f08`、速度 `sub_539f`、拡大 `sub_52b7` は同じループの3回繰り返し。
**メニュー（`sub_4be9`）とは規約が違う。**

| | メニュー `sub_4be9` | つまみ |
|---|---|---|
| 受け付けるキー | `and al, 0x63`（上下・決定・取消） | 税率だけ `and al, 0x6c`（**左右**）、速度と拡大は `0x63`（上下） |
| 移動できたとき | 音 `0x0500`（`0x4c2e`） | **無音**（`0x53e2`→`0x53b3`、`0x4f56`→`0x4f1a`) |
| 端に来たとき | 無音で何もしない | 同じ（`0x4f45`/`0x53d3`/`0x52eb`） |
| 決定 | 音 `0x601`、CF クリア | 同じ |
| 取消 | **`stc` だけ。無音**（`0x4c94`） | **音 `0x601`** |
| 値の反映 | 決定時に呼び出し側が読む | **移動ごとに実バイトへ書く** |

そして取消の意味が3つで揃っていない:

* 速度 — `0x53a1` が `bh = [di]` で保存、`0x53f1` が書き戻す → **元に戻る**
* 拡大 — `0x52b9` / `0x5309` が同じ → **元に戻る**
* 税率 — `0x4f66` は何も戻さない → **取消でも設定された値が残る**

移植は3つとも「決定時にだけ反映」だったので、つまみを動かしても世界の速度が
変わらず、税率は取消で戻っていた。

税率の範囲は 0〜30（`0x4f4d cmp ah, 0x1e`）、速度と拡大は 0〜2
（`0x53d9` / `0x52f1 cmp ah, 2`）。速度は `[0x3c02]` そのまま、拡大は `[0x3c04]`。

### 税率窓のもう一つのつまみ

`sub_4e49` はバーを 1+15+1 = 17 個の 16×16 piece（`0x8a4`/`0x8a5`×15/`0x8a6`）で
VRAM `0xc8f` = 画面 (120,40) に描き、そこに `0x8a7` のつまみを置く。ところが
その位置は税率ではなく **`0x14 - (財布 >> 8)`**（`0x4e7f`〜`0x4e85`、`[bx+0x0f]`
と `[bx+0x10]`）。税率のつまみは別で `sub_4e9f` が `0xc90 + [bx+0x12]` に描く。
つまり窓には**財布の目安と税率の2つのつまみ**がある。

なお `0x4e71` は `bl = [0x3c00]` のあと **`add bx,bx` を忘れている**
（他の全箇所は倍にしてから `[bx+0x12ef]` を引く）。人間側が 0 国なら影響しない
ので表に出ないが、原作側のバグと見られる。

### タイル絵の出どころ（未実装、道筋は判明）

* `sub_8789(ax = piece, di = VRAM)` は `ds = 0x3000 + ax*8` から 16×16 を1枚描く
  （1 piece = 128 バイト = 16行 × 2バイト × 4プレーン）。
  `sub_8738` はその 4 枚を 2×2 に並べて 32×32 にする
* `C_ICON.DAT` は `0x5cef` が **セグメント 0x7000、オフセット 0x5000** に読む。
  よって piece 番号 → 線形 `0x30000 + p*128`、`0x70000 + 0x5000` と合わせて
  **piece `0x8a0` が C_ICON.DAT の先頭**
* `C_ICON.DAT` は `.BZ` で 3956 → **8704 バイト = 68 piece**（移植の `bzDecompress`
  でそのまま展開できた）。`C_MOJI.DAT` は 1396 → 4096 = 32 piece
* つまり税率バーは C_ICON の piece 4〜7。これを描けば原作の見た目になる

## 音（YM2608 リズム音色データの件、2026-09-03）

user から「YM2608風リズム音源音色データ Ver.2.0 を使わないとダメ」との指摘。
uraraworks/WebNP2 が同梱している `2608_bd/sd/top/hh/tom/rim.wav` の6本で、
**OPNA のリズム（ドラム）チャンネル用の代替サンプル**。np2 が本物の 2608 ADPCM
ROM を持たないための差し替え。

**ロードモナークはこれを使わない。** 根拠:

* OPN レジスタを書くのは `sub_740d` と `sub_7439` の2本だけで、どちらも
  ポート `0x188`/`0x18a`。レジスタ番号は `dh`
* `dh` に 0x10〜0x1d が入る経路が存在しない。バイト列 `ba ?? 1x` の一致は5箇所
  あるが全部描画コード（`0xcca9` は `sub_9aa6` の引数、など）
* SSG のハードウェアエンベロープ（`0x0b`/`0x0c`/`0x0d`）も一度も書かれない。
  レジスタ書きは `ch*2`/`ch*2+1`（音程）、`6`/`7`（ノイズとミキサ、`sub_10ee`）、
  `8+ch`（音量、`0x10e8`）だけ
* 減衰は driver が音量を書き直す**ソフト処理**。SSG は `sub_10c2`
  （`ah = (level * ([si+6]+1)) >> 8`、`[0x3b3f]==0` なら `[0x3b42]` も掛ける）、
  FM は `sub_1488`（`DS:0x2445` のアルゴリズム別キャリアマスクに
  `0x48 - ((0x48 - vol) * [0x3b42] >> 8)` を TL として書く）。どちらも移植済み

つまり YM2203（3FM + 3SSG）だけの作品で、リズム部は存在しない。
`ssg_write` が `0x0b`〜`0x0d` を無視し `8`/`9`/`10` を `& 0x0f` している点は
原作（`dl = ah` で素通し）と厳密には違うが、driver がその範囲を超えないので
鳴りには出ない。

## 国情報の窓 `sub_4f76` — 原作のテンプレートをそのまま使えた（2026-09-03）

`sub_4f76` は `DS:0xc792` から 16 バイトごとに 4 国の側レコードを回し、
各国について `sub_4faf` を呼ぶ。`sub_4faf` は

* `0x4fb2`: 国フラグの bit 3 が立っている（滅んだ）国は**行ごと飛ばす**
* 枠（`sub_4b61`）と国旗（`sub_8738`）を描く
* 6 つの数値を `DS:0x1a9b` 以降の変数列に置く
* `DS:0x1a54` と `DS:0x1a74` を `sub_759b` に渡す

```
"@5w    @10l@10w@o@10l"        args 1a9b 1a9d 1aa1 1aa3 000e
"@3b.@1b%   @10b@10w@o@10b%"   args 1aa9 1aaa 1aa5 1aa7 1aa3 0012
```

| 変数 | 中身 | 出どころ |
|---|---|---|
| `[0x1a9b]` | 君主の運搬量 | `0x4ff4`、状態 bit 0x20 のユニット |
| `[0x1a9d]`(32) | それ以外の運搬量の合計 | `0x4ffb`、`adc` で足す |
| `[0x1aa1]` | 村の量の合計 | `0x5026`、タイルが `side+8` のマス |
| `[0x1aa3]` | 側レコードのポインタ。`@o` がこれをベースにするので `0x0e` が財布、`0x12` が税率 | |
| `[0x1aa5]` | 君主以外のユニット数 | |
| `[0x1aa7]` | 村の数 | |
| `[0x1aa9]` | 国土占有率。`sub_bcce`/`sub_bc99` × 1000 のあと `div cl` で 10 で割るので **al が整数部・ah が小数第1位**。だから 2 行目は `@3b.@1b%` で、2 番目の引数が 1 バイトだけ先 | |

数値コードの既定幅は `sub_c455`/`sub_c492`/`sub_c52d` が `cl` を見て決める:
**`@b`=3、`@w`=5、`@l`=10**、つまり byte/word/long が10進で必要な桁数。
桁バッファは `DS:0x32ba` を終端に `ch`（0x20 か 0x30）で埋めてから
digits を末尾に書くので**右詰め**。値0のときは最後の1桁を強制的に `'0'` にする
（`0xc48c`）。

移植では `dlg_say_fmt` が `@s @S @b @w @l @o` を埋め、`@?` は3語・`@Nt` は
2語（`@.Nt` は1語）を読み飛ばして引数列の位置を保つ。答えを持たないアドレスは
**幅ぶんの空白**にする（列が崩れないように）。`@o` のあとは
`FMT_BASED + オフセット` を鍵にする。

`DLG_TEXT` は 34 では足りなかった（1行目39文字、2行目40文字）。48 にした。
症状は「財布の数字が村の合計に食い込む」で、6個の空白が消えていた。

## 「時間切れ」は原作に存在しない（再確認）

`[0x3bca]`（残り日数）への参照は 6 箇所しかない:

```
0475  設定        63f0  面のセットアップ
a74f  cmp [0x3bca], 0    ; 0 でなければ
a756  dec [0x3bca]       ; 減らす
b37d  得点計算    b3fc  得点計算
```

**0 を判定して面を終わらせる命令はどこにも無い。** 経過日数 `[0x3bcc]` の方は
`0xa743` で増え続け `0xffff` で飽和する。つまり日数を使い切ると
得点のその項が 0 になるだけで、面はそのまま続く。移植も同じ挙動で、
`game_check` に「日数を 0 にして 200 ターン回しても `over` が立たない」検査を入れた。

（`game_day` はシミュレーションから呼ばれていないように見えたが、
呼んでいるのは `app_tick` の方で、`game_step` は兵の掃きだけ。
テストが `game_step` だけを回していたのが原因。）

## ドライブと CRT/LCD（未実装、内容は判明）

* `0x203e` DRIVE: ガード無し。`DS:0x10c5` の4行メニュー
  「グラフィック／ＢＧＭデーター／マップデーター／ゲームデーター　ドライブ@3t@s」。
  決定すると `[bx + 0x3476]` を 1 と xor して**トグル**し、メニューに戻る。
  抜けるのは取消で、そのとき `0x602` が鳴る（取消の方が「受付」音）
* `0x206c` CRT/LCD: ガード無し。`[0x3286]` に対するスライダ型
  （`sub_5405`/`sub_542a`/`sub_547c`、税率と同じ形）。窓は 3×4 の枠に
  タイル `0x8c8`/`0x8cc` を2つ描くだけなので、文字ではなくアイコンの二択

## 書式エンジン `sub_759b` — ダイアログ表示の根幹（2026-09-03）

**ダイアログの文面はテンプレート＋引数**で、これを知らずに `@` コードを捨てていた
ので数値も名前も出ていなかった。

* 文字列の **NUL の直後にワード単位の引数が並ぶ**。`0x75c4` の `repne scasb` で
  終端を探し、`bx` がその次を指す
* `@` を読むと次のバイトを **`DS:0x2847` の 28 バイト表**から探し、
  **`CS:0x78bb` のワード表**でジャンプ（`0x764a`〜`0x7664`）
* `@.` は `ch = 0x30`（パディング）、数字は `cl = cl*10 + 桁`（桁数）

| コード | 意味 | 消費 |
|---|---|---|
| `@s` | 引数=文字列アドレス。そこを描く | 1語 |
| `@S` | 引数=**ポインタのアドレス**。間接で描く | 1語 |
| `@b` `@B` | ワード値を10進で | 1語 |
| `@w` `@W` | 別の整形（`0x7928`） | 1語 |
| `@l` `@L` | 32ビット（`[si]`,`[si+2]`） | 1語 |
| `@o` | 以降の引数に足す**ベース**を設定 | 1語 |
| `@O` | 呼び出し元のベースを継承 | 0語 |
| `@?` | `[bx]` の指す語が0なら `[bx+4]`、非0なら `[bx+2]` の文字列 | **3語** |
| `@+` | 引数の指す語に `cl` を足す（表送り） | 1語 |
| `@Nt` | `[bx]` の値 × N + `[bx+2]` を**次の引数枠に書く** | 2語 |
| `@.Nt` | **行番号** `[0xc54c]` − 1 × N + `[bx]` を次の枠に書く | 1語 |
| `@g` `@G` | 名前系 | 1語 |
| `@y` `@Y` | `@y` は日付（`/` 区切り、`>>5 &0x0f`、`&0x1f`）、`@Y` は時刻（`:` 区切り） | 1語 |

`@?` の ModRM を `[si+2]` と読み違えて一度詰まった。`8b 77 02` の rm=111 は
**`[bx+disp8]`**。16ビット ModRM の rm=111 は `[BX]`。

### descriptor は 2 種類ある

* **`sub_4a4d`（メッセージ）… 4 バイト**: x, y, 行数, 高さ, その後にポインタ
* **`sub_49bb`（メニュー）… 6 バイト**: x, y, 見える行数（`0x80` = 「1本の
  テンプレートを行番号で引く」）, 幅, 全体の行数, 予備, その後にポインタ
  （`0x49e6` の `add si, 6`）

`DS:0x10e7`（強行/再選択）を 4 バイトで読んで最初のポインタが `0x0002` になり、
選択肢が空の窓が出た。ここで気づいた。

`sub_49bb` は `[di]` を選択・`[di+1]` を表示窓の先頭に使い、
`sub_5bd4` は**表の位置バイトを書き換えて窓を上半分/下半分に寄せる**だけ
（`[0x3be5] - [0x3be3]` をタイルサイズでシフトして `0x18` と比較）。名前の
差し替えではなかった。

## 命令メニュー `sub_20f0` — 原作は12行固定、地形から作らない

テンプレートは `DS:0x15a5` の
`"@.2t" "@?" "@.2t" "@?" "@.2t" "@S" 0x17`、引数は
`c4f8 0000 1308 1306 c512 0000 12f9 12fe 12b7 0000`。展開すると行 k は

* 色1: `[0xc4f8 + k*2]` が非0なら `DS:0x1308`（0x17=白）、0なら `DS:0x1306`（0x16）
* 色2: `[0xc512 + k*2]` が非0なら何もしない、0なら `DS:0x12fe`（0x12=赤）
* 名前: `*(word*)(DS:0x12b7 + k*2)` = `0xc692 + k*16`

`0xc692` は **`gfx_load_names` が `B_0n0L.CH4` の末尾から読む22レコードの6番目以降**。
つまり12個の命令名はフロッピー上にあり、タイルセットごとに違う。体験版は

```
 0 待機     1 オート   2 拠点防衛  3 援軍
 4 村を壊す 5 村を作る  6 柵を作る  7 橋を作る
 8 開墾     9 柵を壊す 10 橋を壊す 11 洞窟封鎖
```

（レコード 0 = シナリオ名 "First Monarch"、1〜5 = 5国名、18 = 降伏、
20 = 未定義、21 = 破壊）

### 可否マスク `sub_ab3e` + `DS:0x22f5`

目的地の地形バイトを 11 バケットに分類し、`DS:0x22f5 + bucket*4` の
**12ビットマスク2本**を1行1ワードに展開する（`shr dx,1 / sbb ax,ax / stosw`）。
バケットが地形の意味を教えてくれる:

| 地形 | 意味 | m1 | m2 |
|---|---|---|---|
| `0x00` | 平地 | 0x067 | 0x17f |
| `0x08..0x0b` | 村 | 0x037 | 0x17f |
| `0x0c..0x0f` | 平地系 | 0x067 | 0x17f |
| `0x01..0x04` | 荒地 | 0x107 | 0x13f |
| `0x7b` | 柵 | 0x240 | 0x240 |
| `0x20..0x2f` | 橋・道 | 0x407 | 0x53f |
| `0x05` | 洞窟 | 0x807 | 0x93f |
| `0x7a` | 岩 | 0x480 | 0x480 |
| `0x30..0x5f` | 水 | 0x080 | 0x080 |
| `0x14..0x17` | 城 | 0x027 | 0x13f |
| その他 | | 0x007 | 0x00f |

水の上では**橋を作る以外すべて赤**。待機さえ赤い。

### 状態バイト（`0x227b`〜`0x2289`）

```
al = [0x3bf4]          ; 第1メニューの選択 0..11
if al != 1: al |= 0x10
ror ah, 2              ; 第2メニューの答えを bit 6,7 へ
al |= ah
[si+0xa] = al          ; 素の代入。以前のビットは残らない
[si+8]   = 目的地
[si+0xb] = 0xff
if (al & 0x0f) in {6,7,9,0xa,0xb}: sub_c2e7   ; 一歩手前で止まる
```

低位ニブルが `CS:0x3a47` の16本のハンドラ番号そのもの。
**`0x10` ビットは飾りではない**: `0x1657` が「2ユニットが同じマスで出会ったとき
どちらが土地を取るか」に使う。移植は AI の作業命令に素のニブルを渡していた。

第2メニュー（選択が3より大きいとき、`0x2241`）は
`DS:0x10ff` =「〈命令〉を行う」の上に `DS:0x109b` の3行
「オートにする／待機にする／〈命令〉を継続」。橋を作る（7）だけ `DS:0x10a7` の2行。

## 経路は3段階（`0x2136`〜`0x21f5`）

`sub_bd1e` が素の距離場（2000:1200）を作業用（2000:0000）に戻し、
その上でブロッカーを走らせて `sub_aaae` が目的地に届いたか見る。
ユニット表は 2000:c800、16バイト×64。対象は「スロット使用中・自分でない・
`[di+0xb] == 0xff`」のみ。`sub_a61d` が `ah` に**自分の同盟国**を入れる。

| 相手 | `sub_bd84` | `sub_bd3b` | ブロッカー無し |
|---|---|---|---|
| 同盟国 (`ah`) | 塞ぐ | 塞ぐ | — |
| 自国 (`al`) | 運搬量の和が16bit溢れたときのみ | 同 | — |
| それ以外（敵） | 自分の運搬量が相手の1.5倍を超えなければ塞ぐ | 塞がない | — |

* `sub_bd84` で届く → 無警告で通る
* `sub_bd3b` で初めて届く → `DS:0x1147`「目標までの経路に危険な敵がいます。」
* ブロッカー無しで初めて届く → `DS:0x113f`「目標までの経路が仲間によって
  ふさがれています。」
* どれでも届かない → 効果音 `0x402` だけ鳴らして行き先選びに戻る

警告のあとは `DS:0x10e7` の2行メニュー「強　行」「再選択」。0=強行は `0x21f5`
へ落ちて命令が成立し、それ以外と取消は `0x211a`（行き先選びに戻る）。
**ユニットは手から離れない。**

なお `sub_aaae` は `cmp ax,0x1f0 / cmc` なので **CF セット = 到達不能**。

## 同盟 `sub_1cb0` — 1組ではなく2組できる

1. `0x1cc2`: 開く前に選択が自国なら1つずらす
2. `0x1d0c`: 自国を選ぶと**無音で何も起きない**。`0x1d18`: 国フラグが0でない
   （滅んだ）国も同じ
3. `0x1d2d`: `sub_ab2b` で両国の32ビット国土（側レコード +8）を比べ、
   **相手が自分より大きければ拒否**。`sub ax,bx / sbb cx,dx / jae` なので同値は可。
   `DS:0x11dd` =「@Sは同盟を拒否しました。」＋ `0x702`
4. 成立時 `0x1d6a`: 効果音 `0x302`。`sub_1de2` が
   「自分・相手のどちらでもない国」を2つ拾い（`cl=0; while cl in {al,ah,dh}: cl++`
   を2回）、`sub_1df5` が4回呼ばれて
   **自分↔相手** と **残り2国↔残り2国** の両方を張る。
   `DS:0x11c7` は「@Sと@Sの／盟約を交わしました。」を**2組ぶん**並べる

`DS:0x12ad` は5国の名前ポインタ表（`0xc642 + side*16`）、`DS:0x12ef` は
5国の側レコードポインタ表。

## アイコンのガードは同じではない

| アイコン | 原作 | 移植の誤り |
|---|---|---|
| `0x1c06` TAX | `sub_b509` **＋ `sub_b571`** | 国の有無を見ていなかった |
| `0x1c36` INFO | `sub_b509` | — |
| `0x1c4d` ZOOM | `sub_b509` | — |
| `0x1c21` SPEED | **無し**（直接 `sub_727a`） | 面が無いと断っていた |
| `0x1cb0` ALLY | 3つ全部 | — |

INFO は `sub_4f76` を描いて `sub_c90f` で任意キー待ち。メニューではない。

## そのほか今回入れたもの

* `0x210e` `test byte ptr [si], 2`: **死にかけのユニットは拾えない**（`0x402`）
* `0x22a2`: 経路を一歩短くするのは 6,7,9,10,11 の5つだけ。移植は全命令で
  短くしていたので、他の7命令は行き先の一歩手前で止まっていた
* `0x2236` の取消は `0x22ce` 行き。効果音 `0x702` で**手放す**。
  「手に持ったまま」と書いていたのは `jae 0x223b` を取消側と読み違えたため

## まだ原作と合わせていないところ

* INFO / TAX / SPEED / ZOOM の窓は移植が自前の文面を書いている。
  `@` エンジンは `@S` だけ実装済み（`dlg_say_fmt`）。数値系を入れれば原作の
  descriptor をそのまま使える
* EDIT (`sub_2368`)、LOAD (`0x1ee5`)、SAVE (`0x1ff3`)、FORM (`0x2081`)、
  CRT/LCD (`0x206c`)、DRIVE (`0x203e`) の6つは断ったまま
* 勝利条件の bit 2 の出どころ（前回からの持ち越し）
* オープニング/エンディングの演出
* 同盟が戦闘や収入にどう効くか（張るところまでは実装した）

## イベントと進行 — 原作との突き合わせ（2026-09-03）

user の指摘「ダイアログの表示と閉じ方・選択が違う」「アイコンやイベントの動作が
違う」「死亡・時間切れ・面クリアで次の面に行けない」を原作コードと突き合わせた。

### ゲームループの末尾が進行を決めている（`0x1a34`〜`0x1a75`）

```
sub_3332  cells      sub_32cc  units     sub_a6a5  totals
sub_a731  timers     sub_b102  fallen sides
sub_b2f2  ← 面クリア判定。CF が立つと jmp 0x191c（設定画面の入口へ戻る）
sub_b28d  ← 自国滅亡判定。同じく CF で 0x191c へ
sub_a75d
```

### 敗北（`sub_b28d`）

* 条件は `[0x3c00]`（人間側）の**フラグ bit3**。4以上（無人）なら何もしない
* 画面を切り替え（`[0x3bc6] = 2`）、メッセージ `DS:0x112f` を出し、
  効果音 `0x0302`、**`sub_c90f` で任意キー待ち**
* `[0x3bd6] = 0xffff`、`[0x346e] = 0xffff` を置いて **`sub_6033`（面の読み込み）**。
  `[0x3bc2]` は触らないので**同じ面をやり直す**
* CF を立てて戻る → ループが `0x191c` へ

### 勝利（`sub_b2f2`）— **この体験版では到達できないと見られる**

* 条件は「4国のうち **3国が `flag & 0x0c == 0x0c`**」。実バイトで確認
  （`0xb30a: 25 0c 00` = `and ax,0x000c`、`0xb30d: 3d 0c 00` = `cmp ax,0x000c`）
* ところが**フラグの bit2 を立てる命令が見つからない**。滅亡処理が立てるのは
  bit3 だけ（`0xb158` の `or word ptr [bx], 8`。0xb100〜0xb300 でフラグに触る
  命令はこれ1つ）。逆アセンブル全体を `or ..., 4` / `or ..., 0x0c` で検索しても
  該当なし。初期化は `0x0353` で0クリア
* したがって `dx` は3に達せず、勝利パスは**死んだコードの可能性が高い**。
  逆アセンブルの被覆は約95%なので断定はしないが、
  「クリアしても次の面に行けない」の説明としては筋が通る
* 勝った場合の中身は読めている:
  生き残った国を求め、成績を `[0xc538]`（国名）/`[0xc53a]`/`[0xc53c]`/
  `[0xc53e]`/`[0xc4f0]` に置き、**生き残りが人間側で面番号 < 52 なら**
  面ごとの12バイト記録表（基準 `DS:0xcbfa` 付近、参照は `[bx-0x3406]` など）を
  更新し、条件付きで **`inc [0xce70]`**

### 到達面カウンタ `[0xce70]`

* **`[0xce70]` が到達面の番号。** `sub_1afa`（GO アイコン）は
  `[0x3bc2] == 0xffff` のとき `[0x3bc2] = [0xce70]` で面を決める
* 勝利時の加算は条件付き（`0xb3e3`）: クリアした面が `[0xce70]` **以上**のときだけ
  `inc`。5面をクリアしたときすでに10面まで到達していれば次は**10面**で、6面ではない
* **移植は `mapNumber + 1` で進めていて `[0xce70]` 相当を持たない。** 要修正

### 時間切れは存在しない

`[0x3bca]`（残り日数）への参照は4箇所だけ:
設定（`0x0475`, `0x63f0`）、`0xa74f`/`0xa756` の「0でなければ減らす」、
そして得点計算での読み出し（`0xb37d`, `0xb3fc`）。
**0になっても面を終わらせる判定はどこにも無い。** 得点項が0になるだけ。
「時間切れで次へ」は原作の動作ではない

### ダイアログの入力規約（`sub_4be9` + `sub_4ca1`）

| 事象 | 原作 |
|---|---|
| 決定 | 音 `0x0601`、**CF クリア**で戻る（`0x4c89`） |
| 取消 | **CF セット**で戻る。意味は呼び出し側が決める（`0x4c94`） |
| 移動 | ±1。**端では折り返さず、何も起きない**（`sub_4ca1` が CF セット。`0x4c2c` が待機に戻るので音も鳴らない） |
| 移動成立 | 音 `0x0500`（`0x4c2e`） |
| 受け付けるキー | `and al, bh`（`0x4c10`）。呼び出し側は全員 0x63 = 上下・決定・取消。**左右はメニューでは無効** |
| 選択の保持 | `[di]` が選択番号、`[di+1]` が**表示窓の先頭行**。`cl` = 見える行数−1、`ch` = 全体−1。窓の端に来たときだけスクロールして再描画（`sub_4af4`） |

取消の意味の例: 命令メニュー（`0x2238`）では**ユニットを手に持ったまま行き先を
選び直す**。閉じて忘れるのではない。

税の窓はメニューではなく **`sub_531d`/`sub_534d`/`sub_539f` のスライダ**なので、
左右キーを取るのはこの窓だけ。

### 入れた分（このセッション）

* 端で折り返さない・折り返せないときは無音
* 移動音 `0x0500`、決定音 `0x0601`
* 取消を `dlg_cancel()` に集約。命令メニューはユニットを手放さず、
  滅亡と面終了の窓は取消でも決定と同じ（原作は `sub_c90f`/`sub_c921` の任意キー待ち）

### まだ直っていない分

1. **`[0xce70]` 相当**が無く、進行が `mapNumber + 1` になっている
2. **勝利条件**が移植では `alive == 1`、原作は `flag & 0x0c` が3国。bit2 の
   出どころが未解明なので、原作どおりにすると勝利が起きなくなる可能性がある。
   どう振る舞わせるかは方針の判断が必要
3. ダイアログの**スクロール窓** `[di+1]` が無い（税スライダ以外で行数が窓を
   超える窓があるなら影響する）
4. アイコンとイベントの動作は未突き合わせ。パネルの14個は `DS:0x202d` の
   ハンドラ表なので、1つずつ中身と比べる必要がある

## いま何ができているか

* **遊べる。** キーボードだけで完結する。矢印でカーソル、スペースで決定、
  Backspace で取消（マップ上では左パネルが開く、パネル内ではマップに戻る）。
  マップ左端でさらに左を押しても左パネルに入る。`[` `]` でマップ選択、T でタイトル。
  マウスも足してあるが原作にマウスは無い（実行像にマウスのI/Oポートが一つも無い）
* **画面内で完結する。** パネルの命令はゲーム自身の画面にダイアログを開き、
  行き先を決めると原作どおり命令メニューが出る。文字は同梱の東雲フォント
  （パブリックドメイン）で**日本語表示**
* **音が出る。効果音も曲も。** 効果音は SSG（音程・長さ・**振幅の包絡**＝
  `DS:0x34e2` の4バイト段まで全部ゲームの表そのまま）。**曲は FM 3ch ＋
  SSG 3ch** で、`src/opn.c` の OPN は NP2 の `sound/opngen` と突き合わせて
  合わせてある（音色0の各倍音が 1% 以内、包絡は立ち上がりから −55 dB まで
  0.1 dB 以内）。**ブラウザでも鳴る**: 画面が呼ぶ曲を `app_song_wanted` が
  答え、`snd_song_fill` が数千サンプルずつ渡す。`wav/` に全曲 44.1 kHz で
  書き出せる（`tmp/song_wav.exe <image> <n> [out.wav] [--rate HZ]`）
* **国が滅ぶところまで動く。** 王を倒した側が跡目を継ぐ。滅んだら
  `0xb197` のウィンドウが出て、答えると**そのまま続きが動く**（GO を押し直す
  必要は無い）
* **面をクリアすると次の面に進む。** `0xb2f2` の勝ちは `sub_6315` に落ちて
  `[0x3bc2]` を次の面にして地図を読み直す。負け（`sub_b28d`）は同じ面をもう
  一度。詳しくは下の「決着と面の進行」
* **タイトルが原作どおり。** 星空に加えて**下の帯・斜線・3行の文字**
  （「CPU Power Level 3 / FM sound Driver / Disk Caching」）。文字列も位置も
  `DS:0x3b4f` の表から読む
* **右パネルが埋まった。** 日数と残り日数、カーソル下の兵（顔・運搬量・座標・
  状態名）、マス（32×32 の絵・量・座標）、5 国の領地グラフ、資金とレート。
  全部ゲーム自身の表と数字から
* **試験が揃っている。** `sh tools/check.sh` で 5 本。`app_check` は
  **1 面を実際に勝たせて**ウィンドウの作法と面の進行を見る。
  `page_check` は偽の AudioContext を持っていて**音の経路も踏む**（無いと
  `audioKick` が諦めて、音が鳴らないまま検査が通ってしまう）

## ビルドと試験

**このマシンには w64devkit も `C:/Python313` ももう無い。** `tools/cc.sh` が
gcc / clang / MSVC のどれかを見つけて使う（既定は MSVC 2022 + Windows SDK、
開発者プロンプト無しで動く。`cmd /c start` が MSVC を掴むと固まるので
`tools/lowpri.sh` は通さない）。`python` は Store の 3.11 が実体で応答する。

**`make` もこのマシンには無い。** Makefile は説明として残っているだけで、
実際に動くのは次の 2 本:

```sh
sh tools/build.sh          # native ＋ wasm。native / wasm を付ければ片方だけ
sh tools/check.sh          # build してから game/app/sound/wasm/page の 5 つ
sh tools/check.sh -n       # 既にビルド済みのものを試すだけ
bash tools/shots.sh        # tmp/shots/ に一通り描く
```

**`node` も PATH に無い**（emsdk が持っている）。上の 2 本が自分で通すが、
手で叩くときは
`export PATH="/c/prog/emsdk/emsdk/node/22.16.0_64bit/bin:$PATH"`。
**PATH に入れる時は POSIX 形式**でないと引かれない（`C:/...` は駄目）。

曲を WAV にするのは `./tmp/song_wav.exe tmp/monarch.fim <n> out.wav [--rate HZ]`
（既定 44100）。

* Python は **`C:/prog/emsdk/emsdk/python/3.13.3_64bit/python.exe`** が確実
  （emsdk が持っている）。Store の `python` も 3.11 の実体が入っていて応答する。
  以前あった `C:/Python313` はもう無い
* 確認は必ず **PNG に描いて Read** する。GUI は出さない（user が同じデスクトップで作業中）
* `tmp/probe.exe` が経済の数値を tick ごとに出す。「兵が出ない」系はこれで見る
* `tmp/sim.exe <map> <ticks> <every>` が無操作の長回し

## 次にやること（順番に意味がある）

0. **OPN は NP2 と突き合わせ済み。エミュレータ全体を起動する必要はもう無い。**

   `tmp/npcmp.c` が NP2 の `sound/opngen` だけを標準の C としてビルドし
   （`tmp/npsnd/` に `opngenc.c` `opngeng.c` と最小の `compiler.h`）、
   両方に**同じレジスタ列**を食わせて波形を比べる。`tmp/envcmp.c` は同じ要領で
   包絡を時間軸で比べる。これで「音色が違う」が測れる数字になり、3 つ直った
   （feedback の深さと平均の取り方、detune をブロックで拡大していたこと、
   減衰量の 8 ステップ）。**耳ではなく差分表で判断すること。**

   測り方の要点（また同じ穴に落ちないように）:

   * **スペクトルは音そのものの基本周波数で測る。** 間引いた DFT は
     440 Hz を綺麗にエイリアスさせて「ただの正弦波」に見せる。
     0.2% の音程差でも 16 倍音は窓の中を 5 周ずれて消えて見える
   * **アルゴリズム 4/6 は搬送波が複数あって MUL も違う**ので、
     「一番大きい分音」を探すと両者が別のものを掴む。共通の f0/2 の
     整数倍で測る
   * **音色 3 と 10 は合わなくて正しい。** MUL 15・TL 0・feedback 7 は
     OPN でノイズ（打楽器）を作る定石で、広帯域雑音同士は分音ごとには揃わない

   残った作りかけ（`tools/np2run.mjs`、np2 を node で丸ごと動かす）は
   **もう要らない**。置いてあるのは、原曲そのものを一度録りたくなった時のため:
   `Module.onReady` が呼ばれず `_np2_resume` が `MainLoop.scheduler is not a
   function` で落ちる所で止まっている。`np2_main` が早く帰る 4 か所
   （`fontmng_init` / `sysmenu_create` / `scrnmng_create` / `flagload`）に
   printf を入れれば 1 回で分かる。一番怪しいのは `scrnmng_create`


0. **タイトルの下の帯。描いた。残る謎は色ひとつだけ。** `FUN_1000_c946` は

   ```
   c9b5  ax=0x607 bx=0     cx=0x146 si=0x27f di=0x18f  sub_7c1c   帯
   c9c7  ax=0x107 bx=0x49  cx=0x146 si=0      di=0x18f  sub_7ad3   左の斜線
   c9d9  ax=0x107 bx=0x236 cx=0x146 si=0x27f di=0x18f  sub_7ad3   右の斜線
   c9eb  si=0x3b4f: lodsw で di、0xffff まで sub_759b               3 行の文字
   ```

   `sub_724a` は **al = 書き込むプレーンのマスク**（モードレジスタへ
   `0xc0 | (~al & 0xf)`）、**ah = 色**（1 ビットずつ 4 つのタイルレジスタへ）。
   帯は色 6 を B/R/G に、斜線は**色 1**（以前ここには「色 7 を B だけに」と
   書いてあったが、`ax=0x107` の al=7 がマスク、ah=1 が色。色 1 = index 1 =
   `000` で、写真の斜線が黒いのと合う）。`gfx_grcg_fill` / `gfx_grcg_line` と
   `title_panel()` で実装済み。

   **ジオメトリは写真とピクセル単位で一致する**（これは前回「合わない」と
   書いてあったが、写真の左右の黒帯を画面だと数えていた誤り）:

   * 斜線は行 326 で x=73 / x=566、行 398 で x=1 / x=638。ちょうど 45 度
   * 帯は毎行 x 0..639 いっぱい。**斜線の外の三角も帯の色**で、黒くない
   * 文字は x=240（di=0x6919 の x=200 ＋ 文字列先頭の空白 5 個）、
     行 336 / 356 / 376

   **`DS:0x3b4f` の表は全部読めた**（`sub_759b` は 0x75c4 で NUL を先に探し、
   `bx` をその次に置く。だから `@` の引数は**文字列の終端の後ろ**に 1 語ずつ）:

   ```
   3b4f  19 69                 di=0x6919 → 行336 x200
   3b51  11 "     CPU Power Level @1w" 00
   3b6b  1e 3c                 @1w の値は DS:0x3c1e（ブート時の速度）
   3b6d  59 6f                 di=0x6f59 → 行356 x200
   3b6f  "     @? sound Driver" 00
   3b85  3a 3b  aa 3b  ad 3b   @? は [0x3b3a] で "FM"(3baa)/"BEEP"(3bad) を選ぶ
   3b89  99 75                 di=0x7599 → 行376 x200
   3b8b  "      Disk Caching @4w KB" 17 00
   3b9f  e8 c4                 @4w の値は DS:0xc4e8
   3ba3  ff ff                 終わり
   ```

   0x20 未満は文字ではない。`0x7633` が `al -= 0x10` して `[0x32a9]` に入れる
   ＝**色**。だから先頭の `0x11` で 3 行とも色 1（黒）、末尾の `0x17` は
   **次に描くもの**のために 7 を残す（これを行の描画前に適用して 3 行目を
   赤くする間違いを一度やった）。`@` の後ろの 1 文字は `DS:0x2847` の 28 文字
   `.0123456789sSbBwWlLoO?+tgGyY` から引く。数字は桁数、続く英字が種類。

   **合わないのは帯の色だけ。** 写真の帯は 42,156 画素が**単色 `#abaabc`**
   （`aab` = R10 G10 B11）。次は全部潰した:

   * **`ss0.jpg` は信用できる。** `DS7TTL` の index を地図にして写真を引くと、
     index 2=`4fa` 3=`088` 4=`477` 5=`8bb` 6=`fff` 8=`ddf` が**表どおり**。
     index 6 は四近傍まで同じ 202 画素のうち 70.8% がぴったり `fff`（第二の
     山は無い）。`ss3.jpg` も `DS:249b` の index 0/1/5/8/9 と完全一致
   * **ディザではない。** `sub_7c1c` は `al=0xff` の `rep stosb`、
     `sub_724a` のタイルレジスタは `sbb al,al` で 0x00 か 0xff の単色
   * **フェードの途中でもない。** `sub_c630` は `0x24cb`→`0x24fb`。index 6 を
     チャンネルごとに解くと R は dl∈[80,95]、G は [128,153]、B は [171,213]。
     一致する dl が無い。index 0 は始点も終点も `06a` で**フェードでは動かない**
   * **明るさでもない。** `0x7374` は全エントリを同じ `[0x34d6]` で
     `((v+1)*bh)>>8` する。`fff`→`aab` には bh≈168 が要るが、
     それだと `088` が `055` になる
   * **別の表でもない。** ディスク全体を 48 バイト窓で走査しても index 6 が
     `aab` の表は無い
   * **他の描画でもない。** `sub_7c1c` の呼び出しはバイナリ全体で
     **この 1 箇所だけ**（`ax=0x607`）
   * **パレットの経路も全部辿った。** RAM は `0x3e20` だけ、ポートへ出すのは
     `sub_736c` だけ（呼び出し元は `0x07ce` の VSYNC のみ）。実行時に
     書き換わるのは `[0x3e4e]`＝index 15 の赤だけで、しかも `[0x32e0]` が
     1 のときだけ。タイトル中は `0xc9fb` が 0 にしている

   残っている食い違いは 2 つで、たぶん同じ 1 つの原因:
   **index 0 が表では `06a` なのに写真では黒**、そして**帯が `aab`**。
   写真から検証できない index は 7 と 9..15。「帯が実は index 14」なら
   どちらも説明がつく——と思って E プレーンを疑ったが、これは潰れた:
   `c946` の頭で呼ぶ `sub_74c6` は `ax=0x000f`（マスク=4 プレーン全部、色 0）
   で両ページを `rep stosw` するので、**E も含めて画面全部が index 0 になる**。
   `DS7TTL` に E のファイルも無い（`.B1/.R1/.G1` だけ）。帯の領域の E は 0 で、
   帯は index 6。

   port は**バイナリの言うとおり index 6 で描いている**。写真と違うのは
   承知の上で、説明がつくまで写真に合わせにいかない。


0. **減衰（エコーのように尾を引く感じ）がまだ原作と違う。** user の指摘。
   分かったところまで:
   * 音色ブロックは `DS:0x34e2 + (音色<<4)` に**4バイト×4段**。段は
     `[歩幅, 回数(bit7=下降), 目標レベル, 初期レベル(第0段のみ)]`。
     `[si+5]` が `(音色<<4)|段` を持ち4ずつ進む。**bit3 が立つ段（+8, +0x0c）で終わり**
   * `sub_111e`（`0x111e`）= 第0段に戻す。キーオフと新しい音のたびに呼ばれる
   * **`sub_114b`（`0x114b`）= リリース。`[si+5]` を第 0x0c 段へ直接飛ばす。**
     `0x0f40` から呼ばれ、条件は「タイフラグが立っていて、残り長さが `[si+9]` に
     達した」。`[si+9]` は初期化で0のまま
   * 最終段の目標に到達すると `0x1016` が `[si+0x1f]` bit2 を立てて
     **レベル0を書いてから**終わる（目標値のまま放置しない）
   * ここまで移植済み（`snd_env_release`）。**ただし実測では尾が1tick程度で
     終わる**ので、耳で聞こえる長い減衰の出どころはまだ説明できていない。
     容疑者: (a) 効果音が実際には別の音色番号で鳴っている、
     (b) `[si+9]` が実行中にどこかで設定される、
     (c) user が覚えているのは効果音ではなく BGM の残響
   * **(c) の線が濃くなった。** FM 側で 3 つ直したあと（TL を毎ティック、
     タイで再アタックしない、`f2` のゲート時間）、曲の減り方はだいぶ変わった。
     FM の「余韻」は音色の RR そのもので、ソフト包絡は掛かっていない
     （`sub_1488` の TL ループはマスターフェード中しか回らない）
   * 測り方: `tmp/sound_check.exe` と、`node` から `_lm_effect` を呼んで
     振幅を区間ごとに見る（本文の commit ログに例あり）
1. **曲（BGM）— 鳴っている。** WAV にも出るし、ブラウザでも鳴る。
   残っているのは精度の話だけ（上の 0. と、音色 3/10 のような打楽器）。
   書式と道筋はここに残す:

   * **読み込み**: `0x5f7e` が `[0x3bc6]` の番号で `FM%03d.DAT` を組み立てて
     読み、**`DS:0x4f1c`** に置く。`[0x3b44]` にそのポインタを入れると
     ISR（`0x0dfc`）が拾って `sub_0bda` が始める
   * **`sub_0bda`（曲の開始）**: 0x800 ワードを `DS:0x3f1c` へ移し、
     `si = 0x3f22`（= 先頭 + 6）から**6 本**のポインタを読んで
     `DS:0x3a3a` から 0x20 刻みの 6 個のチャンネル構造体へ入れる。
     `bl < 3` が FM、3..5 が SSG
   * **ファイルの書式**（`FM001.DAT` = 1719 バイトで確認）:

     ```
     0x00  uint16 × 10   自分自身へのオフセット表
                         [0..2] 3 バイトの小さな表 3 つ（用途未詳）
                         [3..5] FM トラック 3 本
                         [6..8] SSG トラック 3 本
                         [9]    最後のトラックの終わり
     0x14  0 埋め、0x1e に 01 03
     0x20  FM 音色 7 本 × 32 バイト。YM2203 のレジスタ順そのまま
                         +00 DT/MUL×4  +04 TL×4   +08 KS/AR×4
                         +0c AM/DR×4   +10 SR×4   +14 SL/RR×4
                         +18 FB/ALG    +19.. 0    +1f 0x45
     0x109 以降          トラックの列
     ```

   * **音色は使われる。FM は鳴る。OPN コアが要る。** 以前ここには
     「FM のオペレータレジスタはどこにも書かれない」と書いてあったが、
     **間違いだった**。転送は `0x1430` にある:

     ```
     1430  push bx
     1431  mov dh,[si+0x1e] / add dh,0x80 / mov dl,0xff / cx=4
           call sub_740d / add dh,4 / loop      SL/RR を 0xff にして黙らせる
     1444  bl = [si+5]（音色番号）, bx <<= 5      = 音色 * 32
     1453  bx += 0x3f3c                          曲バッファ + 0x20 = 音色表
     1458  dh = [si+0x1e] + 0x30, cx = 0x18
     1461  dl = [bx] / call sub_740d / dh += 4 / bx++ / loop
                                                 0x30,0x34,..,0x8c を 24 本
     146c  dh = [si+0x1e] + 0xb0, ah = [bx]      25 バイト目 = FB/ALG
     1476  and ah,7 / [si+4] = ah                 アルゴリズムは sub_1488 が使う
     147c  call sub_740d / [si+0x1d]=0xff / call sub_1488
     ```

     これで曲ファイルの 32 バイト × N 本の音色表（`0x20` から）の意味が
     きれいに通る。24 + 1 = 25 バイトが YM2203 のレジスタ順そのままで、
     残り 7 バイトは詰め物。

     **なぜ見落としたか。** `tools/lmdis.py` はフローを追う。曲の命令は
     `jmp word ptr cs:[di]`（`sub_1170`/`sub_1175` の表）でしか届かないので、
     FM 側のハンドラ群（`0x11c6` の f0 → `0x1430`）は
     **到達しなかった 5.2%** に入っていた。`lmdis map` が
     「55221 of 58268 bytes (94.8%)」と言っているのがそれ。
     `mov dh, imm` の総ざらいも、ここが `[si+0x1e]` + `add dh,0x30` なので
     引っかからなかった。**「コード全体を見た」と言う前に map の
     カバー率を見ること。**

     したがって **BGM を本当に鳴らすには 4 オペレータの OPN コアが要る**。
     np2 の `sound/opngen` がそのまま参考になる（user の最初の指摘が正しい）

   * **移植済み**: `snd_start_bytes` / `snd_song_track` / `snd_render_song`
     と `tests/song_wav.c`（`song_wav <image> <n> [out.wav]`）。
     トラックの切り出しは合っている（FM001 なら SSG が 225/227/228 バイト）

   **残り: 命令表の 8 個。** 分岐表は `sub_1170` が FM 用 **`0x1186`**、
   `sub_1175` が SSG 用 **`0x11a6`**、どちらも 0xf0..0xff の 16 ワード。
   SSG 側の中身と、移植にまだ無いもの:

   | 命令 | 番地 | 中身 | 移植 |
   |---|---|---|---|
   | f0 | 1310 | 音色 | ある |
   | f1 | 132e | 音量 | ある |
   | f2 | 11fa | `[si+9] = al`（タイの解放しきい値。効果音では 0 のままだが曲は設定する） | **無い** |
   | f3 | 11fe | `[si+8] = al` | **無い** |
   | f4 | 124d | 0 なら終わり、それ以外は `bx += 2 - ax` の後方ジャンプ | **無い** |
   | f5 | 1208 | `[0x3b3d] = al` → `sub_14c9` がレジスタ 0x26/0x27 を書く = **テンポ** | **無い** |
   | f6 | 120e | ループ | ある |
   | f7 | 1351 | 音程の包絡 | ある |
   | f8 | 1373 | `si += al` した所へ次の 1 バイトを書く（構造体への直接代入） | **無い** |
   | f9 | 1398 | `[si+5] = ([0x3b3f] + [si+0x1e] + 0xa) << 4` = SSG の包絡ブロック選択 | **無い** |
   | fa | 13d6 | `dh = al, dl = 次の1バイト` でチップへ直接書き込み | **無い** |
   | fb | 1332 | 音量を下げる | ある |
   | fc | 133e | 音量を上げる | ある |
   | fd | 1225 | `bx += [bx-1]` の 16 ビット相対ジャンプ | **無い** |
   | fe | 13de | 何もしない（FM 側は全消音） | ある |
   | ff | 127f | 終わり | ある |

   命令表は **SSG が `CS:0x11a6`、FM が `CS:0x1186`**、どちらも
   `(cmd - 0xf0) * 2` で引く。`f2`（`0x11fa`）は両方に共通で
   **[si+9] のゲート時間**、`f3`（`0x11fe`）は `[si+8]` のデチューン。
   FM の音量は 3 つとも最後に `jmp 0x1488` して TL を書く
   （`0x11cc` が `DS:0x2431` 経由の設定、`0x11d8` が +3、`0x11ea` が -3）
2. **右パネル。全部入った。** 中身:

   | 場所 | VRAM | 画面 | 中身 |
   |---|---|---|---|
   | 日数 | `0x1e3d` | 行96 x488 5桁 | `[0x3bcc]`。`DS:0x131a` = `@5w` |
   | 残り | `0x1e46` | 行96 x560 5桁 | `[0x3bca]`。`@?@5w`。**0 になると赤** |
   | 兵 | `0x25c2` | 行120 x528 | `DS:0x1bb6` = `@5w  @2b,@2b` ＝ 運搬量, y, x |
   | 兵の状態 | `0x2ac2` | 行136 x528 | `DS:0x1bc9` = `@S@16t@s` ＝ 状態名 |
   | 兵の顔 | `0x25be` | 行120 x496 32×32 | `sub_ac00` ＋ `sub_8738`。下記 |
   | マスの絵 | `0x323e` | 行160 x496 32×32 | `sub_8756`。下記 |
   | マス | `0x34c4` | 行168 x544 10桁 | `DS:0x1bdb` = `@3b  @2b,@2b` ＝ 量, y, x |
   | グラフ | `0x666f` | 行327 x504 から | `sub_93c5`。下記 |
   | 資金 | `0x6e3e` | 行352 x496 16桁 | `DS:0x1ac0`。資金(10桁) と レート(3桁)+`%` |

   **`B_0n0L.CH4` の尻尾**（オフセット `0x7ca0` から）は 3 つに分かれる:

   * **最初の 512 バイト → `DS:0x3c20`**。**32×32 タイル 1 枚 = 16×16 の欠片
     4 つ**の合成表（128 タイル × 4）。`sub_8756` が
     `(タイル & 0x7f) * 4` で引いて、**左上・左下・右上・右下**の順に置く
     （`di` の増減が `+0x500` → `-0x4fe` → `+0x500` なのでこの順）。
     欠片は **`B_0n0M.CH4`**: `sub_8789` が `セグメント 0x3000 + 欠片 * 8`
     を読み、`0x6144` がその M バンクをセグメント `0x3000` に読み込んでいる。
     `gfx_load_compose` で読める
   * **次の 352 バイト → `DS:0xc632`**。16 バイト × 22 の名前
     （シナリオ・国 5・状態 16）。`gfx_load_names`
   * **次の 45 バイト → `DS:0x3eea`**。用途未詳
   * **最後の 48 バイト** はパレット（前から分かっていた分）

   **グラフ**（`sub_93c5` / `sub_9355`）: 5 陣営の**領地総量**（`DS:0xc792+8` の
   32bit、`game_land_totals` が持っている）の最大値を取り、その上位語のビット数
   ＋1 を右シフト量にして、さらに上位バイトを読むぶん 8 ビット落とす。端数は
   切り上げ、127 行で頭打ち。**1 国につき柱 2 本**（各 8px、隣り合わせ）で、
   左が領地、右が**その国の城主が抱えている量**（`+6` の城主を辿ってその `+6`）。
   24px 間隔、行327 から上へ、`0x5a` のディザ、色は `DS:0x3484` の
   5×10 バイト（マスクと色）で PROG.DAT の既定は 4/2/7/0/15、
   `0x6197` がタイルセットの国名の先頭バイト −0x10 で上書きする

   **兵の顔**（`sub_ac00` ＋ `sub_8738`）: `sub_ac00` が 256 通りの
   キャラ番号を作る ――

   ```
   側4          [si]bit1 で死亡なら 0xc8|[si+0xe]、でなければ 0xc0|コマ|向き
   死亡(bit1)   ([si+0xa]bit5 ? 0xa4 : 0xa0) | 側*8 | [si+0xe]
   手番切れ(bit0) (bit5 ? 0x86 : 運搬量で 0x80/0x82/0x84) | 側*8 | コマ
   ふつう       (bit5 ? 0x60 : 運搬量で 0/0x20/0x40) | 側*8 | コマ | 向き&6
   ```

   「コマ」は `[0x3be8] >> 1 & 1`（ターン数の 1 ビット）で歩いて見える。
   `0x7d21` がそれを `欠片 = 0x400 + 番号*4` にして `sub_8738` へ。

   **`C_0n0L1..L4.CH4` は 16×16 の欠片の並び**（32×32 のタイル列ではない）。
   `sub_8789` は `セグメント 0x3000 + 欠片*8` から 128 バイト＝4 プレーン×16 行
   ×2 バイトを読む。欠片 0x400 はちょうど 0x50000 で、`0x6201` `0x6228`
   `0x624f` とその次が 4 本を `0x5000:0000` `0x5000:8000` `0x6000:0000`
   `0x6000:8000` に読むので、**キャラ 0..63 / 64..127 / 128..191 / 192..255**
   と連続する。

   **並べる順が 2 通りあるので注意**: `sub_8738`（顔）は
   n, n+1 を上段・n+2, n+3 を下段。`sub_8756`（マスの絵）は
   **左列が先**（左上・左下・右上・右下）。`di` の足し方が違う。
   片方の順で両方描くと、城主が紙吹雪になる

2. **左パネルの「指差しの手」。** `ss3.jpg` と並べると左パネルは実質同一で、
   違うのは**選んでいるアイコンの上に出る白い手**だけ（写真では虫眼鏡の上）。
   この移植は枠線（`outline_icon`）で代用している。素材は
   **`C_ICON.DAT`（16×16 × 68、BZ）** にあるはずで、この移植はこの
   ファイルをまだ一度も読んでいない。`tmp/icons.exe` でシートに出せる。
   矢印・BS・DEL・INS・END といったキー表示が並んでいて、手はたぶん
   先頭付近の白い形。どのタイルかを決めて、`0x5cef` が何をしているかを
   見れば入る

   **写真との突き合わせで注意**: `ss3.jpg` の画面は
   **左 70、横 1.7891 倍、縦 1.8 倍**。左パネルだけで最小二乗を取ると
   `左 65、1.784` の方が「良く」出るが、それは偽物。グラフの目盛りが
   `522 / 546 / 570 / 594` とちょうど 24px 間隔に並ぶのは前者だけで、
   これはコードの `di += 3` と一致する。**細かい絵の上で per-pixel の
   一致率を最適化してはいけない** —— 1.79 倍の JPEG では輪郭が全部ずれる
   ので、左パネルは同一でも 55% が「違う」と出る

3. **効果音を曲と同じチップに載せる。** いまは効果音と曲を別々に描画して
   足しているので、原作のように**効果音が曲の SSG を横取りしない**。
   必要なことは全部読めている:

   ```
   声部        構造体      +0x1e   何
   音楽 0..2   3a3a 5a 7a  00 01 02   FM 0,1,2   （sub_0e6e が動かす）
   音楽 3..5   3a9a ba da  00 01 02   SSG 0,1,2  （sub_0f2d）
   効果音 0,1  3afa 3b1a   01 02      SSG 1,2    （sub_0f2d）
   ```

   `+0x1e` は PROG.DAT の初期値そのままで、`sub_0e3f` / `0x16bc` の初期化は
   **30 バイトしか消さない**ので残る。効果音が使うのは **SSG の 1 と 2** で、
   借りる相手（音楽声部 4 と 5）とちょうど一致する。**SSG 0 は必ず曲に残る。**

   * `0x0e67` が `[di-0x21] |= 0x40` ＝ 2 つ前の構造体（借りる音楽声部）に印
   * `0x0f2d` が `[si+0x1f]` の bit6 を見て `[0x3b43]` に 0xff を入れる。
     借りられている間、その音楽声部は**列は進めるがチップには書かない**
   * `0x12d1` が返す: 効果音側に bit7、`si -= 0x40` して bit6 を落とし、
     `sub_10ee` とレジスタ書き戻しでその音楽声部を鳴らし直す

   実装は `SndSong` に効果音声部 2 本を足して `song_tick` で回すのが素直。
   そうすると `lm_effect` は「今の曲のチップで鳴らす」になる

3. **同盟の効果**。設定はできる（パネルの ALLY）が、`allied()` が参照するだけ
4. **EDIT（`sub_2368`）**。WAKU2 のパネル一式（PSET/LINE/BOX/FILL/GET/PUT/
   XCHG/CHANGE/FORM/INFORM/UNDO/GAME）。モード表は `CS:0x2a8a`
5. **エンディングの劇。素材は全部読めるようになった。残りは進行と動き。**

   **`sub_ca4a` が劇そのもの。** `0xb449` から呼ばれ、`[0x3bc6]=6` を立てて
   `sub_5fe2` で **FM006 を鳴らし**、6 つの場面を順に呼ぶ:

   ```
   ca89  sub_cb19    パレード ＋ CONGRATULATIONS!
   ca8c  sub_cdf0
   ca8f  sub_d205
   ca92  sub_d45a
   ca95  sub_da07
   ca98  sub_db5b
   ```

   **`.PAC` は「4 プレーンを 1 ファイルに continuous で入れたもの」**（普通の
   絵は `.B1/.R1/.G1/.E1` の 4 ファイル）。中身は他と同じ BZ。`sub_cb19` が
   `END01.PAC` を `0x3000:0000` に読み、`si` を戻さないまま A800/B000/B800/E000
   へ `0xfa0` ワードずつ移す ＝ **1 プレーン 8000 バイト = 80×100**。
   `gfx_load_pac` で読める。分かった寸法:

   | ファイル | 展開後 | 形 | 置き場所 | 中身 |
   |---|---|---|---|---|
   | `END01.PAC` | 32000 | 640×100 | (0,0) 表ページ | キャラの行進 コマ1 |
   | `END02.PAC` | 32000 | 640×100 | (0,0) 裏ページ | 同 コマ2 |
   | `END00.PAC` | 50880 | 640×159 | `sub_cb19` の後半 | `CONGRATULATIONS!` |
   | `END03.PAC` | 50880 | 640×159 | `di=0x2e40` ＝行148 | `LORD MONARCH GRAFFITI` |

   `END01`/`END02` が**表と裏のページに 1 コマずつ**なのがミソで、
   `out 0xa6` でページを切り替えるだけで歩く。

   **`GAKU` は額縁だった**（「額」）。`sub_9836` が寸法をそのまま持っている:
   `di=0xc8e`（＝行40 バイト14 ＝ **(112,40)**）、`cx=0x130` ＝ 304 行、
   1 行 `0x16` ワード ＝ 44 バイト ＝ **352 ピクセル**、次の行まで `+0x24`。
   44×304 = 13376 で**ファイルサイズとぴったり**。`gfx_load_panel` で読める。
   `monarch_shot panel out.png GAKU` で出る（`--x --y --w --h` で他にも使える）

   **`END10`〜`END70` は素材シート**（全画面 4 ファイル形式、`frame` で出る）。
   `END10` は顔の表情違いと「ゲームスタート」「繰越日数」「占領率」
   「あと10日で3段!!」、`END70` は「おしまい」

   場面のパレットは `DS:0x252b`（`DS:0x249b` から数えて 4 番目の表）。
   `0xcba6` が `0x3e20` へ写し、`0xcbb1` が `[0x34d6]=0xff`（＝明るさは素通し）。
   割り込みは `0xcba3` で `0x0a4e` に差し替わる

   まだ読んでいないのは動きの部分: `sub_99ef`（矩形の塗り、di/cx/dx/ax）、
   `sub_cdc6`・`sub_cddb`（歩かせる所）、ISR `0x0a4e`、それと `sub_cdf0` 以降の
   5 場面。台詞は PROG.DAT の **`DS:0x3059`〜`0x31d6`**。`QUEST` は**中身 0 バイト**
   （ディレクトリにあるだけ）

6. **倉庫番 Select 30**（`C:/prog/claude2/soko_ban_wasm`）は未着手。
   `tools/fat12.py` が `[FDI]` を読めるはず

## ダイアログの中身は全部読める — `docs/windows.txt`

**user の指摘: 「ダイアログ系が全部捏造というか創造なんだよね」。そのとおり。**
文言も並びも移植の作り物で、原作を見ていない。**ただし原作の中身は全部読める。**
`tools/lmwin.py` が `sub_4a4d` の記述子を展開して `docs/windows.txt` に出す。
**次にやるのは、これで移植の作り物を置き換えること。**

記述子の書式（`sub_4a4d`）:

```
[si+0]  bx   位置。sub_4b61 にそのまま渡る（bh = 桁、bl = 行）
[si+2]  cx   ch = 枠の幅（桁）、cl = 行数
[si+4]  cl 個のワード。それぞれ sub_759b の書式文字列の番地
```

行は `di = 2 * (bl * 8 * 80 + bh) + 0x50e + n * 0x500` ＝ **16 走査線おき**。

`mov si, imm` から `sub_4a4d` を呼ぶ所は 37 か所あり、全部読めた。要点:

| DS | 中身 |
|---|---|
| `1105` | `@Sの勝利です。` ＋ 罫線 ＋ 経過日数/残り日数/占領面積/占領率 ＋ 罫線 ＋ `@?` |
| `1119` | 同じ表だが罫線と最終行が無い（勝ったのが人間でないとき） |
| `1129` | `@5w日 繰越日数が増加しました。` |
| `112f` | `あなたの治める国は亡びてしまいました。` ＋ 色6 で `もう一度挑戦してください。` |
| `1137` | `@Sは滅亡しました。` |
| `113f` | `目標までの経路が仲間によって` / `ふさがれています。` |
| `1147` | `目標までの経路に危険な敵がいます。` |
| `1153` | `マップをロードしてください。`（決着後に GO） |
| `1159` | `ゲーム開始後は変更できません。` |
| `115f` | `あなたの治めている国はありません。` |
| `11c7` | `@Sと@Sは` / `盟約を交わしました。` を 2 組 |
| `11d5` | `@Sと@Sの` / `同盟は破れました。` |
| `11dd` | `@Sは同盟を拒否しました。` |
| `11e3` | `@Sの王が倒されました。`（滅亡とは別の窓） |
| `1215` | **INFO**。`@s城　@7t@s` を 4 行 ＋ `外周部当り　　　@?` |
| `2958` | QUEST の認定証（15 行） |

**移植と食い違っているもの**（直す順）:

1. **INFO は「四つの国／領地・保有・資金」ではない。** `DS:1215` は
   **4 国の城の名と数字＋外周部当り**の 5 行。引数は `DS:c642/c652/c662/c672`
   （名前）と `DS:c554..c55a`、最後が `DS:c55c`
2. **同盟・王が倒された・経路がふさがれた**の窓が移植に無い。`sub_759b` の
   `@S` は `DS:c52e` などの**名前ポインタ**で、`country_name()` が読んでいる
   のと同じ表（`DS:0x12ad` / `DS:0x12ef`）
3. **選択肢のある窓は `sub_4a4d` ではない。** `sub_4a4d` は文字を並べるだけ。
   税は `sub_4e49`＋`sub_4f08` の**スライダ**（下記）、速度は `sub_531d`＋
   `sub_534d`＋`sub_539f`（`0x1c21`、`DS:0x3c02`）。命令メニューと国選択は
   まだ追っていない。**移植のメニューはここが全部作り物。**

### 税の窓は 0〜30 のスライダだった（直した）

`0x1c06`（アイコン 2 = TAX）→ `sub_4e49` が (120,40) に
`0x8a4` ＋ `0x8a5`×15 ＋ `0x8a6` の**帯**を描き、`0x8a7` の**つまみ**を載せる。
`sub_4f08` が入力ループで、`sub_c8b0` の `and al, 0x6c` の

* bit 2 → `0x4f45 and ah,ah / je` ＝ **0 より下には行かない**
* bit 3 → `0x4f4d cmp ah,0x1e / jae` ＝ **30 より上には行かない**
* bit 5 / bit 6 → どちらも `0x601` を鳴らして抜ける（clc / stc の違いだけ）

書き込む先は `[side + 0x12]` ＝ 移植の `Side.rate`。**0 も 30 も出せる。**
移植は `{1,4,7,10,13,16}` という**自作の 6 択**で、最低が 1 だった
（user が「税率 0 で」と言っていたのに 0 が選べなかった）。
いまは `dlg_open_tax` / `tax_step` がスライダで、左右（と上下）が 1 目盛ずつ動く。
`tests/app_check.c` が実際に 0 まで下げて 1 面を勝つところまで見ている。

## 決着と面の進行 — 解読済み（`sub_b197` / `sub_b2f2` / `sub_b28d` / `sub_6315`）

ここは user から「国が滅びる系がとにかくバグだらけ」と言われた所で、
**原作を読み直したら移植の思い込みが 2 つ**あった。どちらも直してある。

### ウィンドウは世界を止めるが、走行フラグには触らない

国が一つ滅びたときの `0xb197`:

```
b197  si=0x1137  sub_4a4d     ウィンドウを開く
b19e  ax=0x302   sub_0d12     音
b1a5  sub_72ad                答えるまで待つ（ブロックする）
b1a8  sub_c921                閉じる
b1ab  …                       そのまま続き
```

**`[0x3bd4]`（面が始まっている）にも `[0x3bd6]`（面が終わった）にも書かない。**
世界が止まるのは `sub_72ad` がブロックするからであって、走行フラグを
下ろしているからではない。だから答えれば元のまま動き出す。

移植は `running = 0` にしていたので、国が滅びるたびに GO を押し直す羽目に
なっていた。いまは

* `app_render` が **ダイアログが出ている間は `app_tick` を呼ばない**
  （`if (running && dlg.what == DLG_NONE)`）＝ `sub_72ad` の代わり
* 決着のウィンドウを開く所では `running` に触らない

の 2 つで原作と同じ形にしてある。税・情報・速度のウィンドウも同じ扱いに
なった（以前はその裏で日数が進んでいた）。

### 勝ち — `sub_b2f2`

4 国のうち 3 つが `flags & 0xc == 0xc` になったら決着。勝った側 (`flags & 8`)
を探し、面積と占領率を出して `[0x3c00]`（人間の国）と比べる。

* **勝ったのが人間でなければ** `0xb468`: ウィンドウ `0x1119`、`[0x3bd6] = -1`
* **人間なら** 記録を更新して（`[0xce70]` が到達した最高の面）ウィンドウ
  `0x1105`、記録更新なら `0x1129` も重ねる。閉じてから

  ```
  b433  [0x3bd6] = -1
  b439  面 == 0x33 なら  [0xce70]=0x33 → sub_b661 → sub_ca4a（エンディング）
  b44e  そうでなければ   sub_b58f       （昇格。未移植）
  b451  面+1 == [0xce70] なら sub_6315
  ```

`sub_6315` が**面の進行そのもの**:

```
6316  [0x3bc2] = [0xce70]      いま到達した面
631c  [0x3bd6] = 0             この面はまだ終わっていない
6321  [0xc52c] = 0 / [0x346e] = -1
6328  sub_6033                 その面の地図を読む
```

**`[0x3bd4]` を消していない**のがミソで、`0x1afa`（GO）はこれを見るから、
次の面は**最初から動いている状態**で始まる。移植も `app_show_map` の後に
`running = 1` を置いてある。

### 負け — `sub_b28d`

人間の国に `flags & 8` が立ったら `[0x3bc6]=2` の曲、ウィンドウ `0x112f`
（「あなたの治める国は亡びてしまいました。」＋「もう一度挑戦してください。」）、
そのあと `[0x3bd6] = -1` `[0x346e] = -1` で **`[0x3bc2]` はそのまま**
`sub_6033` ＝ 同じ面をもう一度読む。`[0x3bd6]` を消さないので `0x1afa` は
`0x1b10` のウィンドウ `0x1153` で断る ＝ 面を選び直すまで走らない。
移植では「同じ地図が止まった状態で出る」形にしてある。

### 残り

* **`sub_b58f`（昇格）は未移植。** `[0xce72]` を階級として持ち、`[0x3bc6]=3`
  の曲を鳴らして `DS:0x32b5` の数字を上げる。曲 3 が鳴る場面がここしか無い
* **エンディング `sub_ca4a` も未移植**（下の「これから」5 番）
* **残り日数が 0 になっても終わらない。** `game_day` は減らすだけ。
  0xa749 の周りをもう一度読むこと

## 音の全体像（要点だけ。詳細は下の「音」節）

* YM2203、ポート `0x188`/`0x18a`。`sub_740d` が書き込み、`sub_7439` が読み出し
* **このプログラムは FM のオペレータレジスタを一度も書かない**（`0x30`/`0x50`/
  `0x60`/`0x70`/`0x80`/`0xb0` が全部未設定）。だから体験版の音は全部 SSG。
  OPN コアを書いてもこのフロッピーの音は変わらない
* 効果音の表は `DS:0x38a1`（19個）。`sub_0d12` は優先度ラッチだけ
* 音程表は隣り合っている: `DS:0x244d` が FM の F-number 12語、その直後
  `DS:0x2465` が SSG の周期表
* プレイヤは2本（FM: `0x0e70` 付近 / SSG: `sub_0f2d`）。チャンネル構造体は
  FM が `DS:0x3a3a` から3つ、SSG が `DS:0x3afa` から3つ、どちらも 0x20 刻み

## 効きやすい罠（今日踏んだもの）

* **ヒアドキュメントはバックスラッシュを食う。** `printf("...\n")` を
  ヒアドキュメント越しに書くと壊れる。**Edit/Write ツールを使う**。今日3回踏んだ
* **ページとC側で状態を二重に持つな。** `running` をページ側で別管理していて、
  パネルの GO を押しても世界が進まなかった。`lm_running()` を必ず問い合わせる
* **ブラウザは古い monarch.js を掴む。** `make wasm` が `tools/stamp.py` で
  wasm の md5 を index.html と wasm の URL に刻む。`page_check` が
  「ページが呼ぶ `_lm_*` が全部エクスポートされているか」を検査する
* **座標は測るな、原作のコードを読め。** アイコン位置を目分量で決めて8px ずれた。
  ただし `DS:0x2055` の表は相対関係は合うのに原点が (−8, +16) ずれる。未解明
* **試験が「受理されたか」だけ見ていると通ってしまう。** 橋の命令は受理される
  のに完了しなかった（王が城から出られないバグ）。**完了まで見る**
* **耳で聴けないので、PCM を描画して最強周波数を測る。** これで「分周が2倍で
  1オクターブ低い」を捕まえた
* `disk/` と `orig/` にゲームのデータが入っている。user は承知の上（「おいていいよ」）


# RESUME — ロードモナーク (PC-98, 1991) を C に戻して native -> WASM

先行の [windepth_wasm](https://github.com/yomei-o/windepth_wasm) と
[super_depth_wasm](https://github.com/yomei-o/super_depth_wasm) と同じ手順。

**対象は PC-98 のオリジナル版**。1997 年の Windows 版
（[loadmonarch_wasm](https://github.com/yomei-o/loadmonarch_wasm)）とは
キャラクターも中身も別物で、流用するのは**展開コーデックだけ**。

## 対象

日本ファルコム「ロードモナーク」体験版フロッピー（1991）。
配布アーカイブは `orig/` にそのまま、展開した中身は `disk/`。
実機のスクリーンショット `ss0.jpg`（タイトル）と `ss3.jpg`（ゲーム画面）が
リポジトリ直下にある。**`ss3.jpg` は製品版の画面**で、枠 (`WAKU`) の絵は
体験版と同じだがマップとキャラクターは別物。だから色の答え合わせに使えるのは
タイトルと枠までで、地形タイルの色には使えない。

## 抽出は検証済み

* LZH は格納 CRC-16 と一致（`python tools/lzh.py orig/*.LZH` が `ok` を出す）。
  **イメージはバイト単位で正しい**
* `[FIM]` は **256 バイトヘッダ + 生セクタ**。ヘッダは
  `uint32 1 / uint32 0x2000（= 1 トラック 8192 バイト）/ uint32 0x9a（= 154 面）`。
  256 + 154 × 8192 = 1,261,824 = ファイルサイズちょうど
* FAT12: 1024 バイト/セクタ、1232 セクタ、FAT 2 枚 × 2 セクタ（論理 1..4）、
  ルート 6 セクタ（論理 5..10、192 エントリ）、データ開始 11
* サブディレクトリ無し・同名ファイル無し（**上書き事故の心配なし**）。
  `PROG.DAT` = クラスタ 2 から連続 8 個、`PROG.BIN` = クラスタ 10 から連続 42 個
* **イメージ形式の判定は拡張子ではなくヘッダ長の総当たり**（raw=0 / FIM=256 /
  FDI・HDM=4096）＋ BPB の妥当性。倉庫番の `[FDI]` も同じツールで読めるはず

180 ファイル / 1,147,014 バイト。

## 起動とメモリ配置 — 解読済み

`IO.SYS` と `MSDOS.SYS` は **0 バイトのダミー**で DOS は入っていない。
ブートセクタ（イメージ 0x100 から 1024 バイト、`tmp/boot.bin`）は
`DS = ES = CS` で動き、

```
mov ah,0x0d / int 18h            CRT 初期化
mov ah,0x42 / mov ch,0xc0 / int 18h   640x400 グラフィック
（FAT を CS:0x400 に、ルートディレクトリを CS:0xc00 と CS:0x1800 に INT 1Bh で読む）
mov word [0x3fd],0x376 / [0x3ff]=0x1000 / [0x401]=0      -> PROG.DAT
mov word [0x3fd],0x381 / [0x3ff]=0      / [0x401]=0x1000 -> PROG.BIN
ljmp 0x1000:0
```

ローダ（CS:0xf5）はルート 192 エントリを `repe cmpsb` で 11 バイト比較し、
クラスタを 3000:0000 に読んでから `les di,[0x3ff]` の先へ `rep movsw` で移す。
つまり **`[0x3ff]` がオフセット、`[0x401]` がセグメント**:

| | |
|---|---|
| `PROG.DAT` | **0000:1000**（linear 0x1000〜0x2f53） |
| `PROG.BIN` | **1000:0000**、エントリはオフセット 0 |

セクタ読みは `CH = 3`（= 128 << 3 = 1024 バイト/セクタ）、`CL` シリンダ、
`DH` ヘッド、`DL` セクタ（1 起算）、`ES:BP` バッファ。

### PROG.BIN と PROG.DAT は圧縮されている — 解読済み

**これが長いあいだ全部を狂わせていた。** ブートローダのファイル読み込みには

```
016e  mov ax,[di+0x16]    ディレクトリエントリの時刻フィールド
0171  and ax,0x1f
0174  cmp ax,0x1f
0177  je  -> 0x1bb        一致したら別経路
0179  ...                 素の rep movsw（こちらは死んでいる）
```

という分岐があり、**180 エントリ全部が `time & 0x1f == 0x1f`** なので
常に 0x1bb 側を通る。0x1bb は LZSS 展開器（`tools/lmz.py` に写した）:

```
uint16  展開後サイズ
uint16  最初のフラグ語、以降必要に応じて
...     ブロック

フラグ語は MSB から 16 ビット使う。
  0  リテラルブロック。カウントバイト n が続き
       n & 1 == 0 -> そのまま n>>1 バイトコピー
       n & 1 == 1 -> 1 バイト続き、それを n>>1 回書く
  1  マッチ。uint16 w が続き
       長さ = (w >> 12) + 3        (3..18)
       距離 = w & 0x0fff           出力へのバックリファレンス
```

**嵌りどころ: フラグ語の再読み込みは 16 回目のシフトの時点で、
その項目のオペランドを読む前に起きる。** ブートセクタでは
`shl bx,1` と `jb` の間に `dec dl / jne / lodsw` が挟まっている。
オペランドの後で再読み込みすると **15 項目まで正しく展開してから
ずれる**という、たいへん説得力のある間違い方をする。

| | 圧縮 | 展開後 | 置かれる範囲 |
|---|---|---|---|
| `PROG.BIN` | 42,040 | **58,268** | 1000:0000..1000:E39B |
| `PROG.DAT` | 8,020 | **11,290** | DS:1000..DS:3C19 |

**確認は二重に取れている**: `PROG.BIN` の先頭 2 バイトは `9c e3` = 0xE39C で、
コード中に `call 0xE36C` という確定位置の呼び出しがある（展開前の
42040 = 0xA438 では範囲外で説明がつかなかった）。`PROG.DAT` は
`1a 2c` = 0x2C1A、載る先が 0x1000 なので終端は 0x3C1A で、
startup が `mov [0x3c1a],bx` と書いている番地そのもの。
さらに両ファイルとも**ソースを 1 バイト残らず消費**して展開が終わる。

展開すると逆アセンブルは 89.9% が命令として通り、デコード不能は 6 箇所だけ
（展開前は 80.6% / 195 箇所で、しかも境界がずれていた）。

### PROG.BIN は CS = 0x1000 / DS = ES = SS = 0 で走る

先頭が `xor sp,sp / mov ds,sp / mov es,sp / mov ss,sp / mov sp,0x1000`。
**DS = 0 の裏付け**は 3 か所の PC-98 BIOS ワーク読み出し —
`[0x564]`、`[0x56c]`、`[0x584]`。最後のはブートセクタが
`xor ax,ax / mov es,ax / mov al,es:[0x584]` と明示的に ES=0 で読んでいる
ディスク装備バイトそのもの。だから PROG.BIN 中の `[0x1234]` は**全部 linear
低位メモリ**で、このファイルのオフセットではない。

低位メモリの地図:

| | |
|---|---|
| 0x0000-0x03ff | 割り込みベクタ |
| 〜0x1000 | スタック（SP=0x1000 から下に伸びる） |
| 0x1000-0x2f53 | PROG.DAT。**末尾は作業領域として使い回される**（0x1428, 0x2470 に書き込みがある） |
| 0x3200 〜 0x5exx | ワーク |
| 0xc400 〜 0xf3xx | ワーク |

startup が張るベクタ: INT 00h → `cs:0x73c`、INT 09h → `cs:0x7e0`、
INT 0Ah → `cs:0x755`、INT 13h → `cs:0x873`、INT 15h → `cs:0x8b6`。
元の INT 1Bh は `[0x3e7c]` に保存され、ファイル読みは 0x5706 の
`lcall [0x3e7c]` 経由。

## パレット — 解読済み

**ハードウェア側**（PROG.BIN 0x5db7）:

```
5db7  mov si, 0x3e20          ; DS:0x3e20 = 生きているパレット表（48 バイト）
5dba  mov bh, [0x34d6]        ; フェード値
5dbe  mov bl, 0               ; パレット番号
5dc0  mov al, bl / out 0xa8, al
5dc4  mov dx, 0xae            ; 0xae=BLUE -> 0xac=RED -> 0xaa=GREEN
5dc7  mov cx, 3
5dca  lodsb / and al,0x0f / inc al / mul bh / mov al,ah / out dx,al
5dd4  sub dx, 2
5dd7  loop 5dca
5dd9  inc bl / cmp bl,0x10 / jb 5dc0
```

* **1 色 3 バイト、順序は B, R, G**
* 値は `((byte & 0x0f) + 1) * fade >> 8`。`fade = 0xff` なら実質そのまま
* フェードは `[0x34d6]` 現在値 / `[0x34d7]` ステップ / `[0x34d8]` カウンタ /
  `[0x34da]` 目標表のアドレス。0x8e0c のクロスフェードが
  `[0x3eba+i] = [0x3e20+i] - [si+i]`（i=0..0x2f）で差分表を作り、
  最後に `mov si,[0x34da] / mov di,0x3e20 / mov cx,0x18 / rep movsw` で確定
* RAM 上の表の配列は `DS:0x249b`、0x30 刻み。startup 0x008b が
  `mov si,0x249b / mov di,0x3e20 / mov cx,0x18 / rep movsw` で初期化し、
  フェードは 0x24cb（0x912a）、0x24fb（0x914a）、0x252b（0x9294）を使う

**表の中身はディスクの、32×32 地形バンクの末尾にある。**
`B_0n0L.CH4` は展開すると 32816 バイト = 64 × (32×32×4/8 = 512) + **48**。
その 48 バイトが**素の 0〜15 の値**で並んだパレット。

```
0..9  共通    000 00A D00 AEE 0D3 0BF FD0 FFF E80 08C
10..15 set10  840 888 084 0AA BDB CCC     草原
       set20  840 AA7 DDA 774 552 BBB     電車の国（線路）
       set30  840 0B7 064 089 B60 999
       set40  800 446 007 88B BBE 888
       set50  B73 840 095 06A EA6 999     鉛筆王国
```

**なぜ長く見つからなかったか**（同じ穴を掘らないための記録）:

* `PROG.DAT` / `PROG.BIN` にはパレットは無い。ゲームがタイルセットと一緒に
  ディスクから読んで RAM に写している。両ファイルを 48 バイト窓で
  総当たりし、フェード値 150〜255 も、ニブル詰め 24 バイト形式も試して
  すべて空振りだったのは当然だった
* **`.CH4` の「余り」を見落としていた。** S と M は割り切れるが
  **L だけ 48 バイト余る**。展開後サイズを 512 で割った余りを見れば一発だった。
  プレーン画像（32000）と S/M バンクがきっちり割り切れたので
  「余りは無い」と早合点した
* index 0..9 が 5 セット共通で 10..15 だけ違う、という構造も見落としの原因。
  製品版のスクリーンショット (`ss3.jpg`) の枠から測れたのは 0..9 だけで、
  それは全セット共通の部分だった

**照合は 3 通り独立に一致した**:

| | |
|---|---|
| index 8 = `E80` | 製品版の枠から実測した (13,7,0) |
| index 7 = `FFF` | `org45.gif` の `#efefef` |
| index 1/2/3/5/6/9 | 上記どちらとも一致 |
| set50 の 10/11/13 | `org45.gif` のタイル形状照合の投票（`B_050S.CH4` が 51/145 一致）と一致 |

### タイトル画面のパレット — 解読済み（`DS:24fb`）

地形バンクが無いので `.CH4` 末尾の手は効かないが、**展開後の `PROG.DAT` の中**に
あった。オープニングの流れ（`sub_c946` → `0xc9b2`）:

```
（乱数で星の表を埋める。0x50 / 0xa0 / 0xf0 の 3 段階）
c9b2  call sub_5dfd        DS7TTL を 3 プレーン読む
c9b5  sub_7c1c 全画面矩形
c9fb  si=0x24cb を [0x3e20] に直接コピー
ca0f  sub_c6ad / sub_c612  フェードイン
ca15  [0x32d3]=0x1e でキー待ち
ca28  si=0x24fb / call sub_c630   ここへクロスフェード
```

* **`DS:24fb` がタイトルのパレット。** `ss0.jpg` から実測した値と
  index 2 = `4FA`、index 3 = `088`、index 4 = `477` が**厳密に一致**し、
  5 と 6 も細線の JPEG にじみの範囲内
* **背景は index 0 で、画面では黒。** 表の index 0 は `06A`（青）なのに黒。
  以前は「index 0 を透過にして下の index 1 が見えている」と読んでいたが、
  それだと**星が出せない**（星は E だけなので index 1 の上では index 9 = 黒）。
  測ると背景は index 0 のほう:
  * `ss0.jpg` の帯より上は 74.5% が黒。DS7TTL のその範囲の index 0 は 72.4%。
    index 1 は 6,630 px = 3% しかない
  * 星は実測 `#ddddff` = index 8 ちょうど。E だけで index 8 になるのは
    下地が index 0 のときだけ
  * ロゴは実測 `#008888` = index 3 ちょうど
  **どこでゲームが index 0 を黒にしているかは未発見**（パレット出力 `0x7374` は
  16 本とも `DS:0x3e20` から書き、クロスフェード `0xc630` は最後に
  `DS:0x24fb` の 48 バイトを丸ごとコピーする）。移植は測定に従って
  index 0 を黒にしている。**ここだけは読みではなく実測**
* **`ss0.jpg` は左右に黒帯がある。** 実画面は写真の 1152×720 の部分で、
  `写真x = 64 + ゲームx * 1.8`。ゲーム x 0..30 と 610..640 には
  写真上に 1 ピクセルも無い。ここを見落とすと「帯が x 32..607 しかない」
  という誤った測定になる
* `DS:24cb` はその前段の 2 色だけの表（8 個の `06A` と 8 個の `DDF`）
* 表の配列は `DS:249b` から `DS:267b` まで 0x30 刻みで、
  使うのはタイトルとエンディング。**ゲーム中のパレットは `DS:3eea`**
  （`B_0n0L.CH4` 末尾から写されたもの）

### フェードの中身

| | |
|---|---|
| `[0x34d6]` | 明度 0..0xff |
| `[0x34d7]` | 1 ステップの増減 |
| `[0x34d8]` | クロスフェードの残り |
| `[0x34da]` | 目標表のアドレス |
| `[0x3eba]` | 差分表 48 バイト |

* `sub_c612` — フェードイン。`[0x34d6] += [0x34d7]` を桁溢れするまで、
  毎回 `sub_9aa6`（パレット適用 + VSYNC 待ち）
* `sub_c630` — SI へクロスフェード。まず
  `[0x3eba+i] = [0x3e20+i] - [si+i]` を 48 バイトぶん作り、
  `[0x34d8]` を減らしながら `sub_c673` で補間して適用、
  最後に `rep movsw` で live = 目標

## 圧縮 — 解読済み（借り物）

ディスク上のデータは全部ファルコム自前の LZ（`.BZ` コーデック）。
Windows 版のために書いた `bz.c` がそのまま通る。**実測**:

| ファイル | 圧縮 | 展開 |
|---|---|---|
| `B_000.MAP` | 1175 | **2306** |
| `B_010S.CH4` | 2721 | 4640（145 タイル × 8×8 × 4 プレーン） |
| `B_010M.CH4` | 7622 | 16384（128 タイル × 16×16 × 4） |
| `C_010M.CH4` | 16761 | 32768（256 タイル × 16×16 × 4） |
| `DS7TTL.B1` | 7990 | **32000** = 640×400/8 ちょうど |
| `C_MOJI.DAT` | 1396 | 4096（128 文字 × 8×8 × 4） |
| `C_ICON.DAT` | 3956 | 8704（68 個 × 16×16 × 4） |
| `PROG.BIN` / `PROG.DAT` | — | **BZ ではない。生**（`bzcat` が 0 バイトを返す） |

## グラフィック — 形式は判明

* `.CH4` = 4 プレーンのタイルバンク。1 タイルにつきプレーン 0,1,2,3 の順に、
  各プレーンは行優先・MSB が左
* `.B1/.R1/.G1/.E1` = 640×400 画面のビットプレーン 4 枚を別ファイルに。
  パレット番号は `(E<<3)|(G<<2)|(R<<1)|B` で、**拡張子の順がそのままビット順**
  （`ss0.jpg` / `ss3.jpg` との突き合わせで確認済み）
* `B_nnnS/M/L` は同じ地形セットの 8×8 / 16×16 / 32×32 の 3 段。
  `C_nnnM` `C_nnnL1..L4` がキャラクター
* タイトルの星空は `DS7TTL` に入っていない。**コードが描いている** — 解読済み、
  移植済み（`src/app.c` の `stars_*`）。タイトルが `sub_c6ad(0x949)` で
  INT 0Ah（PC-98 では IRQ2 = 垂直帰線）に張るハンドラで、ループは `0x098c`。
  `FUN_1000_c946` が表を作る: 個数は `DS:0x3bb4`（機械の速度 `DS:0x3c1e` から。
  レベル3で 0x80、1段下がるごとに半分）、終端は `DS:0x3bb6` = 先頭 + 個数×8。
  8 バイト×個数 ということは **4 バイト × 個数×2 個**の表で、
  1 件が `uint16 位置`（32000 バイトのプレーン内のバイトオフセット）と
  `uint16 歩幅`（`0x50`/`0xa0`/`0xf0` = 1/2/3 行）。レベル3なら**星は 256 個**、
  割り込み 1 回で動かすのは `[0x3bb4]` = 128 個だけ（残りは次の回）。
  点は 4 行 1 バイトの `0x18 0x3c 0x3c 0x18`（4 ピクセルの丸）を
  **E プレーンだけ**に OR（割り込みは DS に 0xe000 を入れる）。
  下まで落ちた星は上端のランダムな桁へ戻る（`sub_0a38` の 7 ビット値 ×0xa0>>8
  で 0..79）。乱数は `sub_9a36`（16 ビットの LFSR、タップ 15 と 1）

## `.MAP` — 解読済み

展開後 2306 バイト = **48 × 48 セル（1 バイト/セル）+ `uint16`**。

* 行幅 48 は、候補幅ごとに「あるバイトと 1 幅後のバイトの平均絶対差」を測って
  最小のものを取って決めた。48 が 12.6、49 が 17.5、47 が 17.8、96 が
  第 2 高調波として出る（本物の画像の挙動で、偶然ではこうならない）
* 末尾の `uint16` は**地形セット番号**。52 本の内訳は
  10 が 12 本、20/30/40/50 が各 10 本で、ディスク上の 5 組
  `B_010* B_020* B_030* B_040* B_050*` にちょうど対応する。
  **マップは自分のタイルセットを自分で持っている**
* セル値 = バンク内のタイル番号。`B_nnnS/M/L` = 同じ地形セットの
  8×8 / 16×16 / 32×32 なので、同じマップが 3 段のズームで描ける
* 地形セットは 5 組でそれぞれ世界のテーマが違う —
  10 = 草原・森・山・海、**20 = 電車の国**（線路が道）、30 = 森と煉瓦の村、
  40 = 氷と青いパイプ・結晶、**50 = 鉛筆王国**（「HB」と書かれた鉛筆、
  六角形の鉛筆の束、青い罫線ノート、消しゴム、鉛筆削り）。
  体験版のテーマは鉛筆王国で、セット 50 を使うマップは
  B_014/015/016/029/030/031/044/045/046/051 の 10 本
* セル値の統計（全 52 本）: `0x00` = 草地 33%、`0x01` 11.8%、`0x60` 4.0%、
  `0x30` 3.7%。`0x30` 台が水面（タイルが index 1 の青と index 5 で構成される）、
  `0x00` は index 4 / 12 / 13 / 14 で構成される草地

`tools/map.py` がこれを絵にする。ss3.jpg の地図と照合しようとしたが、
**ss3 は製品版の画面でマップもキャラクターも体験版とは別物**なので
（一致率 78% 止まり）、パレット 10〜14 の実測には使えなかった。

## コードの地図（展開後アドレス、1000:xxxx）

`tools/lmdis.py` と `decomp/`（`sh tools/ghidra.sh` で再生成）から。

### 起動の流れ

```
0000  cli / xor sp,sp / DS=ES=SS=0 / SP=0x1000
000d  INT 13h の元ベクタを [0x3c1a] に退避
001a  ベクタを張る: INT 00h -> cs:073c, INT 09h -> cs:07e0,
      INT 0Ah -> cs:0755, INT 13h -> cs:0873, INT 15h -> cs:08b6
004d  INT 1Bh の元ベクタを [0x3e7c] に退避（ファイル読みはここ経由）
0059  sub_0187 / sub_01b9 / sub_024b  — 8255・PIC・PIT・FM の初期化
0104  sub_5f7e     BGM ロード（[0x3bc6] = 曲番号）
0107  sub_0ceb
010a  sub_e365
010d  sub_5ca2     C_MOJI.DAT（フォント）
0115  int 1ch AH=0, BX=0x3e70   カレンダ BIOS で日時を読み
011d  [0x3e75] 回 sub_9a36 を呼んで乱数を撹拌
0146  sub_74c6
0149  [0x249b] の 48 バイトを [0x3e20] へ（初期パレット）
0154  sub_5cef     C_ICON.DAT（アイコン）
0157  sub_5d0e     WAKU.*（枠 4 プレーン）
0184  jmp 0x191c   ゲーム本体へ
```

### タイトル画面（0x191c）

`[0x32d4] = 0x1e` を置いてキー待ちループ。`sub_c8d8` がキーを返し、
`and al,0x6f` で判定。`[0x32d4]` が割り込みで減って 0 になると
0x1a34（デモ）へ落ちる。

### ファイル名表と読み込み

**`DS:3302` から 12 バイト刻みで 30 エントリ**、FAT の 11 バイト形式。
番号やサイズ文字はその場で書き換える（`sub_c492` が数値を数字に、
`sub_c449` が `cx` 桁を `di` に書く）。

| エントリ | 読む場所 |
|---|---|
| `DS:3302 PROG    BIN` | `e36f` |
| `DS:330e C_000M  CH4` | `61c1` 以降（M と L1..L4 の 5 か所） |
| `DS:331a B_000M  CH4` | `610e`（S） `613c`（M） `615f`（L）|
| `DS:3326 B_000   MAP` | `6059` `62bc` |
| `DS:3332 FM000   DAT` | `5fac`（BGM ローダ `sub_5f7e`） `6002` |
| `DS:333e SAVE00S DAT` | `6372` 以降 |
| `DS:334a SCORE   DAT` | `6342` `637e` `6488` |
| `DS:3356 C_MOJI  DAT` | `5ca9` |
| `DS:3362 C_ICON  DAT` | `5cf6` |
| `DS:336e WAKU    B1 ` | `5d1a` `5d35` `5d50` `5d6b`（4 プレーン） |
| `DS:337a WAKU2   B1 ` | `5d94` 以降（4 プレーン） |
| `DS:339e DS7TTL  G1 ` | `5e09` `5e24` `5e3f`（**3 プレーンだけ**） |
| `DS:33aa GAKU    G1 ` | `5e63` 以降（4 プレーン） |
| `DS:33b6..345e END*`  | `cb19`〜`e1c0` |

読み込みの下請け: `sub_687e` が名前で探す、`sub_6f4b` が転送先を
`ax:di` で設定、`sub_68a0` が読む、`sub_6cb5` がエラー時の再試行。

### 地形バンクの置き場所と、パレットの行き先

`0x6105` の関数が `[0x3bc4]`（地形セット番号）で 3 本読む:

| | |
|---|---|
| `B_0n0S.CH4` | **7000:0000** |
| `B_0n0M.CH4` | **3000:0000** |
| `B_0n0L.CH4` | **4000:0000** |

L を読んだあと `4000:7ca0` から低位メモリへ 3 ブロック写す:

```
4000:7ca0 + 512 バイト -> DS:3c20
4000:7ea0 + 352 バイト -> DS:c632     16 バイト × 22 レコード
4000:8000 +  45 バイト -> DS:3eea     ← パレット 15 色ぶん (0..14)
```

`4000:8000` はちょうど 64 タイル × 512 の直後、つまり**末尾 48 バイトの
パレットの先頭**。index 15 だけ写していないのが面白いところ。
続いて `0000:c642` から 16 バイト刻みで 5 レコード、各先頭バイトから
0x10 を引いて `DS:3485` の 9 バイト刻みに書く。

### パレットの適用（0x7374 と 0x739f）

`[0x3372]` の判定で 2 系統に分かれる。

* `0x7374` — アナログ 16 色。`DS:3e20` の 48 バイトを 1 色 3 バイト
  B,R,G の順に 0xae/0xac/0xaa へ。`((byte & 0x0f) + 1) * [0x34d6] >> 8`
* `0x739f` — **８階調表示液晶**向け。各チャンネルを 1 ビットに落として
  出す（`shr ah,1 / sbb al,al / and al,0x0f`）。文字列にある
  「８階調表示液晶ディスプレイ」がこれ

## 描画のしくみ — 解読済み

### VRAM とタイル描画（`sub_8789`）

16×16 タイルの描画本体。38 か所から呼ばれる。

```
8789  ax = タイル番号
878c  ax *= 8 / ax += 0x3000 / ds = ax     ; タイルは (0x3000 + n*8):0000
8799  es = 0xa800                          ; VRAM
879e  al = 0xc0 / out 0x7c                 ; GRCG を RMW モードで有効化
87a2  dx = [si+0x60]   プレーン 3 (E)
87a5  cx = [si+0x40]   プレーン 2 (G)
87a8  bx = [si+0x20]   プレーン 1 (R)
87ab  ax = [si]        プレーン 0 (B)
      bp = ~(ax & bx & cx & dx)            ; マスク
87b6  out 0x7e に B,R,G,E の各バイト       ; GRCG のタイルレジスタ
87c5  stosb  マスクバイト                  ; 1 バイト書くと 4 プレーンに展開される
87dc  di += 0x4e                           ; = 80 - 2、次の行
```

これで確定すること:

* **VRAM は 80 バイト/行**（`di += 0x4e` と 2 バイト書き込み 2 回で 0x50）
* **タイルは 128 バイト**（4 プレーン × 32 バイト）で、`B_0n0M.CH4` を
  3000:0000 に読んだときタイル n は `(0x3000 + n*8):0000`。
  つまり `.CH4` はプレーン 0,1,2,3 の順という読みが正しい
* **index 15 が透過色。** `~(B & R & G & E)` をマスクにするので、
  4 プレーンすべて立っている画素は書き込まれない。
  裏付け: 16×16 の地形バンクは index 15 が**ちょうど 0.0%**、
  一方キャラクターバンクは 18〜27%（スプライトの背景）

### 画面の中の座標

`sub_54e2` は EDIT モードのタイル一覧を描く。`di = 0x1e3e` から
列送り 2 バイト × 8、行送り `0x4f0`。0x1e3e / 80 = 96 余り 62 なので
**(x=496, y=96) から 8 × 16 個の 16×16 タイル** — 右パネルの中。

## ゲームの中核データ — 解読済み

### ユニット配列 `DS:C800`

更新ループは `sub_32cc`（`0x1a50` から 1 か所だけ呼ばれる）:

```
32cc  [0xc52c] = 0
32d2  si = 0x40 / cl = [0x3c02] / si >>= cl / si--   ; 1 回で処理する体数
32de  si = [0x3bec] + 0xc800                          ; 巡回カーソル
32e6  al = [si]; ビット 7 が立っていれば飛ばす
32ed  al &= 0xfe                                      ; 「今回処理した」印を落とす
32f1  push 0x3318                                     ; ハンドラは ret でここへ戻る
32f4  [si+0]&2      -> 0x493a
32f8  [si+0x0c]==4  -> 0x3b86
32fe  [si+0x0a]&0x20-> 0x3a67
3305  [si+0x0a]&0x10-> 0x3608
3309  それ以外      -> 0x383e
3318  si += 0x1f0; 0xcc00 以上なら -0x400                ; 次のスロット
```

* **1 レコード 16 バイト、64 体**（窓が `0xc800..0xcbff` の 1024 バイト、
  刻み `0x1f0` = 31 レコードで 64 と互いに素なので全部を巡る）
* **`[0x3c02]` はゲーム速度**。1 回に `0x40 >> speed` 体を処理する。
  文字列にある「速い / 普通 / 遅い」がこれ
* 巡回カーソルは `[0x3bec]`

レコードの中身（`[si+N]` のアクセスを全部集計して割り出した）:

| | |
|---|---|
| +0x00 | フラグ。bit7 = 未使用スロット、bit1 = 別ディスパッチ、bit0 = 処理済み印 |
| +0x01 | タイマー（`6` を入れる箇所が多い） |
| +0x02 | word |
| +0x04 | **いる場所**。セル配列への*バイト*オフセット（= セル番号 × 2） |
| +0x06 | **運んでいる量**。セルから `xchg` で受け取って貯める |
| +0x08 | word |
| +0x0a | 状態。**下位ニブルが命令**で `cs:[bx+0x3a47]` の 16 本へ分岐、bit 0x10/0x20 がモード |
| +0x0b | 目標 / `0xff` が「無し」 |
| +0x0c | **所属勢力 0..3** |
| +0x0e | フラグ |
| +0x0f | カウンタ |

### セル配列 `DS:D27E` — 1 セル 2 バイト

`.MAP` は 1 セル 1 バイトだが、読み込んだあと **後ろから in-place で 2 バイトに
展開**する（`0x60c3`）:

```
si = 0xdb7d / di = 0xe47c / cx = 0x900 / ah = 0x64
cli / std / lodsb / stosw / loop / cld / sti
```

つまり各セルが `(タイル, 100)` になる。**上位バイトはそのマスが持っている量**で

* 1 目盛りごとに `add al,0x0a`（+10）で増える
* `add al,ah / jae / mov al,0xff` で 255 に飽和する
* `cmp byte [di+0xd27f], 0xc8`（200）というしきい値がある
* ユニットがそのマスに乗ると `xchg byte [di+0xd27f], al`（al=1）で
  **持って行き**、`[si+6]` に貯める

これがロードモナークの経済そのもの。

アクセスの変位が `±1, ±2, ±94, ±96, ±98` に固まっていることから
**行送り 96 バイト（= 48 セル × 2）**も裏付けられる。
48 × 48 × 2 = 4608 で、次の配列がちょうど `map + 4608`。

### 占有配列 `DS:E47E`

セルごとに 1 word で、そのマスにいるユニットのレコードアドレス（0 なら空）。
`mov word ptr [di + 0xe47e], si` と書かれる。こちらも 4608 バイト。

### 4 勢力の城はマップの中にある

ユニットのハンドラが `cell - 0x14 < 4` を `[si+0x0c]`（所属）と比べている。
**セル値 `0x14`〜`0x17` が 4 勢力の城**で、`.MAP` 52 本すべてに
**ちょうど 1 個ずつ**入っている（例: `B_000.MAP` は
(6,8) (30,11) (41,34) (5,39)）。

`src/game.c` にこの初期状態の組み立てを入れた。ビューアで `C` を押すと
城に印が出る（**原作にはそんな表示は無い**。解析用の目印）。

### 移動 — 解読済み

`0x3793` からの並びが 1 歩ぶん:

```
3793  di = [si+4]
3796  call sub_c291        次に向きたい方向 al を決める（AI / 経路）
3799  cmp [si+1], al
379c  jne -> [si+0x0e] = al / [si+1] = al / ret     向きが違えばこの回は向き変えだけ
37a5  al = [si+1] / call sub_9ad3                   方向 -> 差分 cx
37ab  dx = [si+2] / call sub_ad80                   dx に差分を足す。範囲外なら CF
37b3  call sub_9b34                                 dx -> bx = セルのバイトオフセット
37b6  cmp byte [bx+0xd27e], 0x30 / jae 塞がり        0x30 以上は通れない
37c9  cmp word [bx+0xe47e], 0    / jne 塞がり        既に誰かいる
37d6  [bx+0xe47e] = si / xchg [si+4],bx / [bx+0xe47e] = 0 / [si+2] = dx
37ec  塞がり: dec [si+0x0f]、0 になったら sub_adbe（別のユニットを確保）
```

* **`[si+1]` はタイマーではなく向き。** 初期値 6 = 右で、
  実際 2 体目は城の右隣に置かれる
* 方向は `DS:2827` の 8 エントリ表（`(dy<<8)|dx`）。
  0=上 で反時計回り: 上, 左上, 左, 左下, 下, 右下, 右, 右上
* `sub_ad80` が x も y も **1..0x2e** に収まることを要求する。
  つまり**外周 1 マスは立入禁止**（文字列の「外周部当り」がこれ）
* `sub_9b34` は `(y<<8)|x` を `(y*48+x)*2` に変換するだけ
* **1 マスに 1 体**。占有配列 `DS:E47E` がそれを保証している
* **城の 3×3 タイル `0x70..0x79` は `0x30` 以上なので通れない。**
  中央のマスに出入りできるのは右隣の `0x1d` だけで、
  開始時そこには 2 体目がいるので君主は箱の中にいる

### 経路探索 — 解読済み

セグメント **0x2000** が経路専用:

| | |
|---|---|
| `2000:0000..11ff` | 48×48 の word グリッド。flood fill が距離を書く |
| `2000:1200..23ff` | 2 面目 |
| `2000:2400..43ff` | 64 本の経路バッファ、1 本 128 バイト |
| `2000:4400..` | fill 自身のスタック（`[0x4400]` `[0x4402]`） |

* 経路バッファ = **124 バイトの 2 ビットステップ（496 個）** +
  word の長さ (+0x7c) + word の現在位置 (+0x7e)。
  496 は builder が `cmp ax,0x1f0 / jae` で拒む上限そのもの
* `[si+0x0b]` がバッファの番号。`0xff` は「経路なし」
* `sub_c050` の flood fill は走査線塗りつぶし。コストが一様なので BFS と
  同じ距離になる。**4 近傍のみ**（`±2` と `±0x60` しか出てこない）
* `sub_c0bd` が目標から距離場を最急降下しながら 2 ビットを詰める。
  コードは「どの隣が小さかったか」で **0=下 1=右 2=上 3=左**。
  逆向きに記録されるので、前から読んで `sub_c291` が 2 倍すると
  8 方向表の **上/左/下/右** になり辻褄が合う
* 歩き終わると `0xc2c0` が現在位置を進め、長さに達したら `[si+0x0b]=0xff`

### 世界の tick（`sub_3332`、`0x1a4d` から）

**勢力ごと**（4 つ、`0xc792` 刻み 0x16）:

```
[si+0]&8 なら飛ばす                    ; 脱落
[0x3c00] == その勢力なら飛ばす          ; 人間側は AI 調整しない
[si+0x10] == 0 なら bl = 18 - (資金>>8)、下限 1
[si+0x12]（レート、初期 10）を bl に 1 ずつ寄せる
```

**セルごと**: `(144 >> speed) - 1` 個。カーソル `[0x3bea]` は
**46 バイト = 23 セル刻み**で 2304 と互いに素なので全部を巡る。

| タイル | 処理 |
|---|---|
| `5` | 量が +10。255 を超えるとき空きマスなら**中立ユニットが湧いて量が 0 に戻る**（`0x3533`） |
| `0x08 + side` | 自領。下記 |
| `0x14 + side` | 城。下記 |

`sub_adeb` が 8 近傍を数えて「同じタイルの数 / 空き(タイル 0)の数 /
最後の空きの位置」を返す。

#### 自領のマス（`0x33d1`）

1. 量が「同じ自領の隣接数 + 1」だけ増える。**AI 側は `[0x347e]` が
   立っていると倍**。255 で飽和
2. 量が `(隣接数 + 1) × 16` 以下なら、ここで終わり
3. 隣接数 + 1 が **9 以上（八方すべて自領）ならユニットを産む**
4. そうでなければ、空き隣接があればそこを **`0x0c + side` の量 100** にして
   領土を広げる
5. さらに量が **200 以上ならユニットを産む**

ユニットを産む（`0x346a`）とき: そのマスが空いていれば、
**所持 = 量 - 1** のユニット（状態 1、その勢力）を置き、**マスの量は 1 になる**。

`sub_b7cf` は空き隣接が無いときに参照される分岐で、そこだけ未解明。

#### 城のマス（`0x3581`）— 徴税

```
si = そのマスにいるユニット。いなければ終わり
そのユニットがその勢力のもので、[si+0x0a] & 0x20 でなければ終わり
城の 2 マス右が他勢力の土地（0x08..0x0f のうち別の side）なら終わり
距離場を城から張って、
全 2304 セルのうち タイルが 0x08+side かつ 距離 < 0x4000 のものについて
    取り分 = (レート × 量) >> 8
    資金 += 取り分（[bx+0x0e] と桁上がりの [bx+0x10]）
    マスの量 -= 取り分
```

これで経済が閉じる — 土地が育ち、城が繋がっている土地から取り立て、
育ちきった土地がユニットになる。

**ただし世界の tick が触るのは `0x08 + side` だけ**で、広がった先にできる
`0x0c + side` は何もしない。開始時の `0x08` は 1 マップに 10 個ほどしかない
（52 マップで 561+504+643+539）ので、放っておいてもほとんど育たない。
`0x0c` を `0x08` に変えるのはユニットの仕事のはずで、そこがまだ未着手。
30000 tick 回すと資金だけが 22433 / 8874 / 8874 / 6937 と貯まる。

### ユニットの命令 — 一部解読

ディスパッチャ (`0x32cc`) から:

| 条件 | 行き先 |
|---|---|
| `[si+0]&2` | `0x493a` |
| `[si+0x0c]==4`（中立） | `0x3b86` |
| `[si+0x0a]&0x20`（君主） | `0x3a67` |
| `[si+0x0a]&0x10` | `0x3608` |
| それ以外 | `0x383e` → 下位ニブルで `cs:[bx+0x3a47]` の 16 本 |

16 本の中身は薄いラッパで、本体は `sub_3ec7` / `sub_3f2a` / `sub_3f62` /
`sub_4040` / `sub_4163` / `sub_41dc` / `sub_4247` など。

#### 状態 1 と 3 — 働き手（`0x3998`）

```
sub_3ec7 で今いるマスを開発。成功したら終わり
失敗したら sub_adbe（行動予算 [0xc4f2]）を消費して sub_4881 へ
予算が無ければ sub_af42 で自領の方向へ 1 歩
```

`sub_4881`（の `0x458e`）は:

```
自領の上にいるなら 状態 2、目標 = 現在地
そうでなければ sub_4814 で距離場を張り、sub_bc56 で最も近い自領を探す
持ち物が距離の 2 倍以上でなければ諦める
sub_c0bd で経路を作り、[si+0x0b] に経路番号、状態 2、[si+0x0f] = 4
```

`sub_bc56` は距離場を走査して **最も近い `0x08+side` のマス**とその距離を返す
（496 以上なら失敗）。`sub_af42` は上→下→左→右の順で最初に一致する隣の方向。

#### 開発（`sub_3ec7`）

```
今いるマスが 平地(0) か 0x0c..0x0f でなければ失敗
8 近傍に 0x08+自分 か 0x08+同盟 が無ければ失敗（sub_ae9e、土地は繋がっていること）
資金から 100 を引く（sub_abc7。資金は +0x0e/+0x10 の 32 ビット）
使う量 = min(持ち物, 200)
マス = (0x08+side, 使う量/2 + 1)
持ち物 -= 使う量。0 になったらそのユニットは終わり（sub_a9ca）
```

#### 拾い上げ（`0x34bb`）

自分の `0x08+side` のマスに乗っていると `xchg` でマスの量を持って行き、
マスには 1 が残る。持ち物は 0xffff で飽和。

#### 君主の死（`sub_a9ca`）

`[si+0x0a]&0x20` なら君主。`[si+0x0f]` が倒した側で、それを
勢力レコードの `+0x15` に書く。`+0x15` は世界の tick（`0x34ce`）が
**滅んだ勢力の領地を引き継がせる**先として読む。辻褄が合う。

#### 君主（`0x3a67` → `sub_3c7f`）

勢力レコードの `+0x08/+0x0a` は **その勢力の土地の合計量**（32 ビット）。
`sub_a6a5` が 2304 セルを走って `0x08+side` の量を足し上げる
（タイル 5 の巣は中立勢力に足す）。startup の `0x0140` から呼ばれる。

自分の城の上にいるとき:

```
所持 / 2 > 土地の合計量  ->  門（城の右隣）が空いていれば、
                             所持の 1/4 を持たせた働き手（状態 1）を置き、
                             君主はそのぶん失う
そうでなければ            ->  上限 = 土地の合計量（0xffff で丸め）として
                             所持 += (上限 - 所持)/4 + 1。届いていたら何もしない
```

つまり**君主の手持ちは土地が抱えている量を追いかけ、土地が枯れると
働き手が出る**。生産が自動的に足りるところで止まる仕掛け。

#### 未解読

* 目標選択の `sub_4814` / `sub_bd3b` / `sub_bd84`（AI と人間で分岐する）。
  移植では**そこだけ自前**にしてある（近くて自領に接する開発可能マスを選ぶ）。
  その先の処理は全部原作どおり
* 君主の `0x3a67`、中立の `0x3b86`、`0x10` モードの `0x3608`
* 資金の他の使い道: `imul ax,ax,0x1e`（×30、`0x40b5`）、
  `ax = dx>>2`（`0x3fbe` と `0x4007`）、`ax = dx*2`（`0x4106`）

### 地形コードの意味

| | |
|---|---|
| `0x00`〜`0x07` | 平地（`0x06` だけ特別で中立ユニットになる） |
| `0x08 + side` | その勢力の**生産する土地**。世界の tick が処理するのはこれだけで、育って広がってユニットを産み、城の徴税もここから取る |
| `0x0c + side` | その勢力の**ただ占めている土地**。tick では何も起きない。`sub_41b5` が他勢力のものを踏むと (0,100) に戻す。`0x08` の土地が広がるときに作られるのもこれ |
| `0x14 + side` | 城の中央 |
| `0x1d` | 城の右隣。ここだけ通れる |
| `0x30` 以上 | 通れない（水など）。城の `0x70..0x79` もここに入る |

## `sub_759b` の書式 — 解読済み

ゲームの文字列は全部これで書けている。`si` が文字列、`di` が VRAM の
バイトオフセット（1 文字進むと +1）。

* **0x20 未満は制御**。`0x7633` が `al -= 0x10` して `[0x32a9]` へ ＝ **色**。
  0x10 未満は無視。0x00 で終わり
* **引数は文字列の NUL の後ろに 1 語ずつ**。`0x75c4` が先に `repne scasb` で
  終端を探して `bx` をその次に置くから。読んだ分だけ `bx` が進む
* `@` の次の 1 文字を `DS:0x2847` の 28 文字
  `.0123456789sSbBwWlLoO?+tgGyY` から引き、`CS:0x78bb` の飛び先表へ。
  **数字は桁数（`cl`）**、続く英字が種類

| コード | 語数 | 何をするか |
|---|---|---|
| `@Nb` `@Nw` `@Nl` | 1 | `[bp-2] + 引数` の**アドレスから**バイト/語/long を N 桁で |
| `@NB` `@NW` `@NL` | 1 | 同じだが**もう一段間接**（引数はポインタのアドレス） |
| `@s` | 1 | 引数のアドレスにある**文字列**を入れ子で描く |
| `@S` | 1 | 引数のアドレスにある**ポインタ**の先の文字列 |
| `@?` | 3 | 1語目のアドレスの**語が 0 なら 3 語目**、非 0 なら 2 語目の文字列 |
| `@No` | 1 | **基底アドレス `[bp-2]` を設定**。数字が付くと引数そのもの、 |
| | | 付かないと引数のアドレスにある語 |
| `@Nt` | 2 | `[引数1] * N + 引数2` を計算して**次の引数枠に書き込む**（`0x77b1`）。 |
| | | 直後の `@o` がそれを拾うので、2 つで「レコードを選ぶ」になる |
| `@N+` | 1 | 引数の指す語に N を足す（カウンタ） |
| `@g` `@G` `@y` `@Y` `@.` `@O` | ? | 未解読 |

`@Nb` 等の桁は**空白で右詰め**（ss3.jpg の "293days" は 5 桁のうち後ろ 3 桁、
"2907Left" は後ろ 4 桁で確認済み）。

**罠**: `@?` の 0 と非 0 は**直感と逆**。0 が 3 語目。タイトルの
「FM sound Driver」は `[0x3b3a]` が 0 のとき "BEEP" になるのが正しく、
写真が FM なのは実機が起動時に音源を見つけて非 0 を書いたから。この移植は
自分で答える（`panel_runtime`）。

## PROG.DAT の中身の地図（分かった分）

| オフセット | 中身 |
|---|---|
| 0x0000 | 小さなテーブル群 |
| 0x002b | SJIS メッセージ（「表示装置を選択してください。」「アナログＢＧＢディスプレイ」…） |
| 0x1490 | 8 バイト単位、JIS 風コードと SJIS かなの対応表 |
| 0x1560-0x15a4 | かな一覧（SJIS 2 バイトの並び、0 終端） |
| 0x15a5 | **2 ビット → 8 ビット倍化表** 16 バイト `00 03 0c 0f 30 33 3c 3f c0 c3 cc cf f0 f3 fc ff` |
| 0x15b5 | 0x33e8, 0x37d0, 0x3bb8 … と低位メモリ番地を含むレコード列 |
| 0x2302 | **ファイル名表**（FAT 8.3 の 11 バイト形式、12 バイト刻みで 30 エントリ） |
| 0x149b | **パレット表の配列**（`DS:249b`、0x30 刻み。index 0..9 は地形バンク末尾と一致、`DS:24fb` はタイトル画面の色） |
| 0x1868 | 称号「９級」〜「６段」、続いて「　０」〜「２１」の数字列 |
| 0x2059 | エンディングの台詞（殿・重臣・曲者・家来・敵兵） |

`tools/lmdis.py strings` で全部読める（254 本）。書式は独自のテンプレートで
`@3b` `@.2t` `@5w` `@S` `@O` `@?` `@o` `@10l` などが埋め込まれている。

## これから

1. ~~フロッピーからの抽出~~ 済（検証済み）
2. ~~ブートの仕組みとロード番地・セグメント~~ 済
3. ~~グラフィックのフォーマット~~ 済
4. ~~パレット~~ 済（`B_0n0L.CH4` の末尾 48 バイト）。タイトルの表だけ未発見
5. ~~`.MAP` の構造を確定して地図を絵にする~~ 済
6. `PROG.BIN` のゲームロジック（**大半解読**。初期状態・移動・経路探索・
   土地の成長と拡張・開発・拾い上げ・城の徴税・君主の生産まで移植済み。
   残るのは AI の目標選択（`sub_4814` / `sub_bd3b` / `sub_bd84`）、
   戦闘、中立ユニットの行動、`0x10` モード）
7. ~~ネイティブの描画土台~~ 済（`src/`、`make`）。ゲームロジックはまだ
8. ~~WASM（ソフトウェア描画のみ、WebGL 不使用）~~ 済（`make wasm` → `docs/`）

## ネイティブ側の現状

`make` で `tmp/monarch.exe`（Win32・8bpp DIB・2 倍表示）と
`tmp/monarch_shot.exe`（ウィンドウを開かない PNG 出力）ができる。

* `monarch_shot` の出力は `tools/map.py` の出力と**バイト単位で一致**する。
  FAT12・BZ・プレーン組み立て・タイル・マップ・パレット・PNG の全経路を
  Python 実装と突き合わせて確認済み
* `make shots` で `tmp/shots/` に確認用の PNG が並ぶ
* 枠の中の地図ウィンドウは **320 × 384**（`WAKU` でほぼ全高が空いている列が
  x = 160..479、行が y = 8..391）。16×16 タイルなら 20 × 24 セル分なので、
  48 × 48 のマップに対してスクロールする窓。8×8 で全体を出すには 384 px
  必要で入らないから、全体図は別画面（左パネルの `MAP` ボタン）と思われる
* **`tmp/monarch.exe` は実行して確認していない。** ウィンドウが開くので、
  デスクトップを使っている最中は動かさない方針。共通部分は
  `monarch_shot` で検証済みなので、残る未検証は DIB の blit だけ
* ホスト非依存の部分は `src/app.c` に寄せた。Win32・WASM・
  `monarch_shot view` の 3 つが同じ経路を通る

## WASM 側の現状

`make wasm` で `docs/monarch.js` と `docs/monarch.wasm` ができ、
`docs/index.html` がそれを読む。GitHub Pages が `docs/` を配信する設定なら
そのまま動く。

* **WebGL 不使用。** C 側で RGBA に描いて `putImageData` するだけ
* イメージは `--embed-file` で焼き込み。ディスク読み出しはネイティブと同じ C
* `node tests/wasm_check.js` — WASM を node で動かして生フレームを吐く。
  `HEAPU8` は `EXPORTED_RUNTIME_METHODS` に足さないと `undefined` になる。
  また `Buffer.from(HEAPU8.buffer, p, n)` は**ビュー**なので、次の描画で
  中身が変わる。`subarray` で複製すること
* `node tests/page_check.js` — `index.html` のインラインスクリプトを
  DOM スタブ上で実行して、キーが届くか・`putImageData` が呼ばれるか・
  Shift 併用で地図が変わらないかを見る
* **WASM が吐いたフレームとネイティブの PNG はバイト単位で一致**
  （タイトル / 地形 10 / 地形 50 の 3 枚）

## 環境

* Python `C:\Python313\python.exe`、mingw `C:\prog\w64devkit`、
  emsdk `C:\prog\emsdk\emsdk`（**`emcc.bat` は無く `emcc.exe`**）
* Ghidra 12.1.3 + JDK 21 を `C:\prog\ghidra` に。`ghidra_proj/` は事前に mkdir が要る
* capstone は Python に入っている。**objdump も使える**
  （`C:\prog\w64devkit\bin\objdump.exe -D -b binary -m i8086`）— 逆アセンブルで
  迷ったときの第二意見に有用


## 音 — YM2203 (OPN) の駆動と効果音の書式

原作は鳴る。移植に音が無いだけ。ディスクに `FM001`〜`FM025.DAT`（1.2〜3.6KB）が
あり、駆動は PROG.BIN の中にある。

### 出力側

* ポート **0x188**（レジスタ番号）/ **0x18a**（値）。PC-98 内蔵の YM2203
* `sub_740d(dx)` = **レジスタ書き込み**。dh=レジスタ, dl=値。ビジー待ち
  （`sub_7455`）→ 0x188 にレジスタ番号 → `jmp $+2` を6個並べた待ち →
  0x18a に値
* `sub_7439(dx)` = **読み出し**（dl に返る）。`dh=0x0e` はジョイスティック。
  PC-98 はゲームポートが OPN の I/O ポートに載っているので筋が通る
* `sub_73f8` = タイムアウト付きビジー待ち（音源の有無判定）
* `[0x3b43]`: **0 なら FM、非0 ならビープ**。`DS:0x3baa` の `"FM"` / `"BEEP"`
  の2文字列に対応
* 曲はタイマー駆動。初期化で **レジスタ 0x27 に 0x3a**

### 効果音

* `sub_0d12` は**優先度ラッチだけ**。`[0x3b46]` に `(id<<8)|優先度` を1件保持し、
  同じか高い優先度が来たら差し替える。`0x16b1` の `xchg` が拾って空にする
* id → データの表は **`DS:0x38a1`**、19 個。指す先は PROG.DAT 内の
  `0x38c7`〜`0x3a35` の短い列（**曲ではなく効果音**）
* 呼び出し箇所から意味が確定した id:

  | ax | 意味 | 呼び出し元 |
  |---|---|---|
  | `0x0602` | 受理 | `0x1a83`, `0x2201` |
  | `0x0402` | 拒否 | `0x22c5`, `0x218b` |
  | `0x0702` | 実行不能 | `0x22d3` |
  | `0x0302` | 国が滅亡 | `0xb19e` |
  | `0x0f03` | 終了 | `0xb1fa` |

* 演奏構造体は **`DS:0x3afa` から 0x20 バイト刻み**（`0x3afa`, `0x3b1a`, …)。
  `0x16bf` が 15 ワード零クリアし、`[di]=1`、`[di+2]`=データ先頭、
  `[di+4]=0x80`、`[di+0x1d]=0xff`、`[di+0x1f]=0x0f` を置く。
  既知の欄: `+0` 残り長さ / `+2` データ位置 / `+5` 音色下位 / `+6` 音量 /
  `+7` 直前の音程 / `+0x0a` フラグ / `+0x11..0x15` ピッチ包絡 /
  `+0x1f` 有効ビット

### 列の書式（`sub_0f2d`）

* `0xf0` 未満の対 = **(長さ, 音程)**
  * 長さバイト: **bit7 = 直前のキーオフを抑止**、下位7bitが tick 数
  * 音程バイト: **bit7 = タイ**、下位7bitが **`(オクターブ<<4)|半音`**
    （半音 0..11）。id 15 は 3-7, 3-0, 2-7, 2-0, 1-7, 1-0 と五度で下降。
    id 0 の `9b a2 a5 ab b2` はタイ付きの上昇スラー
* `0xff` = 終端
* 命令は `sub_1175`。`(命令-0xf0)*2` で **`CS:0x11a6`** のジャンプ表へ飛ぶ。
  **飛ぶ前にオペランド1バイトを読む**ので、16個すべて最低1バイト取る

  | 命令 | 飛び先 | 意味 |
  |---|---|---|
  | `f0 nn` | 1310 | 音色。上下ニブルに割る（低位<<4 が `[si+5]`） |
  | `f1 nn` | 132e | **音量** `[si+6] = nn`（`0f` が最大） |
  | `f6 c r ofs16` | 120e | **ループ**。`[bx]` を直接 dec し、0 なら `[bx+1]` で 補充して先へ、さもなくば 16bit 分だけ後ろへ戻る。**データを書き換える**ので移植側は複製か外部カウンタが要る |
  | `f7 w1 w2 b` | 1351 | **ピッチ包絡**。`[si+0x11]`, `[si+0x13]`, `[si+0x15]` に置き、`[si+0x1f]` の bit3 を落として bit0 を立てる（5オペランド） |
  | `fb nn` | 1332 | 音量を1下げる（0 で止める） |
  | `fc nn` | 133e | 音量を上げる |
  | `fd nn` | 1225 | `[0x3b48]` のチャンネルマスクを立てる |
  | `fe nn` | 13de | 全チャンネルを 0x7f にして黙らせる（`sub_13df`） |
  | `ff` | 127f | 終端 |

  他に `f2`(11fa) `f3`(11fe) `f4`(134d) `f5`(1208) `f8`(1373) `f9`(1398)
  `fa`(13d6) が未確認

### 二つのプレイヤ（FM と SSG）

同じ書式を読む解釈器が**2本並んでいる**。どちらも「長さ(bit7=休符) / 音程(bit7=タイ)」
の対を読み、命令だけ別の表を使う。

| | 入口 | 命令 | キーオン | 音程 | 音量 |
|---|---|---|---|---|---|
| FM | `0x0e70` 付近 | `sub_1170` | `sub_1425`（`0xf0\|ch` → reg 0x28） | `sub_1518` → reg `0xa4+ch` / `0xa0+ch` | `sub_1488` → TL `0x40..0x4c+ch` |
| SSG | `sub_0f2d` | `sub_1175` | （なし。ミキサとレベルで鳴る） | `sub_1541`+`sub_1539` → reg `ch*2` / `ch*2+1` | reg `8+ch` |

キーオフはどちらも `sub_1415`（`0x00\|ch` → reg 0x28）。

チャンネル構造体は 0x20 バイト刻みで2ブロックに分かれている:

* **FM: `DS:0x3a3a`, `0x3a5a`, `0x3a7a`**（3本）。`sub_13df` が
  `si=0x3a3a`, `cx=3`, `add si,0x20` で回して全部黙らせる（命令 `0xfe`）
* **SSG: `DS:0x3afa`, `0x3b1a`, …**。`0x0d8b` / `0x0d91` が
  `si=0x3afa` と `si=0x3b1a` に対して `sub_0f2d` を呼ぶ

`[si+0x1e]` はそのブロック内のチャンネル番号（0..2）。
`[si+4]` の bit 0x40 が立っていると SSG 経路を飛ばす（`0x1021`）。

### 音程の2つの表は隣り合っている

`DS:0x244d` が FM の F-number 12語、その直後 **`DS:0x2465`** が SSG の周期表。
`0x244d + 12*2 = 0x2465` ちょうど。FM 表の末尾に「謎の4語」があると見えたのは
SSG 表の先頭を読んでいただけだった。

* FM: `F-number = 表[半音] + [si+8]`、`(オクターブ<<3)` を上位バイトへ。
  ブロック4で C=261.52 / A=440.10 Hz
* SSG: `周期 = (表[半音] + [si+8]) >> オクターブ`。
  オクターブ0で A=27.50 Hz（A0 の定義値）。上のオクターブは切り捨てで粗くなり、
  A4 は 442.55 Hz になる。**これは原作の音**

### 音量→トータルレベル（`sub_1488`）

`0x48 - 音量` を `[0x3b42]`（主音量）で掛けて TL を作り、`DS:0x2445` の
音色ごとのマスクでビットが立っているオペレータにだけ `0x40/0x44/0x48/0x4c + ch`
へ書く。つまり**実行時に触るのは TL だけ**で、音色本体は初期化で置かれた一式。

### 実装の現状

* `src/sound.c` — 列の解釈（両プレイヤ共通の書式）。19個の効果音すべて完走
* `src/ssg.c` — SSG（矩形波3ch＋17ビットノイズ＋ミキサ）。描画した PCM の
  最強周波数を測る試験つき
* まだ無い: **OPN コア（FM 4オペレータ×3ch）**、FM 側の命令表 `sub_1170` の
  中身、音色一式の在処、`FM0nn.DAT` の曲、WebAudio への引き渡し

耳で確認できない環境なので、**PCM を描画して周波数を測る**のが唯一の検証手段。
これで「分周が2倍で1オクターブ低い」を捕まえた。

### 残っている大物

1. **発音**: `sub_10ee`（キーオン・周波数）、`sub_111e`（キーオフ）、
   `sub_114b`、`sub_1488` と、`(オクターブ<<4)|半音` → F-number の表
2. **OPN コアの実装**: FM 3ch × 4オペレータ ＋ SSG 3ch。YM2203 に LFO は無い
3. **曲**: `FM0nn.DAT` の書式（効果音と同じ解釈系に載っている可能性が高い。
   `sub_0f2d` を複数の構造体に対して回している）
4. WebAudio への引き渡し（AudioWorklet で WASM から PCM を引く）

いま入っているのは僕が合成した短い確認音だけで、原作の音ではない。

## 罠

* **PROG.BIN には 8086 の未定義エンコードがある。** 0x0084 の `fe e6` は
  `FE /4` すなわち `inc dh`。capstone も objdump も**両方これを拒否**するので、
  線形逆アセンブルがここで同期を失い、その先が全部ゴミに見える。
  「1 バイト余計に挿入されている」ように見えるが**そうではない**
* **Ghidra は生バイナリにエントリが無いと 0 関数になる。**
  `SetEntry.java` を `-preScript` で挟む。`sh tools/ghidra.sh` が
  展開後イメージを作ってから流すので、**330 関数・失敗 0** で出る。
  展開前に流していたときは偽関数 802 個でほぼ使えなかった
* **Ghidra 同梱の JDK を analyzeHeadless.bat は自力で見つけられない。**
  `JAVA_HOME=C:/prog/ghidra/jdk-21.0.12.1+1` を渡す
* **ファイル末尾を越える `call` は「圧縮されている」の兆候だった。**
  展開前は 2132 件中 351 件が 0xa438 を越え、そのうち 0x00b7 の
  `call 0xc6c2` は前後が一意に確定する位置にあった。展開後は全部収まる
* **Bash ツールのヒアドキュメントはバックスラッシュを食う。**
  バックスラッシュを含む編集は Edit/Write ツールで
* **Windows は大文字小文字を区別しない。** `cp a.mid A.mid` は自分自身への
  コピーになり、続く `rm` が原本を消す
* **planar と packed はサイズが同じことがある。** 4 通り全部 PNG に出して見る
* **エントロピーだけで「圧縮されている」と判断してはいけない。**
  PROG.BIN は全域 7.0〜7.3 だが生のコード＋データ表である（この作業で 3 回
  同じ間違いをしかけた）
* **`subprocess` に相対パスを渡すと CreateProcess が見つけられない。** 絶対パスにする
* **GUI は出さない。** PNG に描いて確認する（user が同じデスクトップで作業中）
